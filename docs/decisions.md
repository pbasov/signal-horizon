# SIGNAL HORIZON — Decision Log
### Living record of engineering & design decisions (ADR-lite)

> One row per decision. **Status:** `PROPOSED` (recommended by scoping, awaiting owner confirmation) · `ACCEPTED` (locked) · `SUPERSEDED` · `OPEN` (needs a call). Keep this current — a decision that isn't written here didn't happen. Source tags: GDD = design doc, PLAN = implementation-plan, SCOPE = scoping, SPIKE = prototype spike.

---

## Locked-in product/design framing (from GDD v0.5 + critique)

| ID | Decision | Status | Rationale / source |
|---|---|---|---|
| DD-1 | **Monochrome machine, living signal.** OS chrome strictly 1-bit; game-space signal (orrery, links, packets, live data) is coloured; freshness = saturation draining to grey. | ACCEPTED | GDD §8. Resolves the v0.4 aesthetic-vs-legibility risk. |
| DD-2 | **Visualization is priority zero** (the make-or-break pillar) — packets-in-flight + the growing coverage web are M0, not polish. | ACCEPTED | GDD §2/§5, critique (CS2 trap). |
| DD-3 | **Currency flip is optional, gradual, € stays relevant.** Information-dominance is an opt-in victory path, not a forced rug-pull. | ACCEPTED | GDD §4.10. |
| DD-4 | **Brokering gated + margin-capped** so it complements (never replaces) the carrier fantasy. | ACCEPTED | GDD §4.4. |
| DD-5 | **No "AI" framing.** Autonomy is diegetic flight-software/expert-systems. The optional local LLM is **cut from v1**. | ACCEPTED | GDD §4.6 (~85% negative AI sentiment). |
| DD-6 | **Multiplayer & mobile = affordance only**, no version target; v1 ships single-player desktop. | ACCEPTED | GDD §6/§7. |
| DD-7 | **M1 is a kill-gate.** If the 30-min light-delay fun-test fails, iterate visualization/core or stop — do not build M2. | ACCEPTED | PLAN §M1. |

---

## Spike decisions (TS/Three.js)

### SD-1 — Stack: TypeScript + Three.js, browser-first
**Status: ACCEPTED.** The spike evaluates whether the DD-10 tiling-WM shell + 3D orrery build more naturally on a web stack than on Godot. Developed and deployed in Chromium.

### SD-2 — No Tauri; browser is the platform
**Status: ACCEPTED.** The app runs in the browser (Chromium). Tauri wrapping is deferred indefinitely — the browser IS the deployment target for now. This removes the WebKitGTK validation gate entirely. The code remains standard web APIs only (no Chrome-only extensions), so a Tauri wrap remains a future option if needed.

### SD-3 — Port the Kepler truth layer to TS; pin against a C# golden master
**Status: ACCEPTED.** `Ephemeris.cs`/`OrbitalBody.cs` ported verbatim to `src/sim/ephemeris.ts`. TS `number` is f64 natively. Bit-identical to the C# golden master. Vitest pin holds `1e-9` tolerance for cross-machine libm robustness.

### SD-4 — Reuse the canonical dataset by symlink (no copy)
**Status: SUPERSEDED by SD-12.** `data/system.json` was a relative symlink to the Godot project's file, with Vite `server.fs.allow` widened to read across trees. Replaced by an in-repo vendored copy so the project clones and runs with no external dependency.

### SD-5 — Floating origin + per-preset log-compression in the orrery
**Status: ACCEPTED.** Mirrors `render/FloatingOrigin.cs`. f64 subtract → f32 cast. Per-preset radial log-fold. The fold is a visual lie only — it never feeds distance/light-delay math. Constant-screen-size dithered billboards.

### SD-6 — Presets are data; WM is a constrained zone-grid (DD-10), Swap + Resize wired
**Status: ACCEPTED.** `zonegrid.ts` implements the DD-10 model. Title-bar drag → Swap and edge-resize are wired. Tab/Split/Close exist in the data shape but are not gesture-wired.

### SD-7 — Sim clock is an f64 accumulator (replaced by fixed-tick clock)
**Status: SUPERSEDED by P0-03.** The spike used an f64 accumulator (non-deterministic). Production now uses an integer fixed-step clock: `SimClock` holds an integer `tick`, each tick is exactly `DT = 1/60` sim-seconds. `seconds` is derived as `tick * DT`. The `TickScheduler` pattern (`scheduleWall()` → drain `nextTick()`) ensures time-acceleration scales tick count, never DT. Same tick count = same sim state.

### SD-8 — SYSTEM.LOG: real packet events + a scripted severity feed
**Status: ACCEPTED.** Packet launch/arrival and genuine LoS occult are real. Solar conjunction is rare, so the log is also fed a deterministic sim-time-paced flavour stream.

### SD-9 — Verify in headful ungoogled-chromium via Playwright (+ MCP)
**Status: ACCEPTED.** `tools/shoot.mjs` drives the user's ungoogled-chromium (148) through `playwright-core` for headful, real-GPU screenshots.

---

## Stack migration decision

### SD-10 — Migrate from Godot C# to TypeScript + Three.js (browser)
**Status: ACCEPTED.** The spike (see `FINDINGS.md`) proved the UX builds at least as naturally in the web stack:
- Kepler truth ported bit-for-bit (f64 is native in TS)
- Tiling WM, 3D orrery, packet honesty, keyboard presets all work
- HMR iteration is dramatically faster
- No numerical-fidelity loss

The app runs in the browser. No Tauri gate — see SD-2. The remaining engineering work is the deterministic fixed-tick + save/replay in TS (bounded, low-risk; the pure layer is already bit-identical).

**Supersedes DD-11** (C# on Godot .NET). Design decisions DD-1 through DD-7 and the full GDD remain the design authority; only the *engineering stack* changed.

### SD-12 — Vendor the canonical dataset as a real file (supersedes SD-4)
**Status: ACCEPTED.** `data/system.json` is now a real, committed file in this repo — a vendored copy of the Godot project's canonical dataset (byte-identical at vendoring time, sha256 `d2bee65a…`). Replaces the SD-4 symlink so the repo is self-contained and clones/runs anywhere with no external Godot tree present. Consequences: (1) `vite.config.ts` no longer hardcodes absolute machine paths and the `server.fs.allow` cross-tree widening is removed (root defaults to cwd — the package dir under `npm run {dev,build,preview}`); (2) the two datasets are now decoupled — if the Godot `system.json` changes, re-copy it here to resync (noted in `README.md` and `src/sim/system-data.ts`). Verified: `tsc --noEmit`, `vite build`, dev server, and 37/37 Vitest all green.

### SD-11 — No UI framework; imperative DOM and Three.js only
**Status: ACCEPTED.** No React, Vue, Svelte, or any reactive/reconciliation layer. The frame loop is `requestAnimationFrame → sim.tick() → orrery.update(state) → renderer.render()` — no diffing, no virtual DOM, no scheduling, no effect lifecycle. Three reasons: (1) The orrery renders to a WebGL canvas — a framework can't schedule or diff GPU draw calls; a React-Three-Fiber wrapper adds JS overhead before the same GL calls. (2) A real-time sim updates every frame — position, packets, freshness all change every tick. Reactive frameworks optimise for skipping unchanged subtrees, but there's nothing to skip; the reconciliation cost is pure overhead. (3) GC pressure: each framework render cycle allocates vdom nodes, memo objects, and effect cleanup closures. At 60fps (~16ms/frame) this competes with the sim and orrery for the frame budget. The adversarial review caught ~960 `Vector3`/frame in Three.js — a framework's allocation pattern would compound this. Panels use `element.textContent = newValue` in the frame loop. If panel complexity grows later, the answer is writing that component imperatively, not introducing a framework.

### SD-13 — GDD bumped v0.5 → v0.7 (Pillar 7 + network topology)
**Status: ACCEPTED.** Source GDD. Two design additions land in the design doc: (1) **Pillar 7 "Leverage compounds"** (§4.11) — capability is discovered through operation, not bought; there is no research building and no tech-point currency. The unit of command rises over a run: asset → fleet → declared intent. The concrete tech tree is **DEFERRED post-M1** (the M1 fun-gate does not depend on it). (2) **§4.3a network topology** — forgiving RF access links (easy, lossy, ubiquitous) versus a scarce point-to-point laser backbone; terminals are finite and exist on backbone nodes only. Governance splits into a Level-1 policy floor and a Level-2 construction ceiling. The trace-view is an "mtr" for the link graph. Records the version bump only — the GDD itself is the authority; this log does not restate it.

### SD-14 — Implementation plan v0.1 → v0.2 (supersedes; v0.1 deleted)
**Status: ACCEPTED.** Source PLAN. The old `docs/signal-horizon-implementation-plan-v0.1.md` was the pre-spike Godot/GDScript plan and is **deleted**. The live plan is `docs/signal-horizon-implementation-plan.md` (this is v0.2), which reflects the TS/Three.js move (SD-10), the M0 spike done, and **M1 as the live fun-gate frontier** (DD-7). v0.2 supersedes v0.1; all references re-point to the live filename.

### SD-15 — Port the save/replay backbone + M1 economy from the tested C# reference
**Status: ACCEPTED.** Source PLAN/SPIKE. The save/replay backbone (P0-05/06) and the M1 economy are **not greenfield** — they are ported from the complete, tested C# reference at `/home/basov/Games/Godot/galaxy-link/SignalHorizon.Sim/` (`SaveGame`/`SimAction`/`StateHash`/`SimScheduler` + `M1/{Cache,Coherence,Demand,Resolver,M1Economy}` + `Tests/{SaveReplayTests,M1ModelTests}.cs`).

**Finding from B1 — the C# golden state-hash is NOT bit-portable to TS.** `StateHash` was ported byte-for-byte to `src/sim/state-hash.ts` (verified: identical fold constants `Mult=1000003`/`Seed=1469598103934665603`, little-endian IEEE-754 byte order, sort order, hashed quantities, unsigned-u64 fold). Yet `canonicalHash` over the golden ticks `{0,3600,86400,2592000}` at `dt=1/60` yields **12899997400407946598** in TS vs **15552073864691245897** (unsigned u64) in C#. Cause: of the 96 folded position doubles, two differ in their lowest 1–2 mantissa bits (`sat_meo_inc.z@t0`, `mars.y@t3600`) — V8-vs-.NET `Math.sin/atan2/sqrt` rounding, the same sub-ULP divergence `ephemeris.test.ts` already tolerates at `REL_TOL=1e-9`. A raw-bit hash has full avalanche, so 1 ULP scrambles the result; matching C# would need an fdlibm-identical trig port (out of scope). **Resolution:** pin the TS-native value as the determinism guard, record the C# baseline as provenance. P0-06 is therefore an *intra-runtime* TS determinism proof (replay → identical TS state), not a cross-runtime C# equivalence proof; the **replay golden (B3) should fold the action-driven mutable state** (clock tick, RNG state, economy integers — all exactly representable), not the transcendental ephemeris positions. The TS pin is stable for a fixed V8/Node (D1 pins Node 26).

Two C# porting traps still hold for SaveGame/replay: (1) `dt = 1/60` is stored as exact IEEE-754 bits and **does not survive JSON** — preserve the bit pattern, do not round-trip through a decimal string; (2) replay must use the **unclamped scheduler kernel** (`SimScheduler`), because the live `SimClock` drops steps under fast-forward and would diverge.

### SD-16 — Defer a single SimState/World consolidation (conscious deferral)
**Status: ACCEPTED (conscious deferral).** Source PLAN. Sim mutable state is small and well-defined — the clock tick, the RNG state, and a few `Mission` fields — so P0-05 serialises it directly rather than introducing a god-object `SimState`/`World`. No consolidation refactor before the economy lands. Revisit only if M1 state sprawls.

### SD-17 — E1 design-conformance: 3-level coherence + sloped stale-band price (GDD §4.4 over the C# port)
**Status: ACCEPTED (user-approved).** Source GDD §4.4. After the faithful E1 port (commit 3dafb70), a GDD-grounded design review found two M1-scoped gaps where the prior C# port under-served the design authority; both applied as a *separate* commit so the faithful port stays a clean baseline. (1) **Coherence is 3 levels, not 2.** The C# port (and the M1-07 ticket) had Eventual/BestEffort; GDD §4.4 names *strong/eventual/best-effort* — three. Now a cheap→premium ladder `EVENTUAL(floor 0.5, cadence 7200s, cost 1.0×) < BEST_EFFORT(0.9, 1800s, 3.0×) < STRONG(0.98, 600s, 6.0×)`, so the spend-vs-staleness dial is a real choice, not an on/off toggle. **GDD outranks the plan**, so M1-07's "two levels" is superseded. (2) **The stale-band price is a slope, not a flat tier.** The 3-band step left the whole `[min,fresh)` band paying a flat €400, so tuning freshness within it bought nothing — but the gate measures whether *optimising* is fun, which needs a gradient (GDD §4.4: a freshness penalty that *scales* with staleness). `Demand.priceCurve` now defaults to `"ramp"` (continuous LERP €400→€1000 across the stale band, keeping the hard min-cliff and fresh-cap), with `"step"` retained for an in-gate A/B. Both are pure-sim, behind tests covering all 3 levels and both curves. *(Latent: the ramp divides by `freshFreshness − minAcceptableFreshness`; guard or validate when scenario configs set these — E6.)* Full §4.4 depth (brokering, prediction, multi-slot, dynamic demand, the €→information flip) remains deferred per the review.

### SD-18 — GDD v0.7 → v0.8 (name the fun + give escalation a mechanism)
**Status: ACCEPTED.** Source GDD (author revision, commit 74dc6fb). Records the bump and its three additions: (1) **§3a "What the Fun Is"** — the spine is *taming the sprawl* (chaos→order, visceral, measured against *functional*) and the mastery layer is *optimising against the parse* (order→optimal, cerebral, measured against *optimal*); the same verb at two bars, hinged on a legible record. Design constraint: the floor verb and the ceiling verb must be the same verb. (2) **§3b "The Escalation Engine"** — replaces "bigger gap" with three endogenous generators (demand growth · freshness decay · earned automation) + the across-tier rule "each new tier must invalidate a strategy that worked the tier before, or be cut." (3) **§4.12 "The Legible Record (The Parse)"** — the combat-log for information delivery, promoting the optimisation layer (§4.2/§4.3a) from footnote to spine. **The M1 gate is sharpened** to "does the player finish a run wanting to look at what happened and do it better?" Consequence for the build: E3's action log + Resolver-outcome stream is the substrate of the parse; **E6 telemetry should surface a player-facing post-run record, not just hidden gate metrics** (tracked on the E6 task).

### SD-19 — E3: M1 economy + prefetch-as-logged-action + the honest-freshness breathing loop
**Status: ACCEPTED (user-approved "make it breathe").** Source PLAN/SPIKE + user. (1) `M1Economy` ported (balance/applyPayout/chargeOpex×coherence.costMultiplier/chargePrefetch/runway/bankrupt) and wired into `M1Session`; the good-vs-bad solvency gap runs through the real economy. (2) **Prefetch is the first logged player action**: `session.prefetch()` (€50, one-fetch-in-flight gate) recorded as a `SimAction` at `clock.tick`; the previously-unlogged pause/faster/slower mutations are now recorded too — closing the reproducibility gap (P0-05). (3) **Honest-freshness breathing loop**: the cache captures the fetched sample at its Earth-*launch* instant, so on arrival it is honestly one-way old — freshness `2^(-oneWay/halfLife) ≈ 0.84` (a paying *stale* hit, never perfectly "fresh", which is the correct physics), yielding a ~44-min fresh-hit window where no fetch is in flight and prefetch is a usable lever (top-up before a conjunction blackout). Earlier the cache effectively re-missed near-continuously, starving the prefetch lever. (4) **Determinism with player actions**: live and replay apply a recorded prefetch at the *same* point (after `step(at_tick)`) via a shared `applySessionAction`, so a recorded log replays bit-identically even when `step()` would itself start a competing miss-fetch (guarded by an adversarial competing-case test — do not delete it; the ordering-regression teeth live there).

### SD-20 — E6a: continuous per-sim-time economy (per-tick → rates)
**Status: ACCEPTED (user: "continuous rates, tune later").** The per-tick economy (€1/tick opex) bankrupted the live loop in ~17 sim-s — opex was coupled to the 1/60 s tick (~60 €/sim-s) while the first delivery is ~923 s away — and per-tick payout would have ballooned once deliveries landed. Reworked to **continuous per-sim-time rates**: `balance += (revenueRate(band) − opexRate(coherence)) × dtSeconds`, so the wallet is invariant to DT / time-compression (adversarially verified bit-identical at 1×/10×/100× and coarse frames). Rates (€/sim-second, EVENTUAL baseline × coherence.costMultiplier): opex 2.0, fresh +5.0 (net +3), stale +2.5 (net +0.5), miss 0 (net −2), blackout −4 penalty (net −6); opening €3000 (~25-min opex runway, survives the ~923 s wait to first delivery); prefetch €50 one-shot. Survivable-if-played-well (well-played 30-min ≈ €8k) vs bankrupt-if-starved (≈ −€7.8k); the pre-blackout prefetch flips solvent↔bankrupt. **Numbers are placeholders — tune later.** Replay golden re-pinned; finance panel reads €/hr rates + live-burn runway.

### SD-23 — E9: the truthful event log (the parse seed / GDD §4.12 legible record)
**Status: ACCEPTED (autonomous gameplay decision per "make gameplay decisions yourself").** GDD §4.12 makes the *legible record* the floor/ceiling hinge — the thing that turns taming-to-functional into optimising-to-optimal — and demands it be COMPLETE + HONEST, "cheap because the sim IS the truth layer." E9 builds the M1-era seed: log truthfully from day one so E10's post-run parse (and the §9 "did the player finish wanting to do it better?" gate) has real data. New pure module `src/sim/m1/eventlog.ts`: `M1Event` is a discriminated union of 8 kinds — `serve` (band transition from→to), `fetch_launch` (cause), `fetch_arrive` (true landedFreshness), `cache_store`, `cache_evict` (victim + freshness + forBy + reason), `prefetch` (cause manual|auto|prestage + eta + costEur), `policy` (mode + floor + from), `blackout` (enter|exit) — each carrying a SIM timestamp (integer tick + tSim, never wall-clock) + a monotonic `seq`. The `EventLog` buffer is append-only (`push`/`readAll`/`readSince(seq)`); it lives unbounded on `M1Session` as the recording log, the panel reads a bounded tail. **All EDGE-TRIGGERED** (per-feed `prevBand`/`prevFeasible` + `prevPolicyMode` gate emission to genuine transitions — never per-tick level; a 200k-tick run emitted only 52 events).

**Honesty:** SYSTEM.LOG now renders ONLY `session.events` — `main.ts` discards `mission.*()`'s scripted LogEntry returns (mission.ts is kept only as the orrery packet director, fully de-wired from the log), so the SD-8 scripted severity feed never reaches the screen. The known cosmetic lie ("PKT 0.50 arrived") is fixed at root: the `fetch_arrive` event carries the real `delayFreshness(t−launchT)` (≈0.70–0.89, decaying), pinned by `log-format.test.ts`. New pure `Cache.evictionVictim()` lets the session LOG an eviction truthfully *before* the store mutates the cache (mirrors `evictStalest`'s lowest-freshness + insertion-order tie-break).

**Determinism (the key honesty guarantee):** events are emitted purely from deterministic state transitions in a fixed order → the event stream is a pure function of (eph, action log, dt) and replays BIT-IDENTICALLY (new `eventlog-replay.test.ts` records then replays and diffs the full ordered sequence + payloads). The stream is a **derived side-output kept OUT of `SessionSnapshot`/the state hash**, so the replay **golden is unchanged** (`8072561960299808504n`) — events proven reproducible separately rather than re-pinned. *(Consequence flagged for E10: a future at-rest fast-load would resume with an empty live log + reset edge-trackers, so the first post-load step could emit a spurious `from=null` serve transition. Not reachable today — no at-rest load is wired.)*

**Render:** new pure DOM-free `src/panels/log-format.ts` maps each event to a `LogRow` (severity row-class + typed tokens) per GDD §8 — severity (info/warn/error/crit; blackout/blackout_miss = crit, prestage/evict/stale = warn), entities (feed/dataset IDs in the violet identifier colour), time+freshness (a `warmthOf()` bucket good/watch/warn/dead so a stale value desaturates toward machine-grey — the §8 freshness ramp), € values. `log.ts` drains only the new tail via `readSince` and appends incrementally (no per-frame whole-list rebuild, zero alloc on a quiet frame, 400-row DOM cap). 237→261 tests; tsc clean; build clean; src/sim stays pure; screenshot-verified the truthful syntax-highlighted log. Minor follow-ups (→ later): a dead `.ts.stale` CSS hook never emitted; the de-wired scripted `mission.ts` left in-tree (churn-avoidance).

### SD-40 — M1 is ONE game (real systems + state-gating), built Act-1-first on `src/sim/net/` (supersedes the isolated-Act-1 framing of SD-38)
**Status: ACCEPTED (user: "design M1 as one game, build Act 1 first on the real systems indeed").** SD-38 framed Act 1 as a sealed, self-contained slice — and I mis-scoped it twice (a contraption with a bent-pipe stub + co-located ground + an `A1_DISH_EIRP=1.1` closing-lever + a `windowCoverage 0.802` / `A1_AVAILABILITY_BAR=0.85` forced imperfection + its own throwaway golden; then an over-corrected relay/solver puzzle). The user supplied the **onboarding script** (`docs/signal-horizon-m1.md` Part IV, the state-gated director's script) and corrected the frame: **M1 is ONE game** — the real connectivity/coverage + launch-planner + routing-solver + fault systems running under a **curated, state-gated arrival sequence** (the contract is the teacher; only the *order* contracts/faults arrive is scripted; the systems run for real). The four acts are **gated beats**, not prototypes; Act 1 is the first slice ON the real systems. See [[m1-scope-defer-to-docs]].

**Build-ready design: `docs/signal-horizon-m1.md` Part II** (produced + adversarially verified against the onboarding script; verdict build-ready). Key locks:
- **Namespace `src/sim/net/`** ("the network game", Acts 1–3). The OLD `src/sim/m1/` (cache economy) is the reclassified Act-4 teaser and is left alone — the `m1/`→`m1-economy/` rename is **DEFERRED** (it touches 7 import sites incl. `m2/session.ts`, which owns the M2 golden path). `net/` imports neither `m1/` nor `m2/session.ts`.
- **Migrate from the stopped build's `a1/`:** the spinning-Earth frame, the toy-radius GEO-parks pacing (`ω == meanMotion` bit-equal), the sat atom, the region geometry. **DROP the contraption** (bent-pipe stub, co-located ground, dish EIRP, 20° clip mask, `windowCoverage`/0.85 bar, throwaway golden).
- **Act 1 = WINNABLE BY THE DEFAULT, no forced imperfection.** Equatorial region re-centered to lat 0; the parked equatorial GEO covers the **whole** disc — pinned as `coveredFraction === 1.0` at eirp 1.0 with margin (so no edge-clip soft-imperfection sneaks back). One connectivity-only contract (avail/latency/bandwidth fields present in the struct but HIDDEN via an `activeAxes` mask); one verb (LAUNCH); gate = served + revenue positive.
- **Generalizes with no rework:** the 3-axis contract + `activeAxes` mask (acts add an axis via a scenario one-liner); the real router degenerate in Act 1 → reactive latency+congestion blend by Act 3 (`w_stab=0`); faults derived off a seeded stream, fenced behind Act-3a re-stabilization (3a/3b are separate cursor beats); the scenario engine touches only demand/mask/fault-enable, never physics.
- **Contract reuse (no drift):** `net/contract.ts` is a fresh region-disc struct (M2's contract is grid-cell-coupled) but **shares `m2/contracts.ts`'s state machine + field names and imports `stepActiveContract`/`stepOfferedContract`/`BREACH_GRACE_SECONDS`** — ONE breach convention, two demand geometries.
- **Determinism:** own `NetSession` + own seed (`4242424242424242n`) + own golden in `src/sim/net-replay.test.ts`; `activeAxes` folded by a FIXED axis ordinal. The two existing goldens (`544847093270497462n`, `8431658617016421069n`) stay untouched.
- **Build plan:** Phase A (A0–A4) builds the shared systems + the Act-1 beat as the smallest provable-fun slice (Act-1-shippable); Phases B/C/D add Acts 2/3/4 as gated beats on the same systems. The two-layer human gate (≥5 cold testers) runs after.

#### SD-40·A2 — the net/ contract REUSE: a fresh region-disc struct that SHARES the m2 state machine (design §2.2)
**Status: ACCEPTED (built; the loop closes in-session).** Records the §2.2 contract-reuse decision now that A2 (contract + session + apply-action) is built. **Why a fresh struct, not the m2 `Contract`:** `m2/contracts.ts`'s Contract is **grid-cell-coupled** — its region is `cellIds: number[]` resolved against the `GeodesicGrid` and its served fraction is `coverageDimsAt` over those cells. The net/ game's demand is a **region/point geodesic disc** on the spinning frame, served via the router's path-existence; a grid-cell contract cannot express a router verdict without dragging the whole `GeodesicGrid` coupling into the new game. So `net/contract.ts` is its own struct (3 SLA axes present + an `activeAxes` gate mask + `prefer` weights + the `SLA_AXIS_ORDINAL` fold key). **Why this is NOT a drift hazard:** it reuses the m2 **state machine and field names verbatim** (`state`/`termSeconds`/`servedSecondsAccum`/`breachSecondsAccum`/`lastServedFraction`/`earnedEur`/`offerExpiresAtS`, and the imported `ContractState` vocabulary), and `net/session.ts` **imports the m2 transition helpers** `stepActiveContract`/`stepOfferedContract` (bound through a tiny `SharedStateMachineContract` lens — the field subset the axis-agnostic helpers actually touch) so the breach/complete transitions and the single `BREACH_GRACE_SECONDS` are the **same code path** the m2 build session uses. **ONE breach convention, two demand geometries:** net/ computes the scalar `servedFraction` from the ROUTER (Act-1 binary: 1.0 served / 0.0 not) instead of from m2's grid coverage, then passes it into the IMPORTED `stepActiveContract(contract, servedFraction, dt)`. The net contract carries `offerExpiresAtS = Infinity` (no Act-1 offer window) so the shared offered-step is a deterministic no-op without a net/ fork. **Determinism:** revenue is accrued as one summed wallet add per step (DT-invariant, the SD-20/m2 pattern); the `activeAxes` mask folds by the FIXED `SLA_AXIS_ORDINAL` (never Set order). **Action boundary:** three ADDITIVE kinds in `action.ts` — `net_launch` (radians + SI metres + `count`), `net_accept`, `net_set_prefer` — applied via the shared `applyNetAction` (step-then-post-drain, live==replay). The two existing goldens (`544847093270497462n`, `8431658617016421069n`) are byte-for-byte untouched; `action.ts` is additive only; net/ imports NEITHER `m1/` NOR `m2/session.ts` (only the axis-agnostic `m2/contracts` helpers + the `coverage/field` formulas via link-budget). The net/ replay golden lands in A3.

#### SD-40·A3 — the state-gating engine + the Act-1 beat + the M1 golden (design §3 / §4 / §5)
**Status: ACCEPTED (built; the Act-1 arrival sequence is deterministic end-to-end; golden pinned).** A3 adds the ONLY authored layer — `net/scenario.ts`, a **pure data table of `Beat`s + a deterministic gate evaluator** — and wires it into `NetSession.step`, plus the M1 replay golden in `src/sim/net-replay.test.ts`. **The beat shape (§3):** `Beat { id, emit(session,t), gate(session,t)->bool, fallback?(session,t)->Shortfall|null }`; `M1_SCENARIO = [act1, act2, act3a, act3b, act4]`. **act1 is the only LIVE beat in Phase A:** `emit` puts ONE equatorial latency-tolerant **connectivity-only** contract on the board (`activeAxes={connectivity}`, avail/lat/bw present-but-hidden); `gate` = one ACTIVE contract `lastServedFraction>0` AND `earnedEur>0` (served AND € rising — the launch→cover→paid chain actually paid); `fallback` = the gentle "footprint does not reach [region]; try the GEO PARK preset" (point at the fix, never do it), fired on a non-covering orbit or a long idle with no sat. **act2/act3a/act3b/act4 are STRUCTURAL placeholders** (empty `emit`, never-true `gate`) so the granularity is real from the start: the cursor steps act1→act2→act3a→act3b→act4, and 3a (escalation) and 3b (faults) are **separate cursor beats** — faults are fenced behind act3a per the onboarding ("not before re-stabilization"). Phase C fills them (C1=act3a, C2=act3b); act4 has NO gate (a read, not a gate) so the cursor stops there. **The wiring (§3, in the fold):** `step` (0) emits the current beat ONCE before serve/breach (so the Act-1 contract is on the board before its first solve), then (1) serve/breach + (2) revenue, then (3) evaluates the current gate AFTER revenue — so a contract served+paid this tick opens the next beat THIS tick; on the first true it records the gate tick (`round(t/dt)`), advances `scenarioCursor`, and emits the next beat — all deterministically in step. `emit()` only adds demand / (later) flips a mask / enables a fault gen — it NEVER touches the solver/physics. `Shortfall` is defined minimally in `scenario.ts` now (the full `diagnose(SolveResult)` parse is `trace.ts`, a later increment). **The golden (§4):** own seed `4242424242424242n`; a fixed Act-1 log (`net_launch` the default GEO PARK at tick 600, `net_accept REGION-0` at tick 1200, run 200 sim-s); the fold mixes wallet + RNG state + `scenarioCursor` + gate-tick stamps + a **fault cursor (0 placeholder, folded now so C2 moves the hash not the fold shape)** + each sat's full `SatOrbit` f64s + loadout eirps + each contract's state/accums/`lastServedFraction`/`earnedEur`/`offeredLoad`/`activeAxes` **by ASCENDING `SLA_AXIS_ORDINAL`** (the one canonical `NetSession.foldAxisOrdinals` path — never Set order). **Bootstrapped + PINNED `NET_REPLAY_GOLDEN = 10424955607522567073n`.** Verified: replay-twice bit-identical; live==replay (deep snapshot incl. SatOrbit f64s + the hash); SaveGame JSON round-trip reproduces the hash (dt survives via `dt_bits`); the act1 gate fires at a DETERMINISTIC tick (TICK_ACCEPT+1, since the accept lands post-step and the first served+paid step is the next tick); a fresh un-launched session never advances the cursor (state-gated, not clock-timed) and surfaces the gentle GEO-PARK fallback. The two existing goldens (`544847093270497462n`, `8431658617016421069n`) are byte-for-byte untouched; `net/` still imports neither `m1/` nor `m2/session.ts`; purity now scans `scenario.ts`.

#### SD-40·A4 — the Act-1 planner consequence-preview + the scoped net-mode render override (design §2.3 / §6)
**Status: ACCEPTED (built; the preview is consequence-truthful; the de-squash override is strictly scoped + byte-identical off-mode).** A4 adds the planner SIDE of `net/world.ts` + the orrery NET RENDER MODE — the two pieces that make Act 1 *legible* on the real systems (the interactive panel/loop wiring + the screenshot pass are the human's follow-up). **The planner (pure, in `world.ts`):** `LaunchDraft {semiMajorM, incRad, subLonRad, loadout, count}`; `Preset {id, label, draft, costBaseEur}` adapted from each `NetPreset` via `presetToPreset` (`GEO_PARK_PRESET` = the default that already mostly works, `LEO_SWEEP_PRESET` = the non-covering fallback); `draftToSat(draft, t)` builds the EXACT sat the live applier commits (epoch-correct `resolveOrbit` + `standardLoadout(NET_REF_LINK_DISTANCE_M)`); `launchDraftCost(draft, base)` = base + altitude term × count; and `previewLaunch(eph, world, draft, t, costBase) -> LaunchPreview {orbit, periodS, costEur, groundTrack, contracts[]}`. **CONSEQUENCE-TRUTH (the A4 verify, pinned in `net/planner.test.ts`):** the preview runs the SAME `router.solve` + `coveredFraction`/`isPointServed`/`orbitPeriodSeconds` the live world runs post-commit, so for any draft the preview's per-contract `{served, latencyFloorS, bindingConstraint}` EQUALS the post-commit `solve` for that exact orbit — what you see before commit is what you get after. The preview surfaces the FOOTPRINT (GEO PARK `coveredFraction === 1.0` over the whole equatorial disc — the Act-1 win, no clip; a fresh LEO covers partially — a truthful gap), the GROUND TRACK (the parked GEO holds a fixed sub-lon over the region meridian; the LEO sub-point WALKS in longitude — `subPointOf` maps the unforked `solveOrbit` inertial position back through θ(t)=ω·t), the PERIOD (GEO == the toy day; LEO shorter), and the per-contract latency floor. `previewLaunch` reads a structural `PreviewWorld {contracts, grounds}` so it never imports the concrete `NetSession` (the session satisfies it by its getters); the new `world.ts → router.ts` edge is a SAFE ES-module cycle (both only touch the bindings inside functions, never at module-eval) — golden + tsc unaffected. **The render override (`orrery.ts`, Decision-G, design §6):** a `netRenderMode` public boolean (DEFAULT OFF) + a PURE `static Orrery.computeOrbitScale(netMode, realSurfaceM, animatedBandM) -> OrbitRenderScale|null` that `refreshOrbitScale` delegates to. OFF → `surfaceM == eph.radiusMeters("earth")` (6371 km) + the live animated band + the SYSTEM-SCALE lift/exponent — **BYTE-IDENTICAL** to the pre-A4 de-squash, so every M1-cache / M2 / M3 framing is unchanged (asserted by re-deriving the original expression inline + `toEqual` across active/identity/dimensionless cases). ON → `surfaceM == A1_BODY_RADIUS_M` (300 km toy body) + `bandOuterM == A1_RENDER_BAND_M` (1.2·a_GEO), so the toy GEO/LEO radii fan out instead of log-folding to sub-pixel. **A toy-scaled lift was REQUIRED:** the toy band is ~1002 km over the 300 km body, so the system-scale 1.8e7 m lift (18 000 km, larger than the whole toy band) would invert the concave de-squash curve — net mode uses `NET_ORBIT_DESQUASH_LIFT_M = 1.2e5` (keeping the `lift < band−surface` `OrbitRenderScale` invariant) so the toy LEO lifts clear of the disc and GEO renders outside LEO (monotonic, macroscopic separation — pinned via `orbitRenderRadius`). The render-mode unit check lives in `src/orrery/orrery-net-mode.test.ts` (exercises the pure static helper — no DOM/WebGL needed). Verified: planner consequence-truth + render scoping GREEN (9 + 6 tests); tsc clean; purity scans `world.ts` (still pure — no three/DOM/clock/`Math.random`); the two existing goldens byte-for-byte untouched (`net-replay.test.ts` `10424955607522567073n` also unchanged — A4 added NO new source to the fold); `net/` imports neither `m1/` nor `m2/session.ts`; `action.ts` untouched this increment (the net kinds were complete at A2).

#### SD-40·B3 — the Act-2 beat + the NET REPLAY GOLDEN re-pin (the availability axis enters the fold; design §3.3 / §3.4 / §4.4)
**Status: ACCEPTED (built; the Act-2 loop closes deterministically; golden re-pinned per the M3a precedent).** B3 fills the `act2` `Beat` in `net/scenario.ts` and extends `net-replay.test.ts` with the full act1→act2 arc, re-pinning the net golden. **The beat (data only — never touches physics):** `emit` offers `REGION-1` (a second equatorial metro at lon 5° E, `NET_ACT2_REGION`) with `activeAxes = {connectivity, availability}` (the availability bar `ACT2_SLA_AVAIL = 0.99` becomes VISIBLE + ENFORCED for the first time — flipped on purely by the mask, NO struct reshape). `gate` = REGION-1 HELD continuous SERVED via a hand-off constellation across ≥1 full hand-off cycle, breach-free — **HARDENED** to a *sustained* clean window (`nowS − cleanServedSinceS ≥ NET_HANDOFF_CYCLE_S = 300 s = 2·A1_LEO_PERIOD_S`) AND `lastAvailability ≥ slaAvail`, so a single served tick mid-sawtooth cannot spuriously fire it. `fallback` = co-phasing specificity in CONSTELLATION terms ("a single satellite cannot HOLD this region — it MOVES… coverage requires a CONSTELLATION; you have K, place ~`ACT2_ZERO_GAP_N`"). **Over-build still completes** (the predicate is coverage-held, not a sat cap); the surplus is silently logged: `wasteLoggedSats = max(0, sats.length − ACT2_ZERO_GAP_N)`, recorded at the firing tick — the Act-3 optimizer-pull seed. **Session additions (additive, in the fold):** `cleanServedSinceS` (the gate-hardening start-stamp: pinned to `t` whenever an availability-active contract is not-yet-active OR feeds a 0 served-fraction, so a freshly-accepted contract must STILL hold a full hand-off cycle — never from `t = 0`) and `wasteLoggedSats`; both fold into the snapshot/state-hash, plus the per-contract `lastAvailability` readout (0 for a connectivity-only contract ⇒ byte-identical fold for REGION-0). The `activeAxes` fold needs NO shape change — REGION-1's `{connectivity, availability}` folds to ordinals `[0,1]` via the existing `foldAxisOrdinals`. **THE GEO-DOMINANCE NOTE (load-bearing physics finding):** on the toy body the act-1 parked GEO sees ~a full hemisphere, so it covers ANY equatorial region within ~85° of lon 0 — and I empirically confirmed (probes against the real `solve`/`windowAvailability`, then deleted) that a small LEO constellation achieves continuous point-coverage ONLY near lon 0 (the LEO node + ground + region coincident), exactly where the GEO dominates; there is NO region placement where the GEO fails AND a ≤6-sat LEO constellation holds (the GEO's reach and the LEO's coverable band are mutually exclusive). So the scripted golden's REGION-1 IS co-served by the act-1 GEO — this is the **seeded oversubscription** the canon design-note calls for (two contracts sharing infrastructure), realised honestly: the GEO is the over-build surplus the waste log counts (5 sats − 4 = 1). The PEDAGOGICAL physics (a lone LEO sawtooths and breaches the shared grace; N=2/3 fall short; N=4 holds rolling-avail = 1.0) is pinned LEO-ONLY in B1's `availability.test.ts` / B2's `phasing.test.ts` (the honest constellation measure, no competing GEO). **The golden re-pin (the M3a precedent, SD-33 pattern):** the replay log now runs act1 (GEO + accept REGION-0 + the act1 gate, which auto-emits REGION-1) then act2 (a `count = ACT2_ZERO_GAP_N = 4` LEO_SWEEP **BATCH** at `phaseSpreadRad = 2π/4` + accept REGION-1), to `MAX_T_SECONDS = 360` (the act2 gate fires at tick 19401 ≈ t 323 s, a full hand-off cycle after REGION-1 goes active). **`NET_REPLAY_GOLDEN` 10424955607522567073n → `12864209889064023665n`.** What moved it: the Act-2 REGION-1 contract, the `lastAvailability` fold, the `cleanServedSinceS` gate-hardening fold, the `wasteLoggedSats` fold, and the N=4 batch phase spread. Verified: replay-twice bit-identical; live==replay (deep snapshot incl. the new fields + SatOrbit f64s); SaveGame JSON round-trip reproduces the hash (the batch `phaseSpreadRad` survives); the act2 gate fires at a deterministic tick AFTER a sustained clean hand-off cycle (re-run to gateTick−1 ⇒ cursor still on act2); over-build (`count = 6`) still completes act2 and folds `wasteLoggedSats = 7 − 4 = 3`; cursor steps act1 → act2 → act3a. The two existing goldens (`544847093270497462n`, `8431658617016421069n`) are byte-for-byte UNTOUCHED; `net/` imports neither `m1/` nor `m2/session.ts` (only the axis-agnostic `m2/contracts` helpers + the single `BREACH_GRACE_SECONDS` — ONE breach convention); `net/` stays pure; the router signature `{served,path,latencyS,bindingConstraint,losses}` is unchanged. 506 sim tests GREEN; tsc clean. _REGION-1 lon offset, `ACT2_SLA_AVAIL`, `NET_HANDOFF_CYCLE_S`, and `ACT2_ZERO_GAP_N` are tuned placeholders pinned to the measured geometry — re-derive if the orbit family/body scale changes._

#### SD-40·B3-FIX — the Act-2 TEACHING-WALL fix: REGION-1 re-placed at HIGH LATITUDE (the latitude wall), router goes multi-ground, golden re-pinned
**Status: ACCEPTED (built; the four Act-2 wall invariants pinned; golden re-pinned).** B3 shipped REGION-1 as an **equatorial** metro at lon 5° E — and the B3 "GEO-DOMINANCE NOTE" above honestly recorded the consequence: lon 5° E sits INSIDE the parked equatorial GEO's footprint, so the act-1 GEO **already co-served REGION-1** and the act2 gate was satisfiable by the GEO alone (the over-build surplus = the GEO). That makes the second contract **solvable by Act-1's method**, which the onboarding (Act 2) forbids — the player never hits the availability wall. **The fix (variant (a), pure geometry — LATITUDE is the only Act-2 physics lever; latency is not a lever until Act 3):** the prior B3 framing assumed the GEO's reach and a small LEO constellation's coverable band were mutually exclusive, but that was an artefact of the **single equatorial ground station** (the router only ever terminated paths at `groundNets[0]`). The real lever is the bent path `region → sat → ground`: an equatorial GEO physically cannot reach a high latitude at ANY longitude (the disc edge sets below the 5° gate). **EMPIRICALLY MEASURED (probes against the real `solve`/`windowAvailability`, then deleted):** the parked equatorial GEO over lon 0 serves a region centre out to **~lat 64°** (the footprint edge); the great-circle from (lat 70°, lon 5°) to the equatorial ground is **~70°**, wider than a LEO's **~55.5°** footprint half-span — so a high-lat region is unreachable via the equatorial ground by ANY constellation. **The fix has three coupled parts:** (1) **REGION-1 re-placed to lat 70°, lon 5° E** (`NET_ACT2_REGION_LAT_RAD = 70°`, beyond the measured ~64° GEO edge — the GEO ALONE never serves it, at any longitude or time); (2) **LEO_SWEEP re-tuned to POLAR (inc 90°, subLon 5°)** so an inclined constellation reaches the high latitude; (3) **a co-located high-lat ground station `GROUND-1` at (70°, 5°)** added to the session's ground network so the constellation's downlink closes — and the **router now bridges via the strongest `(sat, ground)` pair across ALL grounds** (`bridgeForPoint`/`solve`/`isPointServed` iterate `groundNets`, tie-break satId-asc then groundId-asc; an equatorial region with one reachable ground gets a byte-identical bridge — golden-safe for the *shape*). The GEO cannot downlink to the 70° ground either (it is ~70° from the GEO nadir), so adding GROUND-1 does NOT let the GEO serve REGION-1. **The measured zero-gap N stays 4** at inc 90° (so the batch size, the phasing assist target, `ACT2_ZERO_GAP_N`, and the over-build math are ALL unchanged) — the phasing assist derives the same N=4 empirically against the new geometry. **The four pinned invariants (`net-replay.test.ts`, the SD-40 B3-FIX block):** (1) the parked equatorial GEO ALONE does NOT serve REGION-1 (binary served=false, at any t); (2) a single inclined LEO SAWTOOTHS (worst-phase rolling avail ≈ 0.22, max ≈ 0.44, never the 0.99 bar) and FAILS at the shared grace; (3) an N=4 polar constellation HOLDS REGION-1 continuously (worst-phase rolling avail ≥ slaAvail; instant served never drops across hand-off cycles); (4) a GEO-only replay (launch ONLY the GEO, accept both regions) clears act1 but the act2 gate NEVER fires (cursor stuck at 1) — the gate is satisfiable ONLY via the constellation. **The golden re-pin:** `NET_REPLAY_GOLDEN` 12864209889064023665n → **`260489051471786347n`.** What moved it: the REGION-1 placement (lat 70°), the LEO_SWEEP polar inclination + subLon, the second ground station, and the multi-ground bridge. The two existing goldens (`544847093270497462n` M1 cache, `8431658617016421069n` M2 build) are byte-for-byte UNTOUCHED (neither imports `net/`). `net/` stays pure (purity scan green); the router's `SolveResult` signature is unchanged (only the internal `bridgeForPoint` ground-loop + its return `groundId` changed). The B1/B2 pedagogy tests (`availability.test.ts`, `phasing.test.ts`) were re-pointed to the high-lat REGION-1 + both grounds and still pin N=2/3 fail, N=4 holds. The multi-replay golden tests carry a 30 s timeout (a second ground roughly doubles per-tick router work). 598 tests GREEN; tsc clean. _The B3 "GEO-DOMINANCE NOTE" finding is now SUPERSEDED: the GEO/LEO mutual-exclusivity was a single-ground artefact, not fundamental — a high-lat region + a high-lat ground is reachable by an inclined constellation and unreachable by the equatorial GEO, which is exactly the wall._

#### SD-40·C1a — Act-3a router layer: the §7.2 reactive cost-blend + the latency & bandwidth axes, built as the four ADDITIVE signature-stable extensions E1–E4 (golden DELIBERATELY untouched)
**Status: ACCEPTED (built; the cost-blend + both axes pinned in `net/cost-blend.test.ts`; the net golden is byte-for-byte UNCHANGED — by design).** C1a is the ROUTER slice of C1 (act3a escalation): the reactive blend + the latency-then-bandwidth SLA axes, implemented as the design's four ADDITIVE, signature-stable extensions so Act-1/Act-2 routing (and the net golden) are byte-identical when the new optionals are absent. **What was built:** (E1) `RoutableContract` widened with `prefer?: PreferWeights` + `slaLatencyS?: number` (absent ⇒ `NET_ROUTER_DEFAULT_PREFER = {lat:1, bw:0, stab:0}` + `Infinity` ⇒ no-op); (E2) `solve(...)` gains ONE trailing optional `loadBySat?: ReadonlyMap<string,number>` after the existing `faults?` (return UNCHANGED — `{served,path,latencyS,bindingConstraint,losses}`); (E3) `topologyKey(...)` folds a quantized `congestionEpoch` int (default 0 ⇒ legacy 3-field key + `|0`, an internal cache key NOT in the state-hash) and `resolveTick(...)` gains trailing `loadBySat?` + `congestionEpoch` and forwards both into its internal `solve` (the HIGH-2 fix: a rising load that bumps the epoch flips the fingerprint ⇒ a full re-solve through the cache; a static load preserves the cache); (E4) a `NET_LINK_CAPACITY_UNITS = 1.5` constant on the link-budget (per standard antenna; C2 layers a degradation haircut on top). **The min-cost pick (the §7.2 blend):** `bridgeForPoint` replaces the first/strongest-margin choice with a MIN-COST `(sat,ground)` pick under `cost = w_lat·latencyTerm + w_bw·congestionTerm + w_stab·0` (`latencyTerm = up.latencyS+down.latencyS`; `congestionTerm = sharedLoad/NET_LINK_CAPACITY_UNITS`; `w_stab` DORMANT — the term is literally absent from the expression, M1 LOCKED). **BYTE-IDENTITY guarantee (why the golden does NOT move):** the blend engages ONLY when a non-default `prefer` OR a non-empty `loadBySat` is supplied; otherwise the LEGACY max-margin comparison runs VERBATIM. With lat-only prefer + no load the cost is the bare latency term, a monotone function of distance, so the min-latency sat IS the max-margin (closest/highest-above-horizon) sat — the same bridge, same tie-break (satId-asc then groundId-asc). C1a adds NO new session FOLD state (no escalation law yet), so `NET_REPLAY_GOLDEN` stays **`260489051471786347n`** — the golden RE-PIN is deferred to the escalation-fold sub-increment (C1b: `escalationOn`/`congestionEpoch`/`chosenSatByContract`/the 3a gate state in the fold), per the design's documented chained re-pin. **The axes bite BINARY (the HIGH-1 fix), one at a time via `activeAxes`:** LATENCY — a path with `latencyS > slaLatencyS` and `"latency" ∈ activeAxes` ⇒ `served=false`, `bindingConstraint="latency"` (a ~3.57 ms GEO path fails a 3 ms SLA; a ~2.07 ms equatorial LEO passes — the GEO ceiling, felt); BANDWIDTH — the chosen bridge's `sharedLoad ≥ NET_LINK_CAPACITY_UNITS` with `"bandwidth" ∈ activeAxes` ⇒ `served=false`, `bindingConstraint="bandwidth"` (binary, so `breachSecondsAccum` actually accrues under the shared m2 state machine — a pro-rata fraction never would). The blend ALSO routes AROUND a congested sat onto a parallel path when `prefer.bw>0` (the §7.3 tune-by-exception). **Verified (`net/cost-blend.test.ts`, 16 tests):** default-prefer/no-load picks the min-latency=max-margin sat; bw=0 ⇒ load alone never reroutes; a `prefer.bw` contract avoids an over-capacity sat onto a parallel path; the latency axis bites the GEO path + passes the LEO + is a no-op when absent; the bandwidth axis bites at/over capacity + is a no-op when the axis or load is absent; `w_stab` contributes 0 regardless of `stab`; `solve` with no `loadBySat` == empty `loadBySat`; `topologyKey` epoch 0 == legacy key + `|0`, an epoch bump re-solves, a static load preserves the cache. The two existing goldens (`544847093270497462n` M1 cache, `8431658617016421069n` M2 build) are byte-for-byte UNTOUCHED; the net golden is UNCHANGED; `net/` stays pure (purity scans `router.ts`/`link-budget.ts`; `router.ts` gains only a TYPE import of `PreferWeights` from `contract.ts` — no module-eval cycle, `contract.ts` never imports `router.ts`); `net/` imports neither `m1/` nor `m2/session.ts`. 614 tests GREEN; tsc clean. _`NET_LINK_CAPACITY_UNITS` is a tuned placeholder on the `offeredLoad` scale (default contract load 1.0) — one contract sits comfortably under capacity, two sharing one sat after escalation tip over it; re-tune with the escalation law in C1b._

#### SD-40·C1b — Act-3a SESSION layer: the escalation law + oversubscription + the act3a scenario beat + the NET GOLDEN re-pin (the SD-40 chained re-pin)
**Status: ACCEPTED (built; the tame→outgrow→re-tame cycle + the gate pinned in `net/escalation.test.ts` + the `net-replay` act3a invariants; the net golden RE-PINNED; both old goldens byte-for-byte UNTOUCHED).** C1b is the SESSION slice of C1 (act3a escalation), closing the "your own success congests it" loop on top of the C1a router blend. **(1) THE ESCALATION LAW (`session.ts` + `contract.ts`):** `NetSession.stepEscalation(dt)` grows each well-served (`lastServedFraction ≥ ESCALATION_SERVE_THRESHOLD = 1.0`) ACTIVE contract's `offeredLoad` by the EXACT CLOSED-FORM LOGISTIC FLOW `escalateLoad()` toward `ESCALATION_LOAD_CEILING` — the same DT-invariant semigroup the M2 `coverage/dynamic-demand.ts` engine uses (mirrored, NOT imported — `net/` stays pure), with a single ceiling-clamp (no shock-compounding, the commit-2fc0500 fence). Present-from-day-one, gated ON only when the act3a beat emits (`enableEscalation()`, an int 0/1 flag). Folds via `offeredLoad` (already in the hash). **(2) OVERSUBSCRIPTION (the two-pass aggregation):** each step, when escalation is on, the session rebuilds `loadBySat` (Σ `offeredLoad` over each sat's chosen contracts) from the FOLDED prior-tick `chosenSatByContract` map (Pass A's one-tick lag, fully re-derivable across a restore — the MED desync fix), bumps a quantized `congestionEpoch` int when any sat's bucket (`floor(load/NET_CONGESTION_BUCKET_UNITS)` + an over-capacity flag) changes vs the FOLDED `prevCongestionFingerprint` string, and runs the serve/breach loop (Pass B) against the fresh map+epoch — recording each contract's freshly-chosen bridging sat (path[1]) back into `chosenSatByContract` for next step. The epoch feeds the C1a E3 `topologyKey` so a rising load forces a re-solve through the cache; a static load preserves it. **(3) `net_set_prefer` reused unchanged** (the §7.3 tune-by-exception action — already wired through `action.ts`/`apply-action.ts`/`NetSession.setPrefer`; act3a is its first consuming scenario). **(4) THE act3a SCENARIO BEAT (`scenario.ts`):** `emit` calls `enableEscalation()` + offers the REGION-2 equatorial CORRIDOR contract (lat 0, lon 3°E — sharing REGION-0's corridor) with `activeAxes={connectivity,latency}` + a low `slaLatencyS=3 ms` so the parked GEO (~3.57 ms) can't meet it (the GEO ceiling, felt) but a short equatorial LEO (~2.07 ms) does (LATENCY = the AUTHORED arrival). The BANDWIDTH axis arrives ONE AT A TIME via the escalation law: once a served contract's grown load crosses `ESCALATION_BANDWIDTH_AXIS_THRESHOLD = 1.2`, `stepEscalation` flips `"bandwidth"` into its `activeAxes` (the §4.4 escalation-triggered mask add, deterministic in step — NO struct reshape). `gate = escalationReTamed()`: a previously-served contract crossed `NET_NEAR_BREACH_GRACE_FRACTION (0.1) × BREACH_GRACE_SECONDS` of breach WHILE escalation was on (witnessed — reachable because the C1a bandwidth bite is BINARY so `breachSecondsAccum` actually accrues, the HIGH-1 fix), then returned to fully SERVED (re-tamed). State-gated, latched, folded as `act3aReTameWitnessed` (0/1) + the `nearBreachWitnessed` id set. This FENCES act3b structurally (its emit fires only once the cursor advances past act3a). `fallback` surfaces the §4.3 sharing readout. **Tuning (placeholders on the `offeredLoad` scale):** ceiling 1.4 < capacity 1.5 (a single contract alone NEVER self-congests; two sharing sum to ~2.8, well over capacity — relief is SPLITTING the shared sat, not adding headroom on one); rate `2.0e-2`/s (load crosses 1.2 in ~45 s of served time for the toy ~4-min day). **(5) THE GOLDEN RE-PIN (`net-replay.test.ts`, the SD-40 chained re-pin pattern):** `NET_REPLAY_GOLDEN` **`260489051471786347n` → `314363620940498869n`.** The golden log was EXTENDED through act3a (a short equatorial-LEO launch + ACCEPT REGION-2; then a PARALLEL equatorial LEO + `net_set_prefer(REGION-0, bw)` relief that makes the latency-tolerant trunk yield the short path, splitting the shared sat) and run to a longer horizon (470 s / 28 200 ticks); the fold gained the C1b ADDITIONS — `escalationOn` (int), `congestionEpoch` (int), `act3aReTameWitnessed` (int), the `chosenSatByContract` `id|satId` pairs, the `nearBreachWitnessed` ids, the `congestionFingerprint` string — folded in FIXED order after the fault-cursor placeholder, PLUS the value moves already in the fold (REGION-2's grown `offeredLoad` + `activeAxes` latency/bandwidth, REGION-0/REGION-1's escalated load + bandwidth-axis flips, REGION-0's `prefer` change). The act1/act2 invariant sub-tests keep their 360-s `act2Log` horizon (escalation turns on only in the last ~37 s; REGION-1 stays served alone on its polar LEO < capacity, so those invariants hold). **Verified:** `escalation.test.ts` (9 tests) — the growth law is closed-form/DT-invariant/bounded/no-op below threshold; the FULL tame→outgrow→re-tame cycle fires the gate deterministically; the gate does NOT fire before the dip+re-tame (state-gated); load freezes while breaching; escalation dormant when off; router back-compat. `net-replay.test.ts` (+3 act3a invariants) — escalation grew + flipped the bandwidth axis one-at-a-time + the corridor re-tamed; the gate is state-gated (cursor stuck at act3a before the relief, with a REAL accrued breach); and **restore-then-step == continuous-run for the congestion fold** (`loadBySat` re-derived from the folded chosen-sat map — the MED desync fix). The two existing goldens (`544847093270497462n` M1 cache, `8431658617016421069n` M2 build) are byte-for-byte UNTOUCHED (neither imports `net/`); `net/` stays pure (purity scan green — `session.ts` gains only value imports of `NET_LINK_CAPACITY_UNITS` from `link-budget.ts` + `BREACH_GRACE_SECONDS` from the already-imported `m2/contracts.ts`; no three/DOM/wall-clock/unseeded-RNG); `net/` imports neither `m1/` nor `m2/session.ts`. 626 tests GREEN (was 614); tsc clean. **GENERALIZES for C2 (3b faults + the trace view):** the two-pass `loadBySat`/congestion-epoch path is exactly the seam the C2 degradation capacity-haircut feeds; the per-contract chosen-sat map + the binding-constraint readouts (`escalationEnabled`/`congestion`/`loadOnSat`/`lastSolveFor`) are the trace's first inputs; `w_stab` stays DORMANT (reactive-only, M1 LOCKED). _The near-breach witness can latch via ANY previously-served contract that dipped+recovered (REGION-0 or REGION-2) — faithful to the design's "a previously-served contract" gate; the relief deliberately makes the latency-tolerant trunk yield so the latency-critical corridor re-tames._

#### SD-40·C2 — Act-3b FAULTS (the chaos kitten, mild-first) + the self-diagnosing TRACE view + the NET GOLDEN re-pin (the SD-40 chained re-pin, continued)
**Status: ACCEPTED (built; the mild-first fault arc + the act3b gate pinned in `net-replay.test.ts` SD-40 C2 invariants; the net golden RE-PINNED; both old goldens byte-for-byte UNTOUCHED; `net/` stays pure).** C2 closes the Act-3 "AND FAULTS DEGRADE IT" beat on top of C1 (act3a escalation): a working network does not stay working, FENCED behind re-stabilisation. Built around the parallel-shipped LOCKED seams `fault.ts` (the seeded fault SPECTRUM + `rollFaults`) + `trace.ts` (the self-diagnosing VIEW) + `fault-types.ts` (the shared types); this slice is the INTEGRATE step (the only one touching `session.ts`/`scenario.ts`/the golden/`purity.test.ts`). **(1) THE FAULT ROLL IN `session.step` (`rollAndApplyFaults`):** each tick, when the fault generator is gated ON, the session calls `fault.rollFaults` off the SAME seeded splitmix64 `SimRng` it already owns (the M2 launch-failure-roll pattern — **NO new action, NO new seed**) with the active faults + the live roster + the scripted queue; it CLEARS resolved faults + ADDS started ones into an `activeFaults` Map. A faulted sat is **removed from the router graph** via the existing `faults?: ReadonlySet<string>` param (a transient outage, or a telegraphed countdown that EXPIRED — before expiry it still routes, the watch-and-act window); a **degradation** is NOT removed — it is a capacity HAIRCUT modelled by scaling its effective `loadBySat` up by `1/degradedCapacityFactor` so it congests/trips the bandwidth bite sooner (biting whoever cut oversubscription thin in 3a). FIXED ordering: roll (1c) runs AFTER the congestion aggregation (1b) and BEFORE the serve loop (2), so the down-set + the haircut feed the same-tick router; the witnesses + the trace run AFTER serve (2d). **(2) MILD-FIRST IN TIME:** the act3b emit seeds a scripted pair — a **Degradation** (self-recovers, unwarned), then a **Telegraphed failure** (warning + 45 s countdown). The session feeds the roll only the queue HEAD and advances it ONLY once the prior scripted fault RESOLVES (`lastScriptedFaultSatId` gate), so the pair is sequenced over time (the Telegraphed countdown begins only after the Degradation self-recovers ~30 s later) — not fired together. **(3) THE TRACE wired (`diagnose`):** each step once faults are on, `trace.diagnose` reads the per-contract last `SolveResult`s + the roster + the active faults + the shared load (pure read-over-snapshot, re-derives nothing) and returns the `TraceReport` (the binding-constraint + kind-of-fix shortfalls, the SPOF / over-provision resilience shortfalls, the active-fault SYSTEM.LOG, the predictability-seed loss roll); the session latches `surfacedShortfall` once it surfaces ≥1 shortfall and keeps the report as a DERIVED readout (NOT folded). **(4) THE act3b SCENARIO BEAT (`scenario.ts`):** `emit` ASSERTS `escalationReTamed()` (the structural fence — belt-and-braces; the beat is current only after the act3a gate) then calls `enableFaults(ACT3B_FAULT_SCRIPTS)` (flip a flag + seed the mild-first pair — never touches physics, the §3 emit contract; the seeded roll drives the faults). `gate = weatheredFault() && traceSurfacedShortfall()`: the player kept a contract served through a fault's WHOLE lifetime (start → self-recover/drop — so the gate needs the fault SURVIVED, not merely to appear; mild-first means the Degradation must self-recover first) AND the trace did its job. Opens act4. `fallback` surfaces the brittle-builder resilience shortfall. **(5) PURITY:** `fault-types.ts`/`fault.ts`/`trace.ts` added to the `net/purity.test.ts` scan (all green — faults draw ONLY from the seeded `SimRng`; the trace is a pure read; no three/DOM/wall-clock/unseeded-random). `session.ts` gains only value imports of `rollFaults` (`fault.ts`) + `diagnose` (`trace.ts`) + `faultRemovesSatAt` (`fault-types.ts`). **(6) THE GOLDEN RE-PIN (`net-replay.test.ts`, the SD-40 chained re-pin pattern):** `NET_REPLAY_GOLDEN` **`314363620940498869n` → `11632456535472871375n`.** The action log is IDENTICAL to the act3a arc (the faults are scenario-seeded off the stream, NOT player actions); only the HORIZON extends **470 s → 520 s** so both scripted faults play out (the Degradation fires ~t=430 + self-recovers ~t=460 ⇒ the 3b gate fires; the Telegraphed begins ~t=460 + drops ~t=505). The fold gained the C2 ADDITIONS — `faultsOn` (int), the active `FaultState[]` (satId|kind|cause|the three §2.4 predictability-seed sim-times + the degraded-capacity factor, sorted by satId), the pending mild-first script queue, the mild-first gate stamp `lastScriptedFaultSatId`, the `servedThroughFault` set, and the `faultWeathered` + `surfacedShortfall` witnesses (ints) — **REPLACING the old fault-cursor 0 placeholder** in the FIXED fold order. **Verified:** `net-replay.test.ts` (+5 C2 invariants) — NO fault before the act3a gate (the fence: faults disabled + none active until re-tamed); MILD-FIRST (a Degradation precedes a Telegraphed failure, sequenced in time, off the seeded stream, lowOrbit-caused); the 3b gate fires (weathered + trace-surfaced ⇒ cursor → act4, REGION-0/REGION-1 never failed — the redundant builder sails through); the gate does NOT fire mid-Degradation (state-gated); and faults are replay-deterministic (the fault fold replays bit-identically) — PLUS the RESTORE-REPLAY test extended to snapshot WHILE a fault is active so the fault fold (active faults + script queue + rng) carries restore-then-step == continuous-run. The two existing goldens (`544847093270497462n` M1 cache, `8431658617016421069n` M2 build) are byte-for-byte UNTOUCHED (neither imports `net/`); `net/` imports neither `m1/` nor `m2/session.ts`. 673 tests GREEN (was 626); tsc clean; build clean. **VIZ (render-only, no fold touched):** a faulting sat PULSES AMBER on the orrery (driven off the sim clock, deterministic), and `main.ts` drains the live fault SYSTEM.LOG lines (the degradation/telegraphed renderers) + the first trace shortfall edge-triggered into the log. **GENERALIZES for M2:** the stochastic spectrum (`transient`/`hard`) + the causal levers (`overclock`/`cheapBus`) are present-but-dormant — M2 turns them up without a reshape; the trace's predictability-seed loss roll is the seam the M2 forecast surfaces.

#### SD-40·D1 — Act-4 the MARS FRONTIER TEASER ("distance changes everything" — vertigo, FENCED) + the NET GOLDEN re-pin (the SD-40 chained re-pin, continued)
**Status: ACCEPTED (built; the act4 Mars teaser pinned in `net-replay.test.ts` SD-40 D1 invariants; the net golden RE-PINNED; both old goldens byte-for-byte UNTOUCHED; `net/` stays pure).** D1 is a TEASER, NOT a system: it introduces light-delay + freshness + caching as CONCEPTS FELT BY SIGHT on top of a mature acts-1-3 Earth network, then STOPS. The player feels they have got this; act4 is the REVERSAL. **(1) THE ONE NEW MECHANIC — `solveMarsLeg` (`router.ts`):** a special-cased branch keyed on `contract.region.bodyId === "mars"` (Earth contracts keep the toy-frame `bridgeForPoint` VERBATIM — `surfacePointRelative`/`evaluateLink`/the earth-relative cancellation are untouched). The Mars leg is NOT a toy-frame close (no toy 300 km geometry at ~1 AU, no fake EIRP in the fold): connectivity is PRESENCE-based — the leg bridges by construction once the player has launched the deep-space relay (a sat whose id begins `NET_ACT4_RELAY_ID_STEM`, not faulted), else `bindingConstraint="connectivity"` (like Act-1 "no path"); latency is the REAL Earth↔Mars one-way light delay `interBodyOneWayLatencyS(eph,"earth","mars",t)` (= `oneWaySeconds(eph.distanceBetween)`, ~15.4 min one-way at the J2000 epoch, ~1.85 AU) injected into the UNCHANGED `latencyS` field. Latency is a READOUT, never an enforced axis — the Mars contract's `activeAxes` is `{connectivity}` only, so the minutes-long delay never breaches (vertigo, not a system). `isRegionServed` (the cheap re-eval) is also presence-based for Mars so the cached path holds (a parked relay's presence is time-invariant — no spurious horizon flip). **(2) REUSE, NOT REBUILD (the SD-40 import fence preserved):** `link-budget.ts` imports `oneWaySeconds` from `../delay` for the ONE `interBodyOneWayLatencyS` helper; `session.ts` imports `freshness` from `../delay` for the readout. A tiny LOCAL 3-field `MarsSample` (`{datasetId:"mars"; capturedAtT; halfLifeS}`) mirrors the `m1/cache.ts` `CachedSample` SHAPE + its SD-19 honest-staleness CONVENTION (capturedAtT-at-arrival, half-life = the one-way light delay) WITHOUT importing the m1 economy — `net/` still imports neither `m1/` nor `m2/session.ts` (SD-40 invariant). On the Mars path's FIRST carry the sample freezes one-way OLD on arrival (`capturedAtT = t − oneWay`, `halfLifeS = oneWay`); `marsAgeS(t)` = "as of Nm ago", `marsFreshness(t)` = `freshness(age, half)` = the desaturation-by-sight curve (DD-1). **(3) THE ESCALATION FENCE (§8):** `stepEscalation` skips any non-Earth contract, so the act3a escalation/oversubscription economy NEVER grows the Mars load or flips its bandwidth axis — the Mars teaser stays connectivity-only by sight. **(4) ONE CACHE BREADCRUMB:** `KIND_NET_PLACE_CACHE` + `netPlaceCache()` (payload `{}`) + the `apply-action.ts` branch calling `session.placeMarsCache(eph,t)` — deterministic, NO roll: it re-captures the sample at `t` (age ~0 ⇒ the freshness readout jumps UP by sight, "data closer helps"). It does NOT change served/breach/revenue (a felt breadcrumb, NOT a relief lever). A SINGLE placeable — NOT the cache economy (NO prefetch policy, NO coherence levels, NO eviction, NO freshness pricing curve, NO parse/currency/sat-sat coherence — all §8 fenced and undesigned). **(5) "PAYS LESS FOR STALE" = RENDER-LAYER:** NO `freshnessFactor` Contract field, NO freshness→€ wiring (the wallet stays the plain `netRevenueRatePerSecond`); the stale-pay dimming is a render-layer read off `marsSample` — the only fold growth is `marsSample` (a null-flag + 2 f64s). **(6) THE act4 BEAT (`scenario.ts`):** `emit` offers the ONE Mars contract (`NET_ACT4_MARS_REGION`, bodyId "mars", connectivity-only) — pure demand arrival, no escalation/fault enable/mask flip. `gate` STAYS FALSE FOREVER — a READ, not a gate; the cursor STOPS on act4 (the deliberate "to be continued", no win screen, no completion gate; the human two-layer lean-in/bounce read is OUTSIDE the sim). **(7) THE `MARS_RELAY` PRESET (`world.ts`):** the "launch toward Mars" verb is the SAME `net_launch` with a different preset (the applier names a `presetId==="MARS_RELAY"` launch with the relay id stem so the presence test recognises it). The relay's orbit + eirp are COSMETIC (presence bridges, not a toy budget); `previewLaunch` already runs `solve`, so the preview shows the minutes-long crawl truthfully BEFORE commit (itself part of the vertigo). **(8) THE GOLDEN RE-PIN (`net-replay.test.ts`, the SD-40 chained re-pin pattern):** `NET_REPLAY_GOLDEN` **`11632456535472871375n` → `2578549558858135194n`.** The golden replay now builds the REAL ephemeris (`loadEphemeris()`) — the toy near-Earth arc is BYTE-IDENTICAL to the empty `Ephemeris.build({})` the pre-D1 golden used (the router never reads `eph` for Earth-relative sat/surface geometry; only the Mars leg needs the real distance — verified), so the Earth/fault fold is UNCHANGED; the toy near-Earth scale and the REAL interplanetary distance COEXIST. The golden log gained the ACT-4 player inputs — LAUNCH the `MARS_RELAY` + ACCEPT the Mars contract + PLACE the cache breadcrumb — and the HORIZON extends **520 s → 560 s** so the Mars path carries + the sample freezes + the breadcrumb re-captures. The fold gained the ONE D1 ADDITION — `marsSample` (a null-flag + `capturedAtT` + `halfLifeS`) — dormant 0 for the pre-act4 horizons (byte-identical). **Verified:** `net-replay.test.ts` (+7 D1 invariants) — the Mars latency EXPLODES deterministically (one-way 3..23 min, round-trip exactly 2× in 6..45 min, equal to `oneWaySeconds(distanceBetween)`) while the Earth toy latency stays sub-second; the leg is PRESENCE-based (no relay ⇒ connectivity loss; relay ⇒ bridges, path `[MARS-1, MARS-RELAY-N, GROUND-0]`); NO Earth gauge shows freshness (no Contract `fresh*`/`stale*` field, `SLA_AXIS_ORDINAL` has no freshness member); crawl == readout (same formula + same ephemeris distance, no drift); latency stays UN-ENFORCED (`activeAxes={connectivity}`, never failed, `breachSecondsAccum=0`, plain pay accrual); ONE cache breadcrumb RAISES the displayed freshness deterministically with no served/breach/revenue change; and the cursor STOPS on act4 (cursor===4, exactly 4 gate ticks, no gate fires past it) — PLUS the restore-replay test extended through act4 so the `marsSample` fold carries restore-then-step == continuous-run, and live==replay + replay-twice + the SaveGame JSON round-trip all reproduce the new golden bit-identically. The two existing goldens (`544847093270497462n` M1 cache, `8431658617016421069n` M2 build) are byte-for-byte UNTOUCHED (neither imports `net/`; the M2-build golden was NOT conflated — D1 re-pins ONLY the NET golden); `net/` stays pure (purity scan green — `link-budget.ts`/`session.ts` gain only the pure `delay.ts` value imports; no three/DOM/wall-clock/unseeded-RNG). 680 tests GREEN (was 673); tsc clean; build clean. **MARS ONE-WAY LATENCY: ~15.4 min (round-trip ~30.8 min) — deterministic minutes at the J2000 epoch.** **GENERALIZES (the toolkit withheld, the vertigo introduced):** the freshness economy (prefetch/coherence/eviction/pricing slope, the §8/SD-15 ramp) stays FENCED and undesigned — a future increment that makes stale data actually reduce € is post-gate M2+; the `SolveResult` shape never changed across Acts 1→4 (Act 4 extends the `latency_term` only, via the `solveMarsLeg` injection).

#### SD-40·P0b — CHARGE FOR LAUNCHES (§3.5) + the FLAT per-launch FAILURE RISK roll + the NET GOLDEN re-pin (M1-remediation P0, continued)
**Status: ACCEPTED (built; the launch economy + failure roll pinned in `net-replay.test.ts` P0b invariants; the net golden RE-PINNED; both old goldens byte-for-byte UNTOUCHED; `net/` stays pure).** P0b closes the §3.5 launch-economy gap: the planner already SHOWED the launch cost (`launchDraftCost`) and a failure chance was the spec minimum, but neither was wired — `session.launchSat` added the sat for free and never rolled, and `applyNetAction` returned `costEur: 0`. P0b makes both real, reusing the M2 launch-roll pattern verbatim (NO new seed, NO new action). **(1) THE WALLET DEBIT (`session.ts`):** `launchSat(sat, costEur)` now `this.walletBalance -= costEur` BEFORE the roll — charged win OR lose (the m2 "you pay the launch provider" convention); overspending is allowed (the wallet can dip negative — the build-vs-budget tension), so there is NO affordability gate to block a launch. `applyNetAction` resolves the per-member cost from the wire's `presetId` (`launchCostBaseForPreset` → the preset's `costBaseEur`, falling back to `NET_DEFAULT_LAUNCH_COST_BASE = GEO_PARK.costBaseEur` for a hand-dragged CUSTOM orbit or a test corridor preset) + the altitude term via the existing `launchCost`, charges count× that (every batch member, win or lose), and returns the REAL `costEur` (was 0). **(2) THE FAILURE ROLL (`world.ts` `rollNetLaunch` + `NET_LAUNCH_FAILURE_CHANCE = 0.05`):** each launched member draws ONE double off the session's SEEDED splitmix64 RNG (`ok = roll >= NET_LAUNCH_FAILURE_CHANCE`) — the M2 `rollLaunch` shape. A FAILED member loses the sat (no roster add) AND does NOT consume the monotonic id (`launchedCount` advances only on success, so a failed launch leaves `NET-SAT-N` stable for the next try). The chance is TUNABLE (the spec floor ~5%), pinned here so the canonical run is deterministic. The applier returns `kind: "sats_launched"` if ≥1 member reached orbit (with `failedCount` for partial-loss batches) else `kind: "launch_failed"`; `main.ts` surfaces both in SYSTEM.LOG (a `warn` "LAUNCH FAILED — the vehicle was lost; €N charged" / "K lost to a launch failure"). **(3) THE SEED DISCIPLINE (replay-safety):** adding a per-member launch draw SHIFTS the shared rng stream the Act-3b faults later draw from. The canonical golden log's launches were VERIFIED to all CLEAR the 5% roll at `NET_RNG_SEED` (the GEO, the N=4 LEO batch, the two act3a corridor LEOs, and the Mars relay all reach orbit — pinned by a new invariant asserting the full 8-sat roster + the cursor reaching act4), so the acts still gate deterministically (act1..act3b fire; the cursor STOPS on act4) and every prior behavioural invariant holds unchanged. `NET_OPENING_BALANCE` was raised **5000 → 20000** for headroom against the ≈€11.6k of scripted launch capex (also moves the folded balance). **(4) THE GOLDEN RE-PIN (`net-replay.test.ts`):** `NET_REPLAY_GOLDEN` **`2578549558858135194n` → `10597504085086350891n`.** Two things moved the fold: (a) the folded `balance` now drops by the launch capex (off the raised opening balance); (b) each launch member advances the folded rng state by one draw, shifting the downstream fault stream's draws (still deterministic + replay-stable). **Verified:** the new P0b invariants — a successful launch DEBITS the wallet by exactly the previewed `launchCost`; a batch debits count× the per-member cost (charged win or lose); a launch CAN FAIL deterministically off a chosen seed (a session whose first roll is < 0.05 loses the sat, is still charged, and does NOT consume the id — reproducible); and every canonical-log launch clears the roll so the full arc still gates. The re-pinned golden replays bit-identically (replay-twice, live==replay, the SaveGame JSON round-trip, and the restore-then-step == continuous-run test all reproduce `10597504085086350891n`). The two existing goldens (`544847093270497462n` M1 cache, `8431658617016421069n` M2 build) are byte-for-byte UNTOUCHED (different worlds; neither imports `net/`). `net/` stays pure (purity scan green — `world.ts` gains only a TYPE-only `SimRng` import + the seeded roll; no three/DOM/wall-clock/unseeded-RNG). 684 tests GREEN; tsc clean; vite build clean; the DEV-server smoke (`tools/smoke.mjs` on `npm run dev`, default net mode + `?netview=mars`) booted with no console/page errors.

#### SD-40·P2 — THE TELEGRAPHED FAULT GETS TEETH (§5.1 session-ordering fix) + the transient fault + the live countdown + the toy-scaled low-orbit lever + the NET GOLDEN re-pin (M1-remediation P2)
**Status: ACCEPTED (built; the §5 fault-behaviour fixes pinned in `net-replay.test.ts` + `fault.test.ts`; the net golden RE-PINNED; both old goldens byte-for-byte UNTOUCHED; `net/` stays pure).** P2 closes the audit's §5 finding that the SIGNATURE M1 telegraphed event was *functionally dead*: a session-ordering bug deleted the fault before the router ever saw it, then spuriously reported "RECOVERED." Four surgical fixes, all against already-faithful sim code. **(1) THE SESSION-ORDERING FIX (the core — `fault.ts`/`fault-types.ts`/`session.ts`):** `rollFaults`'s RESOLVE pass used `faultResolvedAt` (`t ≥ recoversAtS || t ≥ failsAtS`), so a TELEGRAPHED fault reaching `failsAtS` went into `resolved` → `session.rollAndApplyFaults` DELETED it from `activeFaults` → `downSatIds` (called AFTER the roll) never saw it → the sat was never removed from the router graph, AND the deletion spuriously latched `faultWeathered`/printed "RECOVERED." Now the roll's RESOLVE pass uses the NARROWER new `faultSelfRecoveredAt` (`t ≥ recoversAtS` only) — so ONLY a degradation/transient that self-heals is freed; a telegraphed-expired fault is NOT in `resolved` (the session never deletes it / never credits "weathered"). It stays in `activeFaults` as a PERMANENT drop, where `faultRemovesSatAt(f,t)` (`t ≥ failsAtS`) removes its sat from the router graph from `failsAtS` on — a *warned hard failure*. So a telegraphed failure the player did NOT replace is a real, permanent loss (the §5 "you act before it dies" stakes are finally real). **(2) THE TRANSIENT FAULT (`scenario.ts`):** `ACT3B_FAULT_SCRIPTS` is now a mild-first TRIO `[degradation, transient, telegraphed]` (was a pair). The `transient` kind (a brief 15 s full outage that self-heals — the spec §5 row-2 fault, previously a type-only kind that NEVER fired) now fires in the canonical run (~t=460→475 on the low-orbit LEO), delivering the "you need a backup PATH; the self-healing reroute proves itself" lesson; the router reactively re-routes around the dropped sat then it returns. The trio is sequenced IN TIME (each fires only after the prior leaves the active map — the existing `scriptedHeadReady` mild-first gate). **(3) THE LIVE TICKING COUNTDOWN (`main.ts`, render-only):** `drainNetFaultLog` now re-surfaces "SAT-x telegraphed fault: fails in Ns" every 5 sim-seconds as the countdown TICKS DOWN (keyed on the floored bucket, no per-frame spam) instead of a one-shot line at appearance, and announces the PERMANENT DROP once on the expiry edge ("FAILED — dropped permanently … redundant paths bridge around it; a single-sat region breaches"). The orrery node already pulses amber for any faulting sat (`netBuildRenderState`'s `faulting` flag). `telegraphedCountdownRemainingS` + `faultRemovesSatAt` are imported into `main.ts` for the live read. Render-only — no golden impact. **(4) THE TOY-SCALED LOW-ORBIT LEVER (`fault-types.ts`):** `LOW_ORBIT_REF_M` was a real-Earth GEO radius (~42,160 km) while the toy world orbits at ~610-835 km against a 300 km body, so the toy GEO and toy LEO BOTH saturated near `LOW_ORBIT_MAX_MULTIPLIER` (7.86× vs 7.90×, a ~0.5 % gap) and "a LEO faults more than a GEO" did NOT register. `LOW_ORBIT_REF_M` is now the TOY GEO semi-major axis (`A1_GEO_SEMI_MAJOR_M`, imported from `world.ts` — which does NOT import `fault-types`, so no cycle), so the toy GEO sits AT the neutral multiplier 1.0 (the bare base rate) and the toy LEO sits MEANINGFULLY below it on the ramp: **the toy LEO now faults 2.88× the toy GEO** (a clear causal gap, TUNABLE by moving the reference). At `NET_RNG_SEED` the stochastic floor never fired in the canonical run, so this re-scale moves NO behaviour in the canonical log — only the causal lever's legibility. **(5) THE CANONICAL-RUN WEATHERING (the §5 teaching, deterministic):** the telegraphed fault hits a polar REGION-1 LEO (`NET-SAT-4`) and drops it PERMANENTLY ~t=520. REGION-1's REDUNDANT N=4 polar constellation BRIDGES around the lost sat (it dips intermittently to N=3 then recovers each orbit; `breachSecondsAccum` peaks ~20-30 s, far below the 600 s fail grace) — so REGION-1 (and the resilient equatorial REGION-0/REGION-2) NEVER fail. A brittle single-sat region WOULD breach; the redundant builder weathers it. The act3b gate still fires (weathered via the RECOVERABLE degradation/transient — NOT via the permanent drop — + the trace shortfall), and the cursor reaches + stops on act4. **(6) THE GOLDEN RE-PIN (`net-replay.test.ts`):** `NET_REPLAY_GOLDEN` **`10597504085086350891n` → `16182974603317469058n`.** What moved the fold: the telegraphed-expired fault now PERSISTS in the active-fault fold past `failsAtS` (before it spuriously vanished) and the dropped sat changes the routing solve from the drop on; the transient adds its own fold state + advances the scripted-queue cadence + the rng stream. The horizon is UNCHANGED (560 s) — the drop (~t=520) lands inside it. **Verified:** updated `fault.test.ts` (a telegraphed expiry is a PERMANENT drop, NOT in `resolved`, removes the sat forever; the toy LEO faults ≥1.3× — measured 2.88× — the toy GEO with the GEO at the neutral base) + new/updated `net-replay.test.ts` C2 invariants (the mild-first TRIO degradation→transient→telegraphed fires in time order; the telegraphed fault DROPS its sat permanently — it stays faulted, the dropped id is in NO contract's path, never "recovered"; the 3b gate fires AND the redundant N=4 constellation rides out the permanent loss without failing). The re-pinned golden replays bit-identically (replay-twice, live==replay, the SaveGame JSON round-trip, and restore-then-step == continuous-run all reproduce `16182974603317469058n`). The two existing goldens (`544847093270497462n` M1 cache, `8431658617016421069n` M2 build) are byte-for-byte UNTOUCHED (different worlds; neither imports `net/`). `net/` stays pure (purity scan green — `fault-types.ts` gains only the pure `A1_GEO_SEMI_MAJOR_M` value import; no three/DOM/wall-clock/unseeded-RNG). 690 tests GREEN (was 684); tsc clean; vite build clean; the DEV-server smoke (`tools/smoke.mjs` on `npm run dev`, default net mode + `?netview=mars`) booted with no console/page errors.

#### SD-40·P3 — PER-CLASS ROUTING MADE LIVE (§7.2) + the PER-CONTRACT PREFER SLIDER (§7.3/§10) + the NET GOLDEN re-pin (M1-remediation P3)
**Status: ACCEPTED (built; the traffic classes + the slider pinned in `net/traffic-class.test.ts` + `net-replay.test.ts`; the net golden RE-PINNED; both old goldens byte-for-byte UNTOUCHED; `net/` stays pure; `w_stab` stays dormant).** P3 closes the audit's §7.2/§7.3 finding that the per-class routing was INERT (every contract carried `{lat:1,bw:0,stab:0}`, so the cost-blend's `w_bw`/`w_stab` never bit and "demand-shape → topology-shape" never fired) and that `net_set_prefer` — the §10 "first thing the player tunes" — existed ONLY in tests, with NO UI control. **(1) THE TRAFFIC CLASS (`contract.ts`):** a new `trafficClass: "latency"|"bandwidth"|"availability"` field on `Contract` SETS its default `prefer` via the new `PREFER_FOR_CLASS` table — latency = lat-only `{1,0,0}` (route the short way, = the byte-identical pre-P3 default), bandwidth = `{1, NET_BANDWIDTH_CLASS_W_BW, 0}` (route AROUND congestion onto a parallel less-loaded path), availability = `{0.2, 0, 1}` (lean OFF latency; `w_stab` SET but DORMANT — M1 LOCKED, the instability term contributes 0). `offerNetContract` applies the class default (an explicit `prefer` opt still wins, the §7.3 hand override). **The toy-frame scale:** `NET_BANDWIDTH_CLASS_W_BW = 4.0e-4` is small relative to `w_lat=1` ONLY because the toy latency_term is sub-millisecond (a short LEO ≈ 2.1 ms vs the GEO ≈ 3.6 ms, a ~1.5 ms span) while the congestion_term is a dimensionless 0..~1.7 — the weight is tuned so `w_bw·congestion` outweighs the ~0 latency gap between two SHORT parallel LEOs (so a congested trunk leaves a loaded LEO for a free one — the §4.3 split) yet stays UNDER the ~1.5 ms gap to the GEO (so the latency-critical corridor NEVER routes onto the latency-FAILING GEO). **(2) THE CLASS ASSIGNMENTS (`scenario.ts`):** REGION-0 = LATENCY-class, REGION-1 = AVAILABILITY-class, REGION-2 (the act3a corridor) = BANDWIDTH-class. So REGION-0 (latency, clings to the short hop) and REGION-2 (bandwidth, abandons a congested shared sat for the parallel equatorial LEO) now route DIFFERENTLY over the SAME two equatorial sats — the §7.2 thesis, previously inert, now LIVE (proven in `net/traffic-class.test.ts`: a latency contract picks the min-latency congested SAT-A, a bandwidth contract picks the parallel SAT-B, over the same sats with the same load). **(3) THE PER-CONTRACT PREFER SLIDER (§7.3/§10 — `panels/net-planner.ts` + `main.ts`):** a new "ROUTING · PREFER" group surfaces `net_set_prefer` as a single latency↔bandwidth↔stability range slider (stable selector `[data-net="prefer"]`) + a SELECT-CONTRACT cycle button (`[data-net="prefer-select"]`) + the selected contract's CLASS + current weights. The pure `preferFromSliderPos(pos)` maps 0→latency / 0.5→bandwidth / 1→stability (LERP between the per-class weights — ONE design surface); dragging it appends a `net_set_prefer` action via the SHARED applier (live == replay) → the router re-solves THAT contract → its path visibly RE-ROUTES on the globe (via the P1 link line, whose bridging sat `path[1]` migrates). The control is hidden until ≥1 active contract exists. **(4) THE RELIEF RE-TUNE (`net-replay.test.ts` + `net/escalation.test.ts`):** under the bandwidth-class weights REGION-2's near-breach dip is cleaner, so the canonical act3a relief tick moved **25800 → 27000** (≈ t=430 → t=450) — landing AFTER the breach window crosses the 60 s near-breach threshold so the re-tame is genuinely witnessed; REGION-2 then re-routes onto the parallel equatorial LEO (NET-SAT-6, under capacity) by its OWN class weights (the manual `net_set_prefer(REGION-0)` still vacates the corridor). Every downstream beat shifts (act3a gate t≈450, act3b faults emit t≈451, act3b gate t≈480, act4 reached) and STILL gates deterministically (cursor reaches + STOPS on act4; re-tame/weather/surface all latch). The horizon is UNCHANGED (560 s). **(5) THE GOLDEN RE-PIN (`net-replay.test.ts`):** `NET_REPLAY_GOLDEN` **`16182974603317469058n` → `8969122486022400018n`.** What moved the fold: the per-class `prefer` weights change which bridge several contracts pick (the cost-blend branch vs the legacy max-margin) — the routing solves + the re-tuned relief timing move the roster/contract fold. The `Contract` struct gained a `trafficClass` field (carried by `cloneNetContract` + the snapshot) but the state-hash does NOT fold it (derived metadata) — the value moves only through the changed ROUTING. **Verified:** new `net/traffic-class.test.ts` (the class sets the default prefer; the latency vs bandwidth contract route differently over the same sats; `w_stab` is dormant — varying it never changes the pick) + the updated C1b escalation invariants (the re-tamed corridor rides the parallel LEO under capacity at the cleaned-up horizon) + the re-pinned golden replays bit-identically (replay-twice, live==replay, the SaveGame JSON round-trip, and restore-then-step == continuous-run all reproduce `8969122486022400018n`). The two existing goldens (`544847093270497462n` M1 cache, `8431658617016421069n` M2 build) are byte-for-byte UNTOUCHED (different worlds; neither imports `net/`). `net/` stays pure (purity scan green — `contract.ts` gains only data + pure functions; no three/DOM/wall-clock/unseeded-RNG; the slider DOM is render-layer `panels/net-planner.ts`). tsc clean; vite build clean; the DEV-server smoke booted with no console/page errors. **The per-class weights + `NET_BANDWIDTH_CLASS_W_BW` are placeholders on the toy latency scale — tune later.**

#### SD-40·P4 — OVERSUBSCRIPTION IS THE DETERMINISTIC STATISTICAL BET (§4.3): bursty non-coincident `offeredLoad` + the `slaBandwidth`-reading bandwidth axis + the NET GOLDEN re-pin (M1-remediation P4)
**Status: ACCEPTED (built; the bursty load + the slaBandwidth-reading bandwidth axis pinned in `net/escalation.test.ts` + `net/cost-blend.test.ts` + `net-replay.test.ts`; the net golden RE-PINNED; both old goldens byte-for-byte UNTOUCHED; `net/` stays pure).** P4 closes the audit's §4.3 finding that oversubscription was a DETERMINISTIC CLIFF, not the statistical-multiplexing bet the spec calls for: `offeredLoad` grew MONOTONE (a logistic ramp 1.0→1.4 that never fell or burst), the bandwidth axis tested a UNIFORM sat-side capacity constant, and each contract's own committed `slaBandwidth` floor was DEAD DATA. The §4.3 early-game tension (*N contracts, M sats — share a link across contracts whose peaks DON'T coincide; cut it as thin as you dare before a breach costs more than the hardware*) never materialised. **(1) THE BURSTY, NON-COINCIDENT LOAD (`contract.ts` + `session.ts`):** the escalation law now grows a SLOW `loadBaseline` (the §3a network effect — logistic toward the ceiling, frozen while breaching), and the realized `offeredLoad` is the bursty `burstyOfferedLoad(baseline, t, loadPhase, noise01)` = `baseline·(1 + AMP·sin(2π·t/PERIOD + phase) + NOISE_AMP·(2·noise01−1))` — a periodic "diurnal" oscillation (a pure function of `t`; `NET_LOAD_DIURNAL_PERIOD_S = 300`, `NET_LOAD_DIURNAL_AMPLITUDE = 0.45`) PLUS a bounded `±NET_LOAD_NOISE_AMPLITUDE = 0.06` noise term drawn from the session's SEEDED splitmix64. The PER-CONTRACT `loadPhase` is a deterministic pure hash of the contract id (`loadPhaseForId`, an FNV-1a fold into `[0,2π)`), so REGION-0 and REGION-2 peak at DIFFERENT times (NON-COINCIDENT) — sharing a sat across them is viable when their peaks miss, and tips one toward breach when they coincide. The load RISES ABOVE and FALLS BELOW its `slaBandwidth` over time. The noise is drawn ONCE per active EARTH contract per step, in `contractList` order (Mars is fenced — §8), AFTER the fault roll — a deterministic, replay-safe ordering. **(2) THE BANDWIDTH AXIS READS `slaBandwidth` (`router.ts`):** the dead committed FLOOR (§4.1) is now LIVE. `RoutableContract` surfaces `slaBandwidth` + `offeredLoad`; the bandwidth verdict is: while the shared peak is ≤ `NET_LINK_CAPACITY_UNITS` (kept as the per-antenna PHYSICAL capacity) the link honors every sharing contract's full offered load (always MET — a low-demand contract under its own floor is met too), and ONLY when a coincident-peak spike pushes the shared peak STRICTLY OVER capacity is it shared in PROPORTION (`servedBandwidth = capacity·ownLoad/sharedLoad`) — the contract BREACHES iff that over-subscribed share falls below its OWN `slaBandwidth` (`NET_DEFAULT_SLA_BANDWIDTH` lowered 1.0→0.6, the floor a contract alone always honors). NOT a flat `sharedLoad ≥ capacity` cliff. **(3) THE RE-TAME WITNESS is now STRUCTURAL (`session.ts`):** a contract re-tames when it is fully SERVED and the SOLE active loader of its bridge sat (the SPLIT happened) — so a transient coincident bursty trough on a STILL-shared, still-oversubscribed sat can never spuriously latch it (the brief's "the bet"). The canonical act3a arc is UNCHANGED (`TICK_RELIEF = 27000`): REGION-2's near-breach dip crosses the 60 s threshold ~t=447 (it shares NET-SAT-5 with REGION-0, a coincident peak over capacity), the player's relief (a parallel equatorial LEO + a `net_set_prefer(REGION-0)` yield) splits the shared sat at t=450, and the corridor re-tames a tick later — the dip + re-tame still latch like P3 did. **(4) THE FOLD ADDITION + GOLDEN RE-PIN (`net-replay.test.ts`):** `loadBaseline` is folded right after `offeredLoad` (the slow state the next step's growth depends on; the bursty realized value alone could collide across distinct baselines); `loadPhase` is stable-per-id (derived, not folded). `NET_REPLAY_GOLDEN` **`8969122486022400018n` → `4638365066733440034n`.** What moved the fold: the bursty per-step `offeredLoad` + the new `loadBaseline` fold, and the per-contract noise draws advancing the SHARED rng stream — which SHIFTED the downstream act3b fault stream so the telegraphed fault now drops a DIFFERENT polar LEO (NET-SAT-3, was NET-SAT-4). REGION-1's redundant polar N=4 still WEATHERS the permanent drop (active, never failed; its breach stays ≪ the fail grace), the acts all gate deterministically (act1..act3b fire; the cursor reaches + STOPS on act4), and the HORIZON is UNCHANGED (560 s). One D1 invariant was re-pointed from REGION-1 to REGION-0 as the robustly-served-at-the-horizon Earth example (the N=4→N=3 drop sawtooths a brief hand-off gap that can land on the exact horizon tick; REGION-1 is asserted weathered/not-failed instead). **Verified:** the updated escalation invariants (the BASELINE freezes while breaching — the bursty offeredLoad keeps oscillating; the re-tamed corridor is the SOLE loader of its split bridge), the rewritten cost-blend bandwidth invariants (an over-subscribed share below `slaBandwidth` breaches; a contract ALONE on its bridge is served even above capacity; no `slaBandwidth` floor ⇒ never binds), and the re-pinned golden replays bit-identically (replay-twice, live==replay, the SaveGame JSON round-trip, and restore-then-step == continuous-run all reproduce `4638365066733440034n`). The two existing goldens (`544847093270497462n` M1 cache, `8431658617016421069n` M2 build) are byte-for-byte UNTOUCHED (different worlds; neither imports `net/`). `net/` stays PURE — `burstyOfferedLoad`/`loadPhaseForId` are periodic-of-`t` + a pure id-hash, the only randomness is the session's seeded splitmix64 (no wall-clock, no unseeded RNG). tsc clean; vite build clean; the DEV-server smoke booted with no console/page errors. **`NET_LOAD_DIURNAL_PERIOD_S` / `_AMPLITUDE`, `NET_LOAD_NOISE_AMPLITUDE`, and `NET_DEFAULT_SLA_BANDWIDTH` are placeholders on the toy `offeredLoad` scale — tune later.**

### SD-39 — §7 routing solver RESOLVED in spec v0.2 (supersedes the SD-38 "user-owned" framing)
**Status: ACCEPTED (user shipped spec v0.2 with §7 fully specified; pushed as `d5b1929`).** SD-38 recorded §7 as USER-OWNED / do-not-design; v0.2 closes the last open design atom, so that framing is **superseded** — §7 is now a concrete buildable epic. The locked shape: a **deterministic shortest-path-over-time-varying-graph solver** (Dijkstra/A* over the current line-of-sight adjacency), re-solved on **topology-change events** (link in/out of view, fault, demand change) — NOT per tick, NOT a real routing daemon (so determinism/golden-master survives; §7.1). Link cost is a **physics-blend** `w_lat·latency_term + w_bw·congestion_term + w_stab·instability_term` where the `*_term`s are physics-truth and the `w_*(class)` weights are the design surface set per traffic-class (so demand-shape produces topology-shape; §7.2). First player tunable = the **per-contract prefer-latency↔bandwidth↔stability weight** (§7.3). Three control tiers (auto floor / slider+constructor ceiling / FRR-style terminal). A **self-diagnosing trace view** that surfaces the binding constraint + the kind of fix is an **M1 necessity** (§7.4).

**M1 scope (§7.5 LOCKED): reactive only — `w_stab = 0` (latency + congestion); the cost-blend + per-contract weight present but the stability term DORMANT; floor + basic-ceiling tiers; the trace view; and the PREDICTABILITY SEED — the trace stamps every link loss with its geometric cause + time ("link X↔Y lost: Y set below horizon at 14:32").** Deferred to M2+: `w_stab > 0`, predictive routing as a *discovered* capability, the efficiency-vs-resilience / fragility-cascade tension, the routing↔fault coupling, BGP peering, the full visual constructor.

**Plan adjustment (`docs/backlog.md`):** the `M1-SOLVER` "external/user-owned" placeholder is replaced by the **M1-SLV-1..5** epic (solver core / cost-blend + per-contract weight / control tiers / trace view / predictability seed); the Acts 2–3 + cross-cutting tickets retargeted from `↯ blocks-on M1-SOLVER` to `↯ needs the §7 solver epic`. **Act 1 is UNCHANGED** — it is single-sat bent-pipe connectivity; its `a1/reachability.ts` stub (`(eph, region, ground, sats, t) → {reachable, latency}`) is exactly the seam the §7 solver subsumes later, so the in-flight pure-sim build (workflow wn0pg4b7x) needs no change. The one Act-1 touch this enables: the LEO-set / region-dark serve-breach log should stamp the geometric cause + time (the §7.5 seed's first grain) — folded into build #2.

### SD-38 — M1 reframe to the Earth-orbit connectivity game + the spinning-Earth Act-1 prototype (the new mechanics spec)
**Status: ACCEPTED (user fed the concrete M1 mechanics spec `docs/signal-horizon-m1.md` Part I; ran spec→plan→build workflows; user-approved the spinning-Earth call).** The GDD is the feelings doc; the new spec is the opposite register (numbers/structures/fences) — the antidote to the "beautiful orrery, no gameplay" screensaver ([[gameplay-first-not-screensaver]]). **M1 is no longer the Mars-cache freshness loop — it is the Earth-orbit satellite-ISP CONNECTIVITY puzzle:** accept a pre-shaped standing 3-axis SLA contract → launch via a consequence-showing planner → tune-by-exception a deterministic solver that diagnoses its own shortfall; serve-or-breach heartbeat; oversubscription tension; a one-hour Acts 1–3 curriculum + a fenced Act 4 Mars teaser.

**Reclassification (recorded in `docs/backlog.md`, the codebase is reframed more than invalidated):** the cache/freshness/information-economy (old "M1 core") → the Act 4 concepts teaser + otherwise post-gate (§8); coverage hex grid → M2 (M1 demand is region/point endpoints); M2 contracts → the M1 contract skeleton needing a genuinely-new 3-axis SLA; escalation/rivals bundle SPLITS (escalation → M1 Act 3, rivals → M2-fenced); M3a datacenters → out of M1. Reused wholesale: the determinism backbone, orbit math, the whole orrery render pipeline, the accept verb, the launch-failure roll. **§7 (routing solver) is USER-OWNED** — recorded as a hard external dependency (`M1-SOLVER`); Acts 2–3 block on it, Act 1's reachability stub is the seam it subsumes; **I do not design it.**

**The spinning Earth (user decision):** the shared ephemeris had no body-spin, so a GEO sat drifts instead of parking over a spot — contradicting the spec's locked §2 physics (GEO parks / LEO sweeps) and the transferable-knowledge pillar. The user chose "add the spinning Earth first." Act 1 therefore adds a **body-fixed rotation scoped to a new Act-1 world** (`θ(t)=ω·t` about +Z), and is the spec's real Act 1: a GEO-PARK preset (parks, holds, clips a high-lat region → aim it) vs a LEO-SWEEP preset (covers while overhead, then sets → honestly previews the Act-2 availability wall).

**The Act-1 design (build-ready, `docs/signal-horizon-m1.md` Part II; physics verified in source via revise→adversarial-verify→finalize, two critics each caught real shipped-bugs-in-waiting).** Lives in a fresh isolated `src/sim/a1/` world with its OWN seed + OWN golden — the existing goldens (`544847093270497462n` M1, `8431658617016421069n` M2 build) are NOT touched. Locked decisions: **(A)** the rotation frame, with the epoch-correct `m0Rad = subLon + ω·epochS`; **(B)** pacing synthesis — keep real `EARTH_MU` unforked, set Earth's rotation period == the GEO orbital period (~240 s toy day) so the equatorial GEO is geostationary BY CONSTRUCTION (`ω == meanMotion`, verified bit-equal; GEO body-fixed spread 1.5e-13°, LEO sweeps 358°); the real-μ+~4-min-period constraint forces a **toy body radius `A1_BODY_RADIUS_M = 300 km`**; **(C)** ONE availability bar `A1_AVAILABILITY_BAR = 0.85` governing MET + gated pay + grace (removes a MET-vs-proportional contradiction); **(D)** an engineered, pinned imperfection (GEO default windowMin 0.802 < bar → FAILS unaimed = the auto-pick guard; dish-1.1 → 1.0; LEO default 0.0), sample counts pinned at 400/200; **(E)** non-dominant GEO vs LEO (latency re-quoted as bent-pipe one-way 3.57/2.07 ms); **(F)** preview = window-min `HOLDS:%`, not instant; **(G)** an A1-mode render-scale override driving the de-squash `surfaceM` + shell from the toy radius (else the toy world log-folds to sub-pixel — the make-or-break viz collapse), scoped so M2/M3 visuals + the M2 coverage grid stay byte-identical off-mode. Build order: tasks 1–6 (pure sim → new golden → physics-gate tests) MUST be green before tasks 7–9 (planner UI, spinning-globe render, serve/breach + scenario). **This entry records the design + reframe; no Act-1 code is written yet.**

### SD-34 — M2-fix: anchor the M2 schedulers to the session start + clamp shock compounding (the live epoch-mismatch bug)
**Status: ACCEPTED (autonomous bugfix; live-only, no design change).** The live `BuildSession` is driven in `main.ts` on the SHARED clock, which boots at the M1 scenario epoch (`SCENARIO.tick0` ≈ 14.5M s — the conjunction approach). But the M2 schedulers were written assuming a session starts near t=0: the event generator (`FIRST_EVENT_AT_SECONDS = 2400`), the contract offer generator (first offer ~1800 s), and the demand-growth cadence (`lastGrowthAtS = 0`) all init their cursors near 0. On the FIRST live step at t ≈ 14.5M s every cursor was ~14.5M s OVERDUE, so the catch-up loops fired their whole backlog at once (the event generator hit its 64-fire cap), the per-cell demand-shock overlays compounded MULTIPLICATIVELY on shared cells (`shockMulScratch[id] *= m`), and the demand total ran to ~1e40 — the orrery DEMAND·GROWTH readout painted "+6.5e39%". The `m2-build-replay` golden drives from t=0, so it is internally consistent and never saw this: the bug was purely the live epoch mismatch.

**Fix 1 — lazy first-step schedule anchor (`session.ts` + the two generators).** On the FIRST `BuildSession.step(t)` (a one-time `anchored` latch), `anchorSchedulers(startT)` re-anchors every cursor RELATIVE to `startT`: `ContractGenerator.anchorAt`/`EventGenerator.anchorAt` set `nextOfferAtS = startT + FIRST_OFFER_AT_SECONDS` / `nextEventAtS = startT + FIRST_EVENT_AT_SECONDS`, and the demand-growth cursors become `lastGrowthAtS = startT`, `nextGrowthAtS = startT + GROWTH_INTEGRATION_SECONDS`. So a 14.5M-s boot schedules its first event at 14.5M + 2400, not a 14.5M-s backlog. **When `startT === 0` (the golden replay path) the re-anchor reproduces the EXACT constructor values, so the t=0 fold is byte-identical** — the lazy first-step anchor was chosen over a constructor arg precisely so the test (which steps from t=0) stays unchanged. `restore()` sets `anchored = true` so a resumed session keeps its snapshot cursors (never re-anchors).

**Fix 2 — clamp shock compounding (`session.ts applyShockOverlay`).** Each active shock contributes a per-cell multiplier ≥ 1.0 and overlapping shocks compound their PRODUCT; the compounded per-cell `shockMulScratch[i]` is now clamped at `MAX_SHOCK_MULTIPLIER = 4.0` (comfortably above a single shock's 2.6 peak, so normal overlaps are unclamped). Belt-and-suspenders next to the anchor: even a pathological concurrent-shock pile-up can never blow demand to Infinity.

**Fix 3 — guard the readout (`orrery.ts`).** The DEMAND·GROWTH segment now renders only when `baselineDemand > DEMAND_GROWTH_EPSILON (1e-9)` AND the growth % is finite + in `[0, DEMAND_GROWTH_MAX_PCT (1000)]`; otherwise the segment is OMITTED — the chrome can never paint scientific notation again (the sim, not the readout, would be wrong).

**Determinism.** The M2 build-loop golden stayed **`8431658617016421069n`** (the t=0 path is byte-identical) and the **M1 golden `544847093270497462n` is UNTOUCHED**. New `m2-epoch-anchor.test.ts`: starting a session at the 14.5M-s epoch fires **0** events on the first step (pre-fix: 64) with demand ratio **1.0** (pre-fix: ~877× on step 1, ~1e38 over time); demand stays bounded under sustained stepping at the live epoch; the clamp alone bounds a long shock-heavy run; LIVE==REPLAY holds at the large epoch; and the t=0 first-event still lands at ≥2400 s. 441→446 tests; tsc + build clean; `src/sim` pure (randomness only via the seeded PRNG). **Readout shown sane via** the CONTRACTS preset (key **7**) with the heatmap toggled (**H**) at the live epoch.

### SD-33 — M3a: the first orbital datacenter + the thermal/power model (compute as a force-multiplier)
**Status: ACCEPTED (autonomous decision; M3a, the first M3 spine ticket).** GDD §4.5 makes compute BUILDABLE INFRASTRUCTURE that force-multiplies the information loop — under the explicit hard constraint (Risk-5 / the Per Aspera cautionary tale): **a small number of high-impact strategic nodes you place/power/cool, NOT a sprawling base-builder.** M3a is the first such node + the §4.5 physics.

**The pure DC node + the two physical ceilings (`src/sim/m3/datacenter.ts`, pure).** A `Datacenter{ bodyId, subLatRad/subLonRad (its footprint anchor), panelM2, radiatorM2, rtg }`. `resolveDCCompute(eph, dc, t)` is the model: **POWER** = solarFlux(∝ 1/d² from the Sun, via `bodyDistanceAU`) × panelM2 × `PANEL_W_PER_M2_AT_1AU` (+ a constant `RTG_POWER_W` floor if fitted — distance-independent, the nuclear lever for the dark outer system); **COOLING** = radiative-only, a hard thermal rejection cap = radiatorM2 × `RADIATOR_W_PER_M2`; **COMPUTE BUDGET** = `min(power/COMPUTE_W_PER_UNIT, rejectableHeat/THERMAL_W_PER_UNIT)` — BOTH ceilings bite. Worked numbers (default 1.5 m²/1.5 m² node): Earth (1 AU) → 450 W, 8.33 u, THERMAL-limited; Mars (1.52 AU, solar-only) → 195 W, 3.90 u, POWER-limited (the 1/d² penalty); Mars + RTG → 1395 W, back to 8.33 u (the RTG rescues it). All dials are named constants.

**The force-multiplier (the §4.5 point — feeds the loop or it's cut).** `computeLiftMultiplier(compute) = 1 + MAX_COMPUTE_LIFT·c/(c+HALF)` — a saturating, capped (≤ +40%) lift. `DCRoster.liftFor(eph, region, t)` returns the BEST (max, not sum → no stacking runaway) lift of any DC whose SUB-POINT footprint (`DC_FOOTPRINT_RADIUS_RAD` great-circle cap, same body) covers the region centroid. `BuildSession.step()` reads it: an ACTIVE contract being SERVED earns `tariff × frac × lift × dt` (the lift applies only to served revenue — a breach penalty is untouched; "transmit conclusions not bytes"). Bounded + co-located + per-region → a genuine strategic lever (WHICH region to lift), NOT a side-game.

**Placement + determinism.** New logged capex action `place_dc` (payload: candidate site index) through the SHARED `applyBuildAction` → `BuildSession.placeDatacenter(idx)` (charges `DC_CAPEX_EUR` €6,000 — a dear, sparse node; `src/sim/m3/dc-sites.ts` candidate list: 4 Earth regions + Moon + Mars). The DC roster folds into the BuildSnapshot/state-hash; the lift accrual is DT-invariant (continuous rate × dt, summed into one wallet apply, like the contracts — verified). `Contract` gained `centerLatRad/centerLonRad` (the footprint anchor). **M2 build-loop golden RE-PINNED `6225853297339560787n` → `8431658617016421069n`** (the golden log now places a DC over c0's region; DCs + the lift are in the fold). **M1 golden `544847093270497462n` UNTOUCHED.** 427→438 tests; tsc + build clean; `src/sim` pure (no three/DOM/wall-clock/RNG).

**Render (§8).** A placed DC renders as a distinct hot-VIOLET signal node (set apart from green assets / amber packets / cyan feed legs) with a compute HALO scaled by its compute budget + a dither that coarsens when thermally throttled (redundant colour-off cue); a bl-corner readout `DC <site> · <W> pwr · <W> rej · CMP <u> [PWR|THERM] · LIFT ×N.NN`. Place key **M** (` ' ` cycles the site); footer hint added. f64→f32 stays in the orrery. **Screenshot-verified**: a violet DC node over EARTH · SOUTH AMERICA reading `436W pwr · 375W rej · CMP 8.3u [THERM] · LIFT ×1.23`.

**Scope discipline / deferred (M3+).** Autonomy tiers gated on DC compute (§4.6), the cache/coherence-host + brokerage roles (§4.5), DC upgrades (grow panels/radiators/add RTG), radiation/failure, multi-launch DC construction projects, orbit-phase eclipse shadowing, the §4.10 information-mint endgame. The §4.5 numbers are placeholders — tune later.

### SD-35 — Live orrery: show the physics, give the player the clock (render/interaction/clock-boot)
**Status: ACCEPTED (autonomous decision; the first real step from screensaver → game).** Owner feedback (empirically verified): the game read as a screensaver — "satellites are static / not moving at all", "scale is locked at 1000% — I want to control the speed myself", "I need to see launched satellites' orbital planes like the dataset LEO/GEO", "I need to click celestial bodies to focus them." Diagnosis (confirmed by two-sim-time screenshots): the sats DO orbit and the clock DOES advance, but (a) the SD-5 radial log-fold squashes a LEO/GEO orbit almost onto Earth's billboard so the motion is sub-pixel on the view the player looks at; (b) the live clock BOOTED LOCKED at 1000×; (c) `buildRings()` drew rings only for the dataset `RING_IDS`, never the M2 roster's launched sats; (d) deployed sats drew only with the heatmap on; (e) no click-to-focus. This is **render + interaction + clock-boot ONLY** — no sim/economy/coverage/roster MATH changed; **both replay goldens (M1 `544847093270497462n`, M2 `8431658617016421069n`) are UNTOUCHED** (confirmed by the replay tests).

**(1) Orbital motion legible in the primary view — the near-body de-squash (a documented honest-lie, render-only).** New `src/orrery/orbit-render-scale.ts`: `orbitRenderRadius(d, scale)` — a PURE, strictly-monotonic radial remap applied BEFORE the SD-5 log-fold (`compressScale`) for points within a per-preset near band of the focus body. It is identity at/below the surface (ground stations / the disc / the coverage shell render exactly as before — NO regression) and identity beyond the band (the Moon ring, Earth↔Mars, the system view untouched); inside the band, above the surface, the true altitude maps through a concave curve `lift + (A−lift)·(a/A)^p` (p≈0.32) that lifts LEO clear of the disc and fans LEO/MEO/GEO into clearly separate visual radii — exactly the gap the log-fold destroys. The ANGULAR direction is preserved exactly (only rendered RADIUS is scaled), and like the log-fold it NEVER feeds coverage/link/light-delay math (it lives in src/orrery, the only f64→f32 site). A new **EARTH** camera preset (the boot default) carries the near-body band ON; CISLUNAR/ORBITS get it too; SYSTEM/TOP-DOWN keep the honest log-fold only.

**(2) Launched/deployed sats get orbital-plane rings.** `buildRings()` was dataset-`RING_IDS`-only; new `rebuildSatRings(roster)` wires the M2 roster's launched sats into the SAME dashed-ring renderer, sampling each sat's ring from its OWN `SatOrbit` Kepler elements via the pure `solveOrbit` swept over one period (new `sampleSatOrbitRelative`, mirroring `Ephemeris.sampleRelativeOrbit`), in the same body-relative frame the dataset rings use. Rebuilt ONLY when the launched-sat set changes (keyed by id+epoch+a — X-02, never per frame); positioned each frame through the same rebase→de-squash→fold path as the dataset rings.

**(3) Deployed sats drawn ALWAYS + orbiting.** `updateBuildMarkers` / `updateDCMarkers` are now called every frame (not heatmap-gated) — the player's constellation is the monument, positioned from its Kepler elements each frame so it sweeps with the clock. Only the coverage SHELL stays heatmap-gated. (Ground stations stay surface-pinned in the no-rotation ecliptic frame — a known limitation, not fixed here.)

**(4) Click-to-focus / select.** New pure `src/orrery/pick.ts` `pickNearest(candidates, click, tol)` — screen-space nearest-projected-billboard within a px tolerance (the robust pick for constant-screen-size sprites). Canvas pointerup with < 5 px travel is a CLICK (not a drag): the orrery projects every body + roster asset + DC, picks the nearest, marks it SELECTED (a cyan reticle), and focuses the camera if it is a focusable body. Raycast/pick runs ON CLICK ONLY (never per frame). F-cycle stays as the secondary select path.

**(5) Player controls the clock — boot at 1×, not 1000×.** `main.ts` no longer forces `clock.scaleIndex = SCENARIO.defaultScaleIndex` (1000×); it boots `scaleIndex = 0` (1× real-time) so the player drives the acceleration from t=0 (GDD §3/Risk-6 — the waiting is decision-space, not a slider you watch). `,` / `.` cycle the full 1×…1000× range, Space pauses, the live scale shows in the status strip. LIVE-only: the replay harness drives ticks directly (not via this clock's scaleIndex), so the goldens are unaffected. The M1 scenario EPOCH boot is unchanged; the foreshadow NOTICE still points a passive player at the speed keys. (`SCENARIO.defaultScaleIndex` stays in the scenario data for messaging.)

**(6) Coverage sweeps with the sats.** The heatmap already recomputes per-frame off the live roster positions; with the sats now visibly orbiting, the lit cells visibly sweep as they pass (verified at two sim-times). Camera keys re-mapped: **E** EARTH (default) · C CISLUNAR · O ORBITS · S SYSTEM · T TOP-DOWN.

**Tests + verification.** New pure unit tests: `orbit-render-scale.test.ts` (identity below surface + beyond band, LEO/MEO/GEO separation, strict monotonicity, edge continuity), `pick.test.ts` (nearest within tolerance, off-screen skip, tie stability, edge), `sat-ring.test.ts` (a launched sat samples a full closed ring; GEO ring ≫ LEO ring; epoch-independent; degenerate-orbit safe). 462 tests pass; tsc clean; build clean. **Screenshot-verified** (headless SwiftShader): EARTH default with multiple separated launched-sat orbit rings + the coverage shell; an A/B at +30 sim-min showing the sats swept along their rings; clicking Mars in SYSTEM reframing focus SUN→MARS with the reticle; the scale ramp 1×→10×→100×→1000× + pause. **Minors (flagged):** the de-squash lift/exponent + EARTH-preset band are visual-tuning placeholders; ground stations float at the true-folded surface radius (a pre-existing billboard/fold decoupling, unchanged). All render tunables are placeholders — tune later.

### SD-36 — Mouse-driven chrome: on-canvas camera buttons + 3 WM presets + a window-summon rail (WM/DOM/interaction only)
**Status: ACCEPTED (autonomous build from owner feedback; the through-line is mouse-first direct interaction, GDD §5).** Owner feedback: (a) "camera presets need to be BUTTONS you can mouseclick inside the orrery, in addition to hotkeys"; (b) "there are too many tiling presets — let's come up with main 3 and then have vertical BUTTONS ON THE RIGHT to summon windows into current tiles LIVE." This is **WM/DOM/interaction ONLY** — no sim/economy/coverage/roster MATH changed; **both replay goldens (M1 `544847093270497462n`, M2 `8431658617016421069n`) are UNTOUCHED** (confirmed by the replay tests — the chrome is pure presentation/event wiring).

**(1) On-canvas camera-preset buttons.** New `Orrery.buildCameraButtons()` adds a compact, edge-docked button group (bottom-centre of the orrery overlay, §8 1-bit chrome) with one button per `CAMERA_PRESETS` entry — EARTH · CISLUNAR · ORBITS · SYSTEM · TOP-DOWN (the SD-35 set). A click calls the SAME `setPreset(i)` path the E/C/O/S/T hotkeys use (the hotkeys STAY); `paintCameraButtons()` lights the ACTIVE preset (cyan-filled, redundant with the inverted treatment so it reads colour-off), repainted on every `setPreset` (click OR hotkey) — event-driven, never per-frame (X-02). The button bar lives in the overlay (`pointer-events:auto`) layered over the WebGL canvas and `stopPropagation`s its pointer events, so a button click never starts a camera-orbit drag or the click-to-focus raycast underneath.

**(2) The 7 WM presets cut to 3.** `presets.ts` retires OVERVIEW/OPS/TRACK/STREAM/SPLIT/PARSE/CONTRACTS (keys 1–7) for **PLAY · MAP · REVIEW** (keys 1–3): PLAY = orrery hero + FINANCE/CONTRACTS stack (the default boot working screen); MAP = orrery near-full-bleed single zone (the monument — watch + fly + click + heatmap); REVIEW = THE PARSE wide + SYSTEM.LOG + FINANCE (the §4.12 at-rest record). Each is a valid DD-10 grid (≤3×3, always-tiled). No panel is lost — the retired recombinations are now composed ON DEMAND via the rail. The status-strip tabs + the key-hint legend (1-5 → 1-3) + the topbar/header hints updated; the G key (THE PARSE toggle) now summons PARSE via the rail or jumps to REVIEW (it was a dedicated preset).

**(3) The window-summon rail (the core ask).** New `src/wm/window-rail.ts` `WindowRail`: a VERTICAL button rail docked on the RIGHT edge of the WM canvas (§8 1-bit chrome, collapsed-strip-with-hover-expand), one button per available panel — ORRERY · SYSTEM.LOG · FINANCE · TELEMETRY · CONTRACTS · PARSE (`RAIL_PANELS`, with a clean commented seam for the future FLEET tile). Clicking a button SUMMONS that panel into the focused tile LIVE — it swaps the panel shown there, no teardown/relayout. Buttons show ACTIVE state (`.on` = the panel is visible somewhere; `.focus` = it occupies the focused tile), repainted only on `shell.onActivePanelsChange` (visible-set or focus change) — event-driven, never per-frame (X-02). The shell reserves the collapsed rail's 34 px strip (`setReservedRight`) so tiles never sit under it; the hover-expand overlays transiently.

**(4) The rail mechanism (DD-10-consistent).** The Shell now tracks a FOCUSED tile (`focusedHost`, set on a capture-phase pointerdown into any tile; `.focused` cyan chrome). `Shell.summonPanel(host)` is the rail's action: if `host` is ALREADY visible it just moves focus there (never duplicate); else it summons into the focused tile (falling back to the first/top-left tile when none is focused) via the new pure `zonegrid.summonInto(grid, targetHost, host)` — a Clone-Mutate-Validate op that replaces the target zone's active host with the summoned one (the displaced panel's view is hidden + re-summonable). The always-tiled invariant holds (every zone keeps exactly one host); `summonInto` returns null on a self-summon, an already-visible host (→ focus-instead), an unknown target, or an invariant failure, and never mutates the input. The 3 presets are starting points; the player composes the workspace by clicking the rail (the G key is the keyboard parity for summoning PARSE).

**Tests + verification.** New pure unit tests: `wm/presets.test.ts` (the set IS exactly PLAY/MAP/REVIEW; the 7 retired names are gone; each preset is a legal DD-10 grid; per-preset host sets + heroes), `wm/zonegrid.summon.test.ts` (summonInto swaps into the target tile, preserves the invariant, refuses an already-visible host → null, refuses self/unknown, does not mutate input), `wm/window-rail.test.ts` (the rail lists one button per registry host + covers every preset host + no duplicates; the camera-preset set is the SD-35 E/C/O/S/T order, EARTH at index 0). 462→478 tests pass; tsc clean; build clean. **Screenshot-verified** (headless SwiftShader, real mouse clicks): PLAY/MAP/REVIEW; clicking each camera button lights the active one (CISLUNAR, TOP-DOWN, back to EARTH); focusing the FINANCE tile then clicking the rail's TELEMETRY button summoned TELEMETRY into it LIVE (visible set orrery/finance/contracts → orrery/telemetry/contracts, the rail `.on` set following) with no teardown.

### SD-37 — The FLEET tile: the focused body's constellation (render/read-only)
**Status: ACCEPTED (autonomous build from owner ask; the through-line is direct interaction, GDD §5).** Owner ask: "I need a celestial-body tile that shows the satellite fleet around it." This turns the SD-35 click-to-focus into FLEET MANAGEMENT — select a body, see its constellation. This is **render/read-only ONLY** — no sim/economy/coverage/roster MATH changed; **both replay goldens (M1 `544847093270497462n`, M2 `8431658617016421069n`) are UNTOUCHED** (confirmed by the replay tests). It is a pure SELECT over existing truth, exactly like the contracts/parse projections.

**(1) The pure fleet-derivation (testable, render-free).** New `src/sim/m2/fleet.ts`: `deriveFleet(bodyId, datasetSats, rosterSats) → Fleet` returns the satellites orbiting THAT body — the dataset sats whose parent is the body PLUS the launched roster sats whose `SatOrbit` parent is the body. For each: a stable label, orbit CLASS (`classifyOrbit` — LEO ≤2000 km / MEO ≤35 000 / GEO ≤50 000 / HEO above, by altitude above the parent surface, so a launched LEO sat and the dataset `sat_leo` classify identically), altitude (a−R), period (min), inclination (deg), EIRP, kind (DATASET vs LAUNCHED) and status. Zero DOM/three/Ephemeris-class dependency — it takes plain element descriptors, so it unit-tests with plain objects (`fleet.test.ts`: a body with N dataset + M launched lists exactly N+M with correct classes; no cross-body leakage; a Mars launch is absent from EARTH's fleet; empty body + null focus → empty; deterministic).

**(2) The FLEET panel.** New `src/panels/fleet.ts` (`FleetPanel`, `PanelHandle`): a `.telem` 1-bit-chrome housing — a FOCUS header (BODY · count "N · M launched · K dataset") + a CONSTELLATION list, one row per sat: a kind GLYPH (▲ launched / △ dataset — the colour-off channel, tone-reinforced), the sat id (the §8 entity token), the CLASS word (cool→warm tone ramp LEO cyan → MEO green → GEO amber → HEO violet — the WORD is the channel), and a detail line (alt · period · incl · EIRP). §8 CVD-safe (every colour redundant on a glyph/word). A calm placeholder when no body is selected or the body has no fleet. The header is built ONCE; rows rebuild only on a cheap glanceable SIGNATURE change (X-02 — a propagating fleet does NOT rebuild; a launch / focus change / new sat does).

**(3) Wired to the SD-35 selection.** main.ts builds a `Fleet` each frame (`fleetRenderState`) from the orrery's focused body — the click-SELECTED id when it names a real ephemeris body (SD-35 click-to-focus), else the camera focus body — plus the live roster's launched sats (projected to the pure descriptor shape) and the cached dataset descriptors (`FLEET_DATASET_SATS`, read once off the ephemeris `sat_*` bodies). Clicking a different body re-lists; launching a sat (it joins the roster) makes it appear in that body's fleet. PURE reads of the live roster + ephemeris — no sim state added or mutated.

**(4) Registered in the SD-36 rail (the clean seam).** `RAIL_PANELS` gains `{ host: "fleet", label: "FLEET" }` (the commented seam realised) and the shell registry gains `["fleet", fleetPanel]`, so the right-edge rail has a FLEET button that summons it into the focused tile via the existing `summonInto` mechanism (the `summonPanel`→`views.has(host)` gate is satisfied by the registry entry). `wm/window-rail.test.ts` updated for the new host.

**Tests + verification.** `src/sim/m2/fleet.test.ts` (7 new pure tests). 478→485 tests pass; tsc clean; build clean. **Screenshot-verified** (headless SwiftShader, real mouse clicks + the L launch key): clicking the rail's FLEET button summoned it into the focused tile; EARTH's fleet listed 4 dataset (SAT_LEO/GEO/MEO_INC/MEO_POLAR) + the launched roster sats; pressing L added a launched ▲ sat to EARTH's fleet LIVE (5→6 rows); a canvas click on Earth's billboard (SD-35 pick) drove the same BODY EARTH listing; F-cycling focus to MARS re-listed to the empty placeholder ("no fleet around MARS — launch a sat (L) or focus another body"). **Minor (flagged):** dataset sats carry no per-sat EIRP in the descriptor (the field/score weights them at the nominal 1.0), so a dataset row reads `EIRP 1.0`; launched sats carry their real EIRP.

### SD-32 — M2f: the emergent-event generator v1 (rivals + news shocks) — M2 slice complete
**Status: ACCEPTED (autonomous decision; M2f, the last M2 ticket).** GDD §3 wants the network to be a place where things HAPPEN TO YOU (the Football Manager "stories" hook, Risk-7) — rival operators with names, breaking data-events, outages with consequences. M2f is the deterministic story/shock layer (deep rival-competition AI deferred to M3+).

**Model + generator (pure, seeded).** New `src/sim/m2/events.ts`: `M2Event` union — `demand_shock{region, cellIds, multiplier, durationS, cause}`, `rival_action{rivalId, kind: undercut|peer|relay_failure, spawnedContractId}`, `news{text, severity}` — each with a sim timestamp + monotonic seq, plus an append-only `M2EventLog`. New `event-generator.ts`: a cursor (first at 2400 s, mean 4 sim-h ± 2 h) drawing every choice off the SESSION's seeded splitmix64 stream (after the contract generator's draw, fixed RNG order) — same seed ⇒ identical timeline; a 64-fire catch-up cap bounds big-dt bursts. New `rivals.ts`: 3 fixed named operators with §8 IDENTITY HUES distinct from the data ramps — **HELIX RELAY** (violet #8e84ff), **MERIDIAN LINK** (orange #ff7a1a), **PALE BLUE NET** (blue #5aa9ff, new `--faction-blue`).

**World coupling (events matter + expire cleanly).** A `demand_shock` registers an `ActiveShock`; each step `applyShockOverlay(t)` computes a per-cell multiplier `1 + (peak−1)·(1−elapsed/duration)` (linear decay → 1.0), drops expired shocks, and pushes the overlay into `DynamicDemand` via `setShockMultipliers()`. `DynamicDemand.of()/total` return `current·shockMul` — so a shock ripples through every scorer, the contract served-fraction, and the readout — but `step()` grows the RAW `current[]`, so a shock can NEVER inflate the bounded logistic growth, and at shock end the effective demand equals the raw grown demand exactly (verified to 1e-6). A rival `relay_failure` calls `spawnRelayFailureContract()` — a real OFFERED contract on the board (distinct `r{N}` id, ×1.6 premium tariff): "their customers come knocking" (verified: MERIDIAN LINK over NORTH AMERICA → r0 at ~€44k/hr).

**Log surfacing (§8, truthful).** `log-format.ts` gains `formatM2Event()` + a `faction` token slot; `log.ts` gains `renderM2()` (a second seq cursor draining the M2 tail incrementally, interleaved into the existing SYSTEM.LOG); `main.ts` calls it each frame. A shock row is warn-severity (region entity + ×N.N value + cause prose); a rival row paints the operator NAME in its faction hue (relay_failure = error severity + the spawned-offer id); news shows the headline. Lines are TRUTHFUL — the shock line is emitted only after the overlay is registered; the relay line names the real spawned contract.

**Determinism.** Generator cursor + active shocks + spawnedContractCount + the whole M2 event stream fold into the BuildSnapshot + the M2 state-hash; the shock overlay is re-derived on restore from `activeShocks` at `t`, so replay reproduces the events AND their effects bit-identically. M2 golden re-pinned `15734905161678697793n` → **`6225853297339560787n`**; the **M1 golden `544847093270497462n` is untouched**; the M1 SYSTEM.LOG keeps its own seq space. 418→427 tests; tsc clean; build clean; src/sim pure (randomness only via the seeded PRNG; `m2/purity.test.ts` extended to the 3 new modules). **Screenshot-verified**: the SOUTH-AMERICA dust-storm shock + a `✕ MERIDIAN LINK RELAY FAILURE over SOUTH ASIA — their customers come knocking` line in its orange faction hue.

**This completes the M2 Earth Tier-1 vertical slice** (M2a grid → M2b heatmap/monument → M2c build loop → M2d contracts/revenue → M2e escalation → M2f emergent events): the full GDD §9 M2 scope (coverage field, link budget, placeable assets + launch market, contracts state machine, the escalation engine, an emergent-event generator v1, one coverage heatmap) built + adversarially verified, on a SEPARATE deterministic M2 world from the M1 cache session. **Minors (flagged):** imprecise flavour numbers in the build prose (bare-generator vs live-session RNG order — load-bearing facts correct); the shock-expiry test's 300-s flake-guard window; multiplicative shock compounding (intentional, bounded). Deep rival-competition AI (rivals building coverage, bidding contracts, faction trust/price/exclusivity, §4.10 information warfare) is DEFERRED to M3+. All M2f numbers are placeholders — tune later.

### SD-31 — M2e: the escalation engine (demand grows where you serve)
**Status: ACCEPTED (autonomous decision; M2e).** GDD §3b names generator 1 — "demand grows where you serve" — as the PRIMARY escalation generator + the campaign heartbeat, and Pillar 6 demands success CREATE the next problem (not plateau). Until M2e demand was static, so a fully-built network stayed solved. M2e makes serving a region grow its economic weight (the §4.9 network effect) until it outgrows the capacity you built — "every served gap re-opens one size larger" (the OpenTTD loop).

**The growth law (pure, DT-invariant, bounded).** New `src/sim/coverage/dynamic-demand.ts`: per-cell CURRENT demand `d` rides on the immutable M2a baseline `b`, with carrying capacity `cap = b × CAPACITY_MULTIPLIER (3×)`. Integrated by the EXACT CLOSED-FORM FLOW of each branch (a semigroup ⇒ DT-invariant, not Euler): SERVED (quality q>0) → logistic toward cap `d' = cap/(1 + ((cap−d)/d)·exp(−GROWTH_RATE·q·dt))` (GROWTH_RATE=8e-5/s) — asymptotes to cap, can never blow to Inf/NaN; UNSERVED (q=0) → exp relaxation toward baseline `d' = b + (d−b)·exp(−DECAY_RATE·dt)` (DECAY_RATE=1e-5/s). `q` is the per-cell served quality (0/1 at the connectivity≥1 bar) from the live roster, so demand grows exactly where the heatmap shows service. A new `DemandReader` interface (`of()`/`total`) makes static + dynamic demand drop-in for every scorer; the static M2a `DemandField` stays the immutable floor (M1 + static scorers untouched).

**Wiring + perf.** `BuildSession.step` advances growth on a fixed sim-time cadence (`GROWTH_INTEGRATION_SECONDS=60`): contract revenue accrues first (reads the demand at step start), then when `t` crosses the next 60-s boundary it sweeps the whole-grid served-quality ONCE (reusing the same Kepler positions + the allocation-free `coverageDimsAt` gates) and advances `DynamicDemand` over the elapsed time by the closed-form flow. Throttling to a 60-s cadence (exact, so chunking doesn't change the result) keeps the 420k-tick replay ~1.2 s. Firing on `t` (which `tick·DT` hits EXACTLY at 60 s — multiplication, no accumulator drift) — not on a drifting elapsed accumulator — is what lets fine + coarse callers sample the discontinuous 0/1 served gate on the SAME geometry at the SAME boundary, so growth stays bit-DT-invariant through the geometry.

**Escalation is felt (verified).** Fixed roster (5 ground + 3 sat), time-averaging the covered fraction across orbital phase: baseline total demand 154.3, covered 0.546. After a 150,000-s served run the engine grew total demand to 392.3 (2.54×, bounded under 3×) and the covered fraction ERODED to 0.507 (absolute covered demand rose 84→199 but couldn't keep pace with the ballooning total). Then adding capacity (6 LEO + 6 MEO) lifted it to 0.914 — re-solving the now-bigger demand, which then grows faster from the higher base: the renewing cycle. A starter-only roster grows total only 1.20× (serves little → grows little) — the growth is causal on service.

**Determinism + render.** The per-cell `current[]` + the growth cursors fold into the BuildSnapshot + the M2 state-hash; the M2 golden re-pinned `5706799219860839795n` → **`15734905161678697793n`** (a swap-test confirms the dynamic demand moved it). The **M1 golden `544847093270497462n` is untouched**. 408→418 tests (+8 DynamicDemand unit, +1 escalation invariant, +1 purity); tsc clean; build clean; src/sim pure (no RNG in the growth law — a deterministic function of served quality + sim-time). The orrery readout shows `COVERED NN% · DEMAND·GROWTH +NN%` (covered fraction scored against the CURRENT dynamic demand). **Screenshot-verified**: after a served run, `COVERED 81% · DEMAND·GROWTH +55%` — demand visibly ballooned where served.

**Minors (flagged):** orrery keeps a coexisting static fallback `demandField` (the render prefers the dynamic build-state value; latent footgun); growth DT-invariance requires the 60-s cadence to divide the caller's dt (the only real callers use fixed DT=1/60, which lands exactly); the per-sitting erosion is gentle (−0.039 over ~41 sim-hr) — the constants (3×, 8e-5, 1e-5, 60 s) are named placeholders, a designer may want a stronger bite.

### SD-30 — M2d: contracts + coverage revenue (closes the gap→asset→integration→revenue loop)
**Status: ACCEPTED (autonomous decision; M2d).** M2c lets you build coverage and spend €; M2d pays you for the coverage you SERVE, so the GDD §3 loop (gap→asset→integration→**revenue**→bigger gap) finally closes and the wallet earns back its capex (GDD §4.9: revenue = demand met × quality × tariff − cost-to-serve).

**The contracts state machine (pure).** New `src/sim/m2/contracts.ts`: a `Contract` = a demand to cover a TARGET REGION (a sorted set of grid cell ids resolved from a demand-hotspot centre + angular radius) at a min QUALITY (connectivity ≥ 1 today; a `QualityAxis` enum leaves room for bandwidth/latency bars), a TARIFF (`regionDemand × TARIFF_PER_DEMAND_PER_SECOND=0.3` €/s at full service — a denser metro pays more and is harder to fully serve), and a TERM (~6 sim-h ± jitter). States: OFFERED →(accept)→ ACTIVE →(served-time hits term)→ COMPLETED, or → FAILED (servedFraction==0 sustained past `BREACH_GRACE_SECONDS=600`, or the 4-sim-h offer window elapses unaccepted). New `contract-generator.ts` offers contracts deterministically off the session's SEEDED splitmix64 stream (≤`MAX_OPEN_OFFERS=3`, mean 3 sim-h apart, first at 1800 s) — same seed ⇒ same offer timeline; cursor + count + rng state are in the snapshot.

**Revenue (continuous, DT-invariant, the SD-20 model).** New `BuildSession.step(eph, t, dt)` — M2c was event-driven; this adds the per-tick economy. Each ACTIVE contract accrues €/s = `tariff × servedFraction` (or −`BREACH_PENALTY_PER_SECOND=1.5` when wholly unserved — the SLA bite); `servedFraction` is the demand-weighted fraction of the region covered at/above the quality bar via the new pure `servedFractionAt()` over the LIVE roster (the same allocation-free `coverageDimsAt` gates the heatmap uses, so the € matches the lit cells). One summed `economy.apply(Σrate × dt)` per step ⇒ DT-invariant (verified 1× vs coarse dt to f64 noise). Step is wired into `main.ts` `tickSim` (on the fixed clock ticks) and the replay driver; capex stays one-shot in the appliers. The step early-outs when no contract is active, so the pre-acceptance run is ~free.

**Actions + render.** `accept_contract{id}` / `decline_contract{id}` logged SimActions via the SHARED `applyBuildAction` (live==replay; activation stamped at `atTick·dt`). Keys **N** cycle selected / **K** accept / **J** decline; `targetContractId()` falls back to the first OFFERED so K/J always act on something sane. New `src/panels/contracts.ts` (CONTRACTS board, WM preset **key 7**): a NETWORK summary (WALLET that now EARNS, REVENUE €/hr, EARNED, BOARD N offered·N active) over per-contract cards (region · quality · tariff · term · STATE as a colour-redundant WORD: `OFFER ⟨countdown⟩` / `SERVE NN%` / `BREACH ⟨clock⟩` / `✓ DONE` / `✕ ENDED`), §8 1-bit chrome.

**Determinism.** Contracts + generator cursor + `lastStepS` fold into the M2 state-hash; the M2 build-loop golden was legitimately RE-PINNED `2503511112643458855n` → **`5706799219860839795n`** (`m2-build-replay.test.ts` now drives `step(t)` per tick + post-step actions, and adds LOOP-CLOSES, DT-invariance, and decline tests). The **M1 golden `544847093270497462n` stays separate + untouched**. 393→408 tests; tsc clean; build clean; src/sim pure (randomness only via the seeded PRNG; `m2/purity.test.ts` extended). **Loop closes (verified):** golden replay €5000 → −€350 trough after ~€5,350 capex → the accepted contract earns €2,398 → final **€2,048 net-positive**; a separate live run climbed €1,304 → €6,192 while serving.

**Screenshot note (honest):** I confirmed by screenshot that the CONTRACTS board renders correctly (offer countdowns, tariffs, cell counts, the wallet reflecting capex EXACTLY: €5000 − 7×€250 − €3,500 launches = −€250) and that the FAILURE path is visible (accepting a contract whose region I hadn't covered → 0% serve → breach → `✕ ENDED €0`). I did NOT capture the active-serving wallet-climbing state via the blind screenshot harness — aligning scripted keypresses so the placed coverage matches the offered contract's region at the 1000× offer cadence isn't reliably scriptable — but the earning path is verified numerically by the golden replay + the verify agent's measured live run. **Minors (flagged):** a latent dt-dependent re-arm branch in the generator (unreachable at the current ≤2-open cadence; harden if `MAX_OPEN_OFFERS`/cadence change); sub-tick accept timing differs by ≤1 tick of revenue across coarse dts (bit-identical at the live fixed DT). Tariff/term/grace/penalty are placeholders — tune later.

### SD-29 — M2c: the build-the-monument loop (placeable assets + launch market)
**Status: ACCEPTED (autonomous decision; M2c, the heart of the Tier-1 slice).** GDD §3's loop is gap→asset→integration→revenue→bigger gap, and §1/§5 make the growing coverage web the player's MONUMENT. M2a/M2b built + rendered the coverage truth layer reading a fixed sat list; M2c makes coverage **a thing the player builds** — deploy ground stations + launch satellites, pay €, watch the web grow.

**A separate deterministic M2 world.** New `src/sim/m2/`: `roster.ts` (`Roster` of `RosterGround{lat,lon,alt,eirp}` | `RosterSat{orbit,eirp}`, monotonic ids, `worldPositions(eph,t)`), `orbit.ts` (a launched sat carries its OWN parametric Keplerian elements propagated by `solveOrbit` — the ephemeris's exact fixed-8-iter Newton + 3-1-3 rotation — so it is NOT added to the shared ephemeris and the orbital golden stays clean), `launch.ts` (3 presets LEO53°/MEO63°/GEO0° at €600/€1100/€1800, 4/7/10% fail), `sites.ts` (ground candidate sites + starter roster), `session.ts` (`BuildSession`: roster + its own `M1Economy` wallet at €5000 + a seeded splitmix64 `SimRng` for failure rolls + `launchedCount`; snapshot/restore by value), `apply-build-action.ts` (the SHARED applier). Two new logged `SimAction`s (`deploy_ground{siteIndex}` / `launch_sat{presetId}`) applied via that shared path live + replay. Keys **B** deploy / **L** launch / **;** cycle preset. A launch charges € ALWAYS (you pay the provider win-or-lose), then rolls failure from the seeded PRNG (replays bit-identically); on success a fanned circular orbit joins the roster and starts covering cells.

**Coverage grows off the live roster.** The M2b heatmap + a new `scoreCoverageAt` now read the player roster, not the hardcoded sats; `main.ts::buildRenderState()` folds the roster into a `BuildRenderState` each frame (X-02: scratch grows only on add, never per frame). Boot is a small starter (2 ground + 1 LEO ≈ 9% demand); a fully-built network reaches ~100%. Assets render as pooled signal-green markers; the orrery readout shows `COVERAGE <dim> · DEMAND NN% · Ngs/Nsat · €NNNN [OVERSPENT]`.

**Determinism.** A SEPARATE world from the M1 cache session → its OWN replay golden **`2503511112643458855n`** (`m2-build-replay.test.ts`: records 3 deploys + 4 launches incl. an engineered seed-failure, asserts same-log-twice + LIVE==REPLAY + JSON round-trip + the coverage-grows invariant). The **M1 golden `544847093270497462n` is untouched** (no M1 source changed). Purity enforced by `m2/purity.test.ts` (randomness only via the seeded PRNG). 369→393 tests; tsc clean; build clean. **Screenshot-verified**: built 6gs/4sat live, coverage 9%→76%, €‑3200 OVERSPENT (exact: 4×250 + 4×1800 − 5000; one GEO launch failed the seeded roll yet still charged — the §4.7 risk). M2d adds the contract revenue that pays for it.

**Minors (flagged, deferred):** stale % in the build notes (actual growth is stronger); `launch.ts` `altitudeKm` field actually holds the semi-major-axis radius (math correct, rename owed); constellation RAAN/m0 spread can wrap-collide for many same-preset launches (cosmetic); ground stations + demand are body-fixed in the no-rotation frame (inherited M2a/b simplification — geography is ecliptic-aligned, not true Earth rotation). Costs/EIRP/sites/starter are placeholders — tune later. Keyed/list deploy was chosen over globe-raycast placement (later polish).

### SD-28 — M2b: the coverage heatmap render (GDD §5 view #2 — the monument begins)
**Status: ACCEPTED (autonomous decision; M2b, the first VISIBLE M2 change).** GDD §5 view #2 is the coverage heatmap, and §1/§5 name the growing coverage web as the MONUMENT — the player's visible trophy (the make-or-break visualization pillar). M2b renders the M2a grid over Earth as a coloured coverage shell, computed LIVE from the system sats. Render-only (src/orrery + main.ts) — NOT wired into M1Session/replay, so the golden stays untouched.

**The overlay.** New `src/orrery/coverage-overlay.ts` (`CoverageOverlay`): a triangle mesh built ONCE in the ctor — one flat triangle per grid cell from the cell's 3 corner UNIT-VECTORS into a static f32 position buffer (320 cells → 960 verts), added to the scene at renderOrder 7 (under the body billboards). Per frame the orrery rebases Earth with the SAME `renderInto` path the bodies use (f64 m − focusAbs → log-compress → ecliptic→three) and calls `place(rebasedEarthPos, shellRadius)`: `position.copy` + `scale.setScalar(radius*1.012)` so the unit-sphere shell hugs Earth a hair above the surface; `shellRadius` mirrors `sizeBillboard` so the shell matches the Earth disc. The ONLY f64→f32 crossing is Earth's rebased position + the scalar radius; the grid geometry is pure static f32 — no metres ever reach the mesh.

**Live coverage + the §8 encoding.** Each frame (only when the overlay is toggled on) the 4 dataset sats (`sat_leo`/`sat_geo`/`sat_meo_inc`/`sat_meo_polar`) are read straight off the ephemeris into reused Vec3 scratch (no session involvement), then the static grid is swept with a NEW allocation-free pure helper `coverageDimsAt(cell, eirps, satPositions, earthCenter, earthRadius, out)` in `field.ts` — it reproduces `coverageOf`'s two gates (sin-elevation horizon mask + inverse-square link budget) writing INTO preallocated `CellCoverage` scratch (`coverageOf` allocates per cell, so the render path gets this faster twin; parity-pinned cell-for-cell in `field.test.ts`). `heatmap-color.ts` (pure, tested) maps each cell to a §8 hue ramp by the selected dimension: connectivity=cyan, bandwidth=green, latency=amber (LOWER latency = hotter, per §5); uncovered cells read as a dark desaturated wash bleeding to the 1-bit substrate (a visible HOLE in the web — what view #2 is for). Per the GDD §8 CVD hard-requirement the distinction is carried REDUNDANTLY in BRIGHTNESS (0.55→1.0) and OPACITY (uncovered 0.16; covered 0.42→0.92), not hue alone — colour-off it still reads as a brightness×opacity map. Chrome stays 1-bit (no css touched); only the coverage signal is coloured.

**Controls + perf.** Keys **H** toggle the shell (default OFF at boot, so the default view is unchanged until pressed); **D** cycles the dimension (connectivity→bandwidth→latency); the footer + the orrery readout show the active dimension. X-02 honoured: geometry built once, the RGBA colour BufferAttribute (DynamicDrawUsage) rewritten in place each frame, scratch reused — no geometry rebuild, no per-frame buffer alloc, only `needsUpdate` flagged.

**Determinism.** Render-only; `coverageDimsAt` (added to the pure `field.ts`) is imported ONLY by the orrery — nothing in the M1Session/save/replay path imports it. The replay golden is **UNCHANGED `544847093270497462n`**; `coverage/purity.test.ts` still passes (field.ts has no three/DOM/wall-clock/RNG); f64→f32 stays only in src/orrery. 355→369 tests; tsc clean; build clean. **Screenshot-verified** (OVERVIEW + ORBITS camera + H): the coverage web hugs Earth, cyan connectivity vs green bandwidth read distinctly across D, covered/uncovered legible, toggle-off clean.

**Tuning placeholders (flagged, not blocking).** (1) `CONNECTIVITY_HOT=3` makes the DEFAULT connectivity dimension read nearly flat at the current 4-sat geometry (tops out ~2) — lower to ~2 once more assets exist (covered-vs-uncovered is still legible via the opacity/brightness step; bandwidth/latency read as rich gradients). (2) Shell PROMINENCE is modest at the curated camera distances (per-preset log-compression keeps the scene compact); a dedicated Earth-coverage framing is later polish — the web grows + reads bigger as M2c adds placeable assets. (3) `coverageCellColor` returns a fresh `{r,g,b,a}` per cell (~320 tiny objects/frame ≈ 3.5KB young-gen churn) — negligible; a reused scratch is the trivial fix if ever needed. Colour ramps + warmth thresholds are placeholders — tune later.

### SD-27 — M2a: the geodesic coverage grid + coverage field (GDD §4.2 "The Heart")
**Status: ACCEPTED (autonomous decision; M2a is the first M2 ticket after the M1 gate passed).** GDD §4.2 models coverage as a field over a body surface: a geodesic cell grid (an "H3-like hex tiling"), each cell with demand + a coverage value derived from which assets have line-of-sight + adequate link budget, tracked across information dimensions (connectivity / bandwidth / latency / …). The implementation plan (~line 170) pins the build as **roll-your-own subdivided-icosahedron, NO H3 dependency**. M2a builds this as the PURE, STANDALONE truth layer the whole M2 slice (heatmap §5, contracts, escalation) stands on. M2b renders it; later tickets wire it into a session.

**The grid — triangular faces (not the Goldberg/hex dual).** New `src/sim/coverage/grid.ts` (`GeodesicGrid.build(level)`). We tile with the TRIANGULAR FACES of a subdivided icosahedron: base 20 faces, each subdivision splits every triangle into 4, so `cellCount(level) = 20·4^level` (level 0→20, 1→80, 2→320, 3→1280). Chosen over the hex/pent dual because the count is an exact closed form, every cell has exactly 3 edge neighbours (no pentagon special-casing), and it is the simplest thing satisfying §4.2 (the dual is a later heatmap-aesthetic refinement). Each `Cell` has a stable id, a UNIT-VECTOR centre (normalised centroid) + lat/lon, its 3 corner vertices, edge-adjacency `neighbors`, and a solid-angle `area` (L'Huilier). Built deterministically from fixed golden-ratio icosahedron constants + integer subdivision with a shared midpoint cache (no RNG); same level → byte-identical grid. **Default level 2 → 320 cells** (a one-place dial `DEFAULT_GRID_LEVEL`): enough to read demand gradients and keep the §5 heatmap legible without perf worry. Invariants pinned in `grid.test.ts`: count == formula, neighbour adjacency SYMMETRIC + degree-3 + no self-loops, areas SUM to ~4π (12.566 to 8 d.p. at every level 0–3), no degenerate cells, max/min area ratio < 2.

**Per-cell demand — a deterministic placeholder (no RNG).** New `src/sim/coverage/demand.ts` (`DemandField.build(grid)`). A procedural PROXY from FIXED constants (no `Math.random`, no SimRng): a northern-mid-latitude population band + a handful of fixed great-circle metro HOTSPOTS (Gaussian falloff in angular distance) + a small baseline so open ocean isn't exactly zero. Non-negative, byte-identical per grid, gradient-bearing (max ≫ min) so serving a high-demand cell is worth more. Documented as a placeholder to swap for a `data/` surface later; the `DemandField` shape stays.

**The coverage field — `coverageOf(cell, assets, t)` + a whole-grid sweep.** New `src/sim/coverage/field.ts`. Two gates per the GDD: (1) LINE OF SIGHT — the asset must be at ELEVATION ≥ `MIN_ELEVATION_RAD` (5°) above the cell's local horizon, measured against the cell's outward normal (= its centre unit vector). Because the horizon plane is tangent at the cell, this IS the body-self-occlusion test (a far-side cell sees the asset below its horizon ⇒ negative elevation ⇒ the body occludes it) — generalising the links.ts occlusion reasoning to a surface point rather than duplicating the segment-sphere solve, and reusing the ephemeris for all asset positions. (2) LINK BUDGET — a simplified inverse-square model (§4.3): received ∝ eirp·(ref/d)², closing at ≥1 (`REF_LINK_DISTANCE_M` = 50,000 km, beyond GEO). Assets: SATs via `ephemeris.position(id,t)`; GROUND stations pinned at lat/lon + a small antenna `altitudeM` (`DEFAULT_GROUND_ALTITUDE_M` = 200 km) — needed because a transmitter exactly on the surface sees every neighbour at/below its horizon and would cover nothing; the altitude raises the local horizon to a ~one-ring footprint, with over-the-horizon cells correctly failing the elevation gate. Returns the §4.2 dimensions per cell: CONNECTIVITY (covering-asset count), BANDWIDTH (summed capacity), LATENCY (min one-way d/c hop; Infinity when uncovered). (Observation + freshness are reserved for later tickets.) Pure function of (cell, assets, t). `field.test.ts` pins: a sat near-overhead its subsatellite cell covers it at >70° elevation; an antipodal cell is NOT covered (occluded); a ground station covers its own cell at ~90° but not an over-the-horizon one; the inverse-square + min-elevation gates behave independently (a huge-EIRP over-the-horizon link still fails); capacity is linear in EIRP; connectivity counts assets and bandwidth sums capacity.

**Multi-axis scoring stub (the §4.2 optimisation spine, promoted from M1).** New `src/sim/coverage/score.ts` (`scoreCoverage(grid, demand, assets, t)`). Sweeps `coverageOf` over every cell and rolls up DEMAND-WEIGHTED axes: the headline COVERED-DEMAND FRACTION ∈ [0,1] (the "does it work?" bar, §3a), plus demand-weighted bandwidth and demand-weighted mean / worst covered latency. Deliberately NO single scalar score (§4.2: competing axes, elegant trade-offs). Pure over grid+field. Tests: empty asset set → all-zero, adding assets → fraction in (0,1], monotonic in the asset set, pure.

**Purity + determinism (golden UNTOUCHED).** All four sources are pure src/sim (no three/DOM/wall-clock/RNG) — enforced by `purity.test.ts`, which scans the sources (via Vite `?raw` imports, so no `@types/node` needed) for forbidden patterns. The coverage group is fully STANDALONE: nothing in the M1/session/replay path imports it, so the replay golden is structurally untouched and verified still **`544847093270497462n`** (the isolated `m1-session-replay.test.ts` passes). 304→355 tests (+51); tsc clean; build clean. No render (M2b renders it). **Cell-count level, demand constants, link-budget knobs, ground altitude, min elevation are all sane placeholders — tune later.**

### SD-26 — E10c: the post-run PARSE + gate telemetry + the onboarding nudge (GDD §4.12 / §9)
**Status: ACCEPTED (autonomous gameplay decision per "make gameplay decisions yourself").** §3a names the mastery fun as *optimising against a record*; §4.12 specifies that record (the "combat log for information delivery"); §9 sharpens the M1 gate to "does the player finish a 30-min run wanting to look at what happened and do it BETTER?" E9 built the truthful event stream; E10c makes the parse — the floor/ceiling hinge — real and answers the gate.

**The pure summariser.** New `src/sim/m1/parse.ts` — `parseRun(log: EventLog, ctx: RunContext): RunParse`. It is a PURE READ of the E9 stream (no three/DOM/wall-clock/RNG, and crucially it NEVER re-derives "what happened" — every number folds straight out of the events the sim already emitted), so the parse can't disagree with the record (the §4.12 honesty precondition) and is deterministic (same log → same parse; `parse.test.ts` asserts a known sequence twice is deeply equal and the log is untouched). It produces: (1) PER-CONTRACT post-mortem — per feed, fresh/stale/miss/blackout SECONDS by integrating the edge-triggered `serve` transitions between t and the next, fetches/prefetches launched, evictions (eviction events keyed by datasetId → feed), a `blackedOut`/`servedThroughBlackout` pair, and the specific miss called out as prose ("went dark for the conjunction", "pre-staged and served through", "evicted N×", "served mostly STALE", …); (2) AGGREGATE `GateMetrics` (the M1-GATE telemetry) — fresh/stale/miss/blackout fractions, prefetches **timely vs wasted** (a leg is *timely* if a later fresh-via-cache serve for that feed converted it; *wasted* legs carry €) with the wasted €, the blackout-handling verdict `none|served_through|partial|went_dark` + a `blackoutHandled` flag, net € / final balance; (3) the HEADLINE efficiency + the gap.

**The heuristic achievable bound (NOT a solver — M2+ deferred).** The bound is a simple, defensible upper ESTIMATE: with `slotCapacity` (3) slots < `feeds` (5) you can hold at most `slots/feeds` of the feeds fresh at once, applied to the LINK-UP time only (blackout time is excluded — you cannot freshly fetch through a dead link, though a pre-stage can serve through). `efficiency = freshFraction / achievableFreshFraction` (clamped ≤1) and `freshGap = max(0, achievable − actual)` is the "do it better" hook. Labelled honestly `achievable (est.)` with a one-line rationale string — explicitly a heuristic, not a proven optimum (the solver-proven optimum is M2+).

**Reviewable at rest (§4.12 / §5 view #9).** New pure-DOM `src/panels/parse.ts` (`ParsePanel`, title "THE PARSE") renders the `RunParse`: §8 1-bit chrome housing (the `.telem`/`group`/`row` vocabulary), data on the SAME warmth ramp as log-format.ts (good/watch/warn/dead → green/cyan/amber/grey), HEADLINE → AGGREGATE → PER-CONTRACT groups + a "no run yet" placeholder. Surfaced as a 6th WM preset **PARSE** (key extended 1–5 → 1–6; THE PARSE in the wide left column with the live log + finance beside it) and a **G** toggle key; refreshed (forced) on PARSE-view entry and per-frame *only* while that preset is shown AND **only when the truthful log actually grew** (`refreshParse()` is dirty-checked on `session.events.appended`, so a PARSE view left open on a paused/quiet run costs nothing — this is the genuinely "reviewable at rest" guarantee, not a per-frame rebuild). `main.ts` builds the `RunContext` from `session.feeds`/`session.events`/balance + `SCENARIO.tick0`; the panel holds no sim state.

**The onboarding nudge (the E10b minor, resolved).** Two mechanisms: (1) scenario `defaultScaleIndex` (a new ScenarioConfig dial; `defaultScale()` resolves it against TIME_SCALES, clamped) — `main.ts` boots the LIVE clock at **1000×** (index 3) so a PASSIVE player who never touches the speed keys reaches the blackout ENTER ≈15.7 real-min in and dwells to ≈25.4 min, inside the ~30-min sitting; the contention strain is felt at any scale and the speed keys stay available to slow down for the light-gap (GDD Risk-6 — the waiting is decision-space). (2) A one-shot foreshadow NUDGE: the first frame the Sun-miss margin enters the watch band (`conjunctionApproach > 0`), `main.ts` appends a SYSTEM.LOG NOTICE via the legacy `SystemLog.append` (an orchestration line — keeps the TRUTHFUL sim event stream pure): "CONJUNCTION in Nd — blackout approaching — set time-accel ( , / . ) … pre-stage (P / A) …" — it POINTS at the controls, it does not move the clock.

**Determinism (golden unchanged).** The parse never mutates sim state (it `readAll()`s a copy), and the default-scale dial + nudge touch ONLY the live loop (the replay harness starts at t=0 with its own setup, boots `defaultPolicy()` and steps at fixed DT). So the replay golden is **UNCHANGED `544847093270497462n`** and all sibling determinism tests pass. 292→304 tests (9 parse + 3 scenario-dial); tsc clean; build clean; src/sim stays pure. Screenshot-verified THE PARSE (preset 6 / G, with a seek ≈15.435e6 s then play across the blackout for a real run summary) + the foreshadow nudge line. **Numbers/labels are placeholders — tune later; the solver-proven optimum is the M2+ upgrade of the heuristic bound.**

### SD-25 — E10b: the strain-tuned 30-min scenario (the full arc placed at a real conjunction)
**Status: ACCEPTED (autonomous gameplay decision per "make gameplay decisions yourself").** GDD §9's sharpened M1 gate needs a 30-min run that contains a full strain→relief arc AND ends with the player wanting to do it better — and its scenario note demands "enough simultaneous feeds that you feel it getting away from you." E7–E10a built the mechanisms (5-feed strain, prefetch-policy relief, truthful log, live conjunction blackout); E10b PLACES them on a timeline that fits one sitting. **The core tension resolved (GDD Risk-6):** light-delay (one-way ~900–1300+ s) wants a slow scale to be felt, but the conjunction (182 days from J2000 t=0) wants a fast scale to be reached — ~5 orders apart. Resolution: start the scenario near a real conjunction so the macro-timeline is short, and let the player bridge scales with the existing 1×–1000× controls, the waiting filled with prefetch decisions (not a slider you merely watch). New `src/sim/m1/scenario.ts` is the one-place dial: `SCENARIO.t0Seconds = 14_500_000` (tick0 = round(t0/DT) = 870,000,000), with `missionElapsedSeconds(simSeconds) = max(0, simSeconds − t0)`. The sim clock IS ephemeris time — `main.ts` boots `clock.setTick(SCENARIO.tick0)` so every position/LOS read uses the true J2000 epoch with zero translation; the ONLY presentation adaptation is the telemetry CLOCK row showing **MET** (mission-elapsed) so it reads 0 at boot, not ~168 days.

**The placement (computed against the real ephemeris):** t0 is ≈10.87 days before the corridor opens; conjunction at t≈15,731,438 s. Starting Sun-miss margin ≈15.93 Rsun (SAFE/green, well above the 9-Rsun watch edge), so the foreshadow is a CHANGE the player watches tighten. Earth↔Mars ≈2.589 AU at t0 → one-way light ≈1292 s (≈21.5 min, near the worst — Mars across the Sun — the dramatic light-delay teaching moment that coincides with the looming blackout). Real-time-to-blackout: 100× (default boot) ≈156 real-min, but the early first-fetch wait is ≈13 real-s so the strain is FELT; 1000× ≈15.7 real-min to ENTER, ≈25.4 to EXIT, with ≈9.7 real-min of dwell inside the 6.74-day corridor. The arc: green @100× (mash P, feel 5 feeds slip) → discover AUTO+BLK (a/[/]) → control → crank to 1000× → green→watch→warn→BLACKOUT → ride it out.

**Lead retune (fixes the SD-24/E10a minor):** `defaultPolicy().blackoutLeadS` 1200 → 1800 s. Max one-way light over the approach is ≈1305 s, so the old 1200 s lead launched a pre-stage that landed ≈100 s AFTER the gap opened. 1800 s exceeds the worst one-way by ≈495 s, so the earliest default-mode pre-stage lands ≈500 s BEFORE the gap — it genuinely beats the light-gap now (proven in `scenario.test.ts`: new lead beats the gap, old 1200 does not; a default-tuned freshness_blackout pre-stages ahead and a pre-staged feed serves through while an unprepared feed takes blackout_miss).

**Determinism:** epoch + lead change moved the replay golden `8072561960299808504n` → **`544847093270497462n`** (the fold change is exactly snap.policy.blackoutLeadS 1200→1800; the session boots `defaultPolicy()`). All 7 sibling determinism tests pass unchanged (same-log-twice, scale/frame-slicing independence, LIVE==REPLAY, competing-prefetch, mid-run toggle, JSON round-trip) and the E9 event-stream replay still holds. 277→292 tests; tsc clean; build clean; src/sim pure (scenario.ts imports only DT). Screenshot-verified: the CONJUNCTION readout foreshadow tightening (6.9 Rsun, half-gauge, 21m39s fetch ETAs) + the BLACKOUT band. **Open (→ E10c):** the default boot scale is 100×, so a passive player who never ramps to 1000× never reaches the blackout in a sitting — add a scenario default-scale dial or an onboarding nudge. (Also latent: the live action log records at the large absolute tick0; harmless today — no test replays a live-saved session.) **Epoch/lead/corridor values remain placeholders — tune later.**

### SD-24 — E10a: the solar-interference CORRIDOR makes the conjunction blackout live (retires the SD-22 gap)
**Status: ACCEPTED (autonomous gameplay decision per "make gameplay decisions yourself").** SD-22 self-flagged the single most important missing beat of M1: the GDD §4.4/§3a *marquee* transferable insight — "pre-stage the cache before the predictable conjunction blackout and you beat the light-gap" — was DORMANT in real play. The Earth↔Mars line of sight in this ephemeris never crosses the 1-Rsun solar disk (tightest Sun-miss **3.322 Rsun** over a full synodic period), so the disk-occlusion blackout never fired live and `freshness_blackout` was proven only against a fake ephemeris. E10a makes it live.

**The honest fix (geometry):** a real Mars solar conjunction is NOT a literal disk occultation — it is a comms blackout from solar RF interference at small Sun-Earth-probe angle (NASA stops commanding Mars craft at SEP ≲ 2°, degraded from ~2–5°). So the blackout is modelled as a **solar-interference CORRIDOR**: the Earth↔Mars link is dead when its LoS passes within `SOLAR_CORRIDOR_RSUN` solar radii of the Sun **centre** (not just inside the 1-Rsun disk). This **generalises** the existing occlusion check (N=1 ≡ the physical disk) — no duplication. New tunable `SOLAR_CORRIDOR_RSUN` (default **5**, one-place dial in `src/sim/links.ts`): with the tightest real approach at 3.322 Rsun, N=5 opens a genuine blackout window. `segmentSphere` gains a separate `blockRadius` (geometry distance stays the true miss; the corridor is the blocking radius); `lineOfSight` threads a `corridorRsun` defaulting to the constant; `earthMarsLos` returns `{corridorRsun, inCorridor}` alongside the existing `{marginSolarRadii, occulted}`. The "Sun strictly between the endpoints" guard (0<t<1) is preserved, so a near-side Sun never blacks out. The corridor is the minimum Sun-centre→**segment** distance (projection clamped to [0,1]).

**The live conjunction:** scanning the real eph, the conjunction is at **t ≈ 15,731,438 s** (≈182 days), min miss **3.3219666904 Rsun**; the default corridor opens a blackout window from t ≈ 15,439,238 to 16,021,888 — **≈582,650 s ≈ 6.74 days wide**. The whole pre-wired pathway lights up unchanged: `feasible()` → false in-corridor → resolver `blackout_miss` → economy SLA penalty rate (−€4/s) → BLACKOUT readout → blackout enter/exit events. New `src/sim/m1/conjunction-blackout.test.ts` proves it against the REAL eph: scans for the conjunction, asserts in-corridor infeasibility + `blackout_miss` (payout −500) + bracketing enter/exit events; and the **marquee payoff** — `freshness_blackout` pre-stages live (`feasible(now) && !feasible(t+lead)`, prefetch cause `prestage`) and a pre-staged feed (science, long half-life) **serves through** the blackout from cache while an un-staged feed takes the SLA hit. N=1 corridor is asserted to reduce EXACTLY to the disk occultation.

**Determinism:** the replay golden is **UNCHANGED `8072561960299808504n`** — and that is correct, not a skip: the action-driven replay window runs to t=2000 s where the min Sun-miss is ~156 Rsun (link wide open), so disk-vs-corridor feasibility is identical there and no resolve outcome moved (verified by sweep). 1×==N× fixed-dt replay still holds; a corridor-purity determinism test asserts stepping the same in-corridor instants twice is bit-identical (state + events) and `feasible()`/`earthMarsLos()` are pure of (eph, t).

**Render (the cue leads the event — §4.3a "blackouts are geometrically predictable"):** the CONJUNCTION readout bands are re-keyed off the live corridor threshold (`losCorridorRsun` added to `FrameState`; watch opens at `CONJUNCTION_WATCH_FACTOR×corridor`, full/alarm at/inside it). The gauge fills (cyan watch → amber warn) as the margin tightens and the label reads the live `N.N Rsun`; once the LoS enters the corridor the label reads **BLACKOUT** (red, full bar) — the same verdict the resolver reaches. The orrery titlebar lamp + readout blackout badge now key off `inCorridor` (the corridor), not the never-firing disk. `main.ts` stays thin (f64→f32 only in src/orrery); a DEV-only `window.seekSim(tSeconds)` hook (stripped from prod via `import.meta.env.DEV`) lets the screenshot harness jump to the conjunction (unreachable by wall-clock even at 1000×). Screenshot-verified: OVERVIEW + SYSTEM camera at t=15,731,438 shows the BLACKOUT readout + conjunction geometry; ~95 min before the window with AUTO+BLK shows the pre-stage relief firing. 261→277 tests; tsc clean; build clean; src/sim stays pure. **Corridor N and feed/rate values remain placeholders — tune later.**

### SD-22 — E8: the standing prefetch POLICY (the tame-it lever / the relief)
**Status: ACCEPTED (autonomous gameplay decision per "make gameplay decisions yourself").** E7 made the strain real; E8 is the GDD §3a *taming-to-functional* relief and the first rung of the §4.11 leverage curve — the unit of command rises from "asset" (hand-mashing 'P' per feed) to "declared intent" (a standing policy the system executes). New pure module `src/sim/m1/policy.ts`: `PrefetchMode` = manual | freshness | freshness_blackout; `PrefetchPolicy {mode, freshnessFloor (default 0.70), blackoutLeadS (1200), maxConcurrentAuto (3)}`; pure `selectAutoPrefetches(policy, feedStates, cache, eph, t)` → ordered feedId list (eligibility = link feasible AND no leg in flight; budget = maxConcurrentAuto − legs-already-in-flight so it self-rate-limits; "freshness" tops up feeds below the floor most-urgent-first; "freshness_blackout" *also* pre-stages a feed where feasible(now)&&!feasible(t+lead), with priority over routine top-ups; "manual" = []). It fires inside `M1Session.step()` step 2 (after arrivals land, before resolve), launching legs + charging €50 each. **Default mode is "manual" — default behaviour unchanged.**

**Determinism contract (the load-bearing decision):** AUTO-prefetches are a *pure function of (policy, state, geometry)* computed inside `step()`, so replay re-derives them with **no extra logging**. Only a *change* of policy is player intent → new `set_prefetch_policy` SimAction (`src/sim/action.ts`), applied via the SHARED `applySessionAction` so live ('A'/'['/']' keys at `clock.tick`) and replay set the policy at the SAME tick. Snapshot/restore round-trips the policy. Golden re-pinned `8387670477081443185n` → **`8072561960299808504n`** (folds the policy + the economy tune below); a new determinism test toggles the policy mid-run and asserts replay-bit-identical + LIVE==REPLAY. *(One honesty note in `session.ts`: the per-step accrual is DT-invariant, but the autopilot evaluates once per tick, so an autopilot-ON run is not bit-DT-invariant across different dt — the contract is fixed-dt replay, which IS bit-identical; comment corrected to say so.)*

**Economy tune (placeholder, flagged):** with the original numbers NO floor beat manual — a light-gap-crossing copy lands at freshness ~0.70–0.89, below Demand's 0.9 fresh cap, so a top-up never reached the paying FRESH band and only burned €50. Fix: added `freshFreshness` to FeedConfig and set it 0.75 (a just-arrived copy IS fresh), so a top-up converts STALE (+€2.5/s) → FRESH (+€5/s). Now there is a real interior optimum: floor sweep over 200k ticks — manual €9394; floors 0.05–0.60 *below* manual (wasted fetches); **0.70 peak €10355 (+€961, beats both extremes)**; 0.75–0.95 declining (over-prefetch/eviction churn). The 57% structural miss (3 slots < 5 feeds) is the E7 strain and is intentionally NOT tamed away — E8 relieves the *served* portion (fresh-time 11.8%→15.6%). **Numbers are placeholders — tune later.**

**Render (the §5 relief made visible):** PREFETCH readout row (MANUAL dim vs AUTO@70% / AUTO+BLK, cyan armed → green the step it fires), status-strip PREFETCH cell, a subtle "prestage" audio cue, and the existing per-feed roster + finance NET visibly stabilising. Screenshot-verified: manual ↔ AUTO+BLK@70%, autopilot launches top-ups on screen, balance climbs.

**Known gap → RESOLVED in SD-24 (E10a):** the solar-interference corridor made the conjunction blackout live against the real ephemeris; `freshness_blackout` now pre-stages and serves through a real conjunction. The original gap text is retained below for the record. ~~`freshness_blackout` — the GDD §4.4/§3a *marquee* transferable insight ("pre-stage before the predictable conjunction blackout and you beat the light-gap") — is **dormant in real play**: the Earth-Mars LoS in this ephemeris never crosses the 1-Rsun solar disk (tightest 3.322 Rsun over a full synodic period), so the pre-stage never fires live; its logic + economics are proven against a fake-eph unit test only. **E10 must make a conjunction blackout live-exercisable** — most likely by modelling the blackout as a *solar-interference corridor* (LoS within ~N Rsun of the Sun, N≈3–5, which is the physically-honest comms-blackout criterion — real Mars conjunction is small-angular-separation RF noise, not literal disk occultation) rather than a hard 1-Rsun occultation, OR by choosing a scenario epoch where the disk is actually crossed. Without it, M1's single most important "real insight" beat is missing.~~

### SD-21 — E7: single feed → several feeds + multi-slot cache (the strain becomes real)
**Status: ACCEPTED (autonomous gameplay decision per "make gameplay decisions yourself").** The single Mars feed was the *floor*; GDD §9 demands "enough simultaneous feeds that you feel it getting away from you, so the tame-it lever has something to tame." So M1 now runs **5 distinct feeds** (data-driven in the new `src/sim/m1/feeds.ts` — `FEED_CONFIGS`, designer-editable): mars_imagery/weather/telemetry/science/comms, each its own half-life (1800–5400 s), base price (€500–1200), and min-acceptable (0.4–0.6), decaying on independent clocks. The cache is **3 slots** (`CACHE_SLOTS=3 < 5 feeds` — the strain is structural: you provably cannot hold all five fresh). `Cache` generalised to a `datasetId`-keyed multi-slot store (capacity ctor arg, default 1 keeps the single-slot resolver/economy/m1-model tests faithful); on a full store it **evicts the lowest *current* freshness** (judged at real store-time `t`, so a just-arrived ~0.84 copy compares fairly), ties break first-inserted — deterministic. `M1Session` rewritten to drive `Demand[]` + the shared cache + a per-feed `FetchState` (one in-flight leg each); arrivals land before resolve, revenue **sums** over serving bands, **opex scales with occupied slots** (`OPEX_RATE × max(1, occupied) × coherence` — full 3-slot = €6/s at EVENTUAL, idle still burns a one-slot floor so there is no free idle). Manual prefetch ('P') now targets the **most-urgent eligible feed** (link up, no leg in flight, lowest current freshness; empty slot reads 0 → most urgent). Render adapts: orrery draws one cyan crawler per in-flight feed (small lateral fan) + Mars-node saturation = peak cache freshness; a per-feed Mini-Metro roster (glyph ◆fresh ◇stale ▸fetch ○miss ▰blackout + freshness bar) + SLOTS N/3; finance sums revenue/opex/net and FRESHNESS PREMIUM across feeds + CACHE SLOTS N/3; telemetry a compact "N/5 hit · N fetch · N blk" digest. **Replay golden re-pinned to `8387670477081443185n`** (fold now covers every feed's fetch state + every cache slot). 222 tests green; determinism (1×==1000×, snapshot round-trip, LIVE==REPLAY) holds over the full roster; purity intact. Screenshot-verified: the 3/5-served strain reads at a glance on OVERVIEW + OPS. **Note (design-correct):** the realistic ceiling on the shipped 5-feed roster is below the idealised +€9/s — you can't hold all five, held copies decay — which IS the intended strain (E8's prefetch *policy* is the relief). **Feed/rate values are placeholders — tune later.**

---

## Resolved engineering decisions (from scoping)

| ID | Decision | Status | Rationale |
|---|---|---|---|
| D1 | **Pin stack versions** | PROPOSED | Node 26 + Three.js 0.184.x + Vite 8.x. Pin in README + CI. |
| D2 | **Sim-core language** | ACCEPTED | TypeScript (SD-3). Pure `src/sim/` with zero DOM/Three.js imports. Bit-identical to C#. |
| D3 | **Test framework** | ACCEPTED | Vitest for pure sim; Playwright for headful screenshot CI. |
| D4 | **RNG portability** | ACCEPTED | bigint splitmix64 in `src/sim/rng.ts` (P0-04). Golden values cross-verified bit-identical against C# `SimRng`. bigint arithmetic is spec-defined, portable across all JS engines. |
| D5 | **Fixed sim `DT`** | ACCEPTED | `DT = 1/60` s. Time-accel scales *how many fixed steps run*, never `DT`. Integer tick accumulator (P0-03). |
| D6 | **Project rename** | SUPERSEDED | Was `GalaxyLink` → `SignalHorizon`. Done in the Godot repo; the TS repo starts as Signal Horizon. |
| D7 | **Epoch / reference frame** | PROPOSED | **J2000 + ecliptic-of-J2000.** Convert any equatorial-sourced data on load. |

---

## Resolved open technical questions

| ID | Question | Resolution | Status |
|---|---|---|---|
| Q1 | SGP4 vs Kepler for Earth-orbit assets | **Kepler now, SGP4 deferred indefinitely.** Orbital decay is a cheap parametric pressure, not SGP4. | ACCEPTED |
| Q2 | Routing solver for the time-varying graph | **Dijkstra/A\* is ample through M2.** Re-solve on topology-change events only. Spike heavier solvers at M4. | ACCEPTED |
| Q3 | Hex grid: H3 binding vs roll-your-own | **Roll your own** subdivided-icosahedron geodesic grid; no native dependency. | ACCEPTED |
| Q5 | Time-compression ratio | A *feel* parameter — keep it a `data/` tunable. Determinism safe at any ratio. | ACCEPTED |

---

## UI shell decisions

### DD-8 — The Ops Console is a tiling window manager with layout presets
**Status: ACCEPTED.** Direction **A · TILE**, dynamic (Sway/i3-style). Non-overlapping tiles always fill the screen; player splits/swaps/resizes/zooms tiles and switches between named layout presets. 1-bit window chrome, always-visible status strip. *Supersedes* floating-window (B · DESKTOP).

### DD-9 — Crisp, resolution-independent scaling
**Status: ACCEPTED.** In the web stack: CSS `image-rendering: pixelated`, `-webkit-font-smoothing: none`, viewport-driven scaling. The 2560×1440 design-density reference still applies as a CSS baseline.

### DD-10 — Tiling WM model (constrained zone-grid)
**Status: ACCEPTED.** 1–3 cols × 1–3 rows, one-panel-or-tab-group per zone, mouse-driven zone-snapping, presets-as-data, always-tiled invariant. Full spec in `tiling-wm-spec.md`.

### DD-15 — Orrery is 3D + body-anchored orbit camera + curated preset framings
**Status: ACCEPTED.** 3D is non-negotiable (orbital planes must read). Body-anchored orbit camera with rotate/zoom/reset. Curated presets (CISLUNAR/ORBITS/SYSTEM/TOP-DOWN) with animated transitions.

---

## Decisions still owed

- **At M0-02:** Kepler accuracy tolerance target — add as **D8 when reached**.
- **M3–M4:** default "hardcore" level — is the player's *own* network awareness delayed, or only in-fiction data products?
- **Before M6:** does the optional currency flip read as climax or confusion?

---

## Superseded decisions

| ID | Old decision | Superseded by | Why |
|---|---|---|---|
| DD-11 | C# on Godot .NET | **SD-10** (TS/Three.js/browser) | Spike proved web stack is at least as natural with faster iteration and no fidelity loss. |
| DD-12 | Godogen frame-grounded loop | **Retained in spirit** | Judge from rendered output, never from "it compiled." Replaced by Playwright screenshot CI. |
| DD-13 | Develop on `main`, auto-push | **Retained** | Same policy, new repo. |
| DD-14 | Build C# frontend fresh from v3 mockups | **SD-10** | Frontend is now TypeScript/DOM, built fresh from the spike + v3 mockups. Design target unchanged. |
| (old) SD-2 | Defer the Tauri native shell | **SD-2** (no Tauri; browser is the platform) | Owner decision: skip Tauri, keep it in the browser. Removes WebKitGTK gate. |
| SD-4 | Canonical dataset by symlink (Vite `fs.allow` widened) | **SD-12** (vendored real file in-repo) | Symlink + absolute `server.fs.allow` paths broke cloning/running on other machines; vendoring makes the repo self-contained. |

---

*Append new decisions at the bottom of the relevant section; never silently overwrite — mark SUPERSEDED and add the replacement.*

### SD-41 — Docs consolidation (16 → 8 .md files; GDD untouched)
**Status: ACCEPTED (user: "consolidate the docs, don't touch the GDD").** The `docs/` tree had accreted 16 markdown files with overlapping scope, stale historical docs, and 4 M1 design docs (mechanics / build design / acts 3-4 / onboarding) that were each cross-referenced by source `@see` comments. Consolidated to 8:

- **Merged 4 → 1:** `signal-horizon-m1-mechanics.md` + `signal-horizon-m1-design.md` + `signal-horizon-m1-act3-act4-design.md` + `signal-horizon-m1-onboarding.md` → **`signal-horizon-m1.md`** (4 parts: Part I mechanics, Part II build design, Part III acts 3-4 design, Part IV onboarding script). §numbering within each Part preserved so the 17 `@see` references in `src/sim/net/*.ts` resolve (rewritten `signal-horizon-m1-{name}.md` → `signal-horizon-m1.md Part {I-IV}`; no code/imports/behavior touched).
- **Deleted 5 historical:** `act1-rotating-earth-design.md` (superseded by SD-40 / Part II), `signal-horizon-scoping-v0.1.md` (superseded by implementation-plan v0.2), `m0-acceptance.md` (M0 complete), `m1-spec-audit.md` (audit remediated P0-P4), `ui-critique-punchlist.md` (resolved by v3 mockups). Git history preserves them; their content is captured in `decisions.md` or the merged doc.
- **Kept as-is:** `signal-horizon-gdd.md` (untouched per instruction), `signal-horizon-implementation-plan.md`, `backlog.md`, `decisions.md`, `sim-render-contract.md`, `tiling-wm-spec.md`, `README.md`.
- **Reference fixes:** 9 stale doc-name references in `backlog.md` / `decisions.md` / `tiling-wm-spec.md` repointed at the merged doc (Part I-IV) or noted "(deleted, historical)"; `AGENTS.md` §1 + §4 and `docs/README.md` updated to the new doc set + precedence.
- **Verified:** 697 tests pass; `tsc --noEmit` clean; `vite build` clean; zero broken doc references remain in source or living docs. The merged doc's own `> *Source: <old-name>*` attribution lines record provenance and are intentionally kept.


### SD-42 — Player-POV attack on the GDD (`docs/signal-horizon-player-attack.md`)
**Status: ACCEPTED (user: "adversarial research of my gamedesign philosophy first, READ ONLY, NO COMMANDS RUN" → then "attack the GDD from the player's point of view … reconcile the two contentious views from the player's perspective, verify the GDD's claims through analytical/paper assessment, and propose adjustments. The agent only designs and writes the document; the user will handle actual playtesting themselves").** A read-only design critique living at `docs/signal-horizon-player-attack.md` (336 lines, 21 sections + appendix). NOT a GDD edit — the GDD is the user's domain (AGENTS.md §1). NOT a playtest — the user handles empirical validation. It is a *hypothesis pressure-test*: the strongest case a player-advocate can mount *before* a human sits down.

**Scope.** Walks the entire GDD (§0 through §10 + version footer). For each section: states the claim as the player would encounter it, challenges the confirmation-bias-prone framing, verifies against the GDD's *own* cited precedents (*Factorio*, *DSP*, *OpenTTD*, *Mini Metro*, *KSP*, *EVE*, *Paperclips*, *Frostpunk*, *Stellaris*, *Per Aspera*, *Duskers*/*Uplink*, *Zachtronics*, *WoW* combat logs, *Aurora 4X*) and named design theory (MDA framework — Hunicke/LeBlanc/Zubek 2004; Koster's *Theory of Fun*; Ware on perception), reconciles the designer's view with the player's view, and proposes a concrete adjustment. The spine is the prior critique's finding #10, restated from the player's seat: **the GDD is a designer's document, sophisticated and self-consistent, and it has never met a player.** A coherent hypothesis is not a validated theory.

**The six confirmation-bias load-bearing claims identified (ranked).** (1) §3a fun claim (taming→optimising, same verb, parse-hinged) — most elegant, most unfalsifiable; measure unprompted specificity of "what I'd do differently." (2) §3b endogeneity claim (escalation self-generated) — true in sim, authored in experience (the scenario beat table gates `escalationOn`); measure player attribution. (3) §4.11 discovery claim (capability discovered, not purchased) — unfalsifiable as stated ("nudged discovery" is instruction); measure unprompted discovery. (4) §4.12 novice-floor claim (parse is dark matter for novice) — asserted against the genre grain (*Dwarf Fortress*/*Aurora* records are load-bearing); track non-parse-opening testers. (5) §4.10 mastery-preservation claim (the flip doesn't rug-pull mastery) — unaddressed by the four commitments (they protect money, not skill); test whether a master carrier feels advantaged or reset. (6) §4.3a same-verb claim for topology (Level 2 same verb at higher altitude) — unverifiable until M4.

**Eighteen adjustments proposed** (collected in the document's appendix, ranked by bias risk), ranging from: make the §3a/§4.11 claims falsifiable via behavioural gate measures (Adjustments 3, 11); test the novice-doesn't-need-the-parse claim (12); decouple escalation engine from reveal in one playtest condition (4); add a separate light-delay-only playtest so the connectivity gate doesn't stand in for the signature-mechanic gate (8); add a fifth §4.10 commitment that mastery stays load-bearing through the flip (10); promote escalation to co-make-or-break with visualisation (2); add "autonomy working" to the must-be-visible list (9); to framing fixes (1, 5, 6, 14, 15) and process discipline (13, 17, 19). The strongest recommendation is the one the GDD already makes and the project has not yet followed: **run the M1 gate before further M2+/M3+ work** (Adjustment 17 — restates DD-7/SD-40 gate discipline from the player's seat). The meta-adjustment (0): tag player-experience claims `[CLAIMED]` until playtest confirms them `[OBSERVED]`, making the bias surface visible without rewriting the GDD.

**Fences honoured.** Design + write only. No GDD modification, no code, no playtests run. All measurement proposals are for the user's gate runs. External-precedent reasoning marked `[INFERENCE]`; named-theory citations marked `[THEORY]`; everything else grounded in the GDD's own text or the M1 mechanics spec.

**Consequence for the build:** none directly (read-only doc). Consequence for the *gate*: the document supplies the falsifiable forms the user should measure at the M1 gate, ranked by how confirmation-bias-prone each claim is. The backlog's M1-GATE ticket is the place these land if the user adopts them.

### SD-43 — M1 play-readability remediation (the connectivity opening, by hand in the browser)

**Status: ACCEPTED (user, after a live Playwright-MCP play session: nine concrete complaints — coverage flicker, "no clear goals / no contracts view", "still focusing on caching — confusing at the start", flat alt/inc/phase/raan loop, hard onboarding, black SYSTEM/TOP-DOWN views in PLAY, "where is the moon, the sun even?", and "wireframe isn't enough info to aim — show continents"). Worked autonomously; verified every fix in the live `vite dev` build via the Playwright MCP browser (per [[verify-the-dev-build-not-just-prod]]).** All fixes are net-mode-scoped; `?mode=cache` stays byte-identical, and the three replay goldens are untouched.

A parallel diagnosis workflow (6 read-only investigators) root-caused each issue to file:line before any edit. The fixes:

1. **Caching leak out of the connectivity opening.** Boot instantiated three sessions (cache `M1Session`, M2 `BuildSession`, `NetSession`); in net mode the chrome still rendered the *cache* session — `mars_imagery serving MISS` / `fetch launched EARTH→MARS` in SYSTEM.LOG, `FRESHNESS PREMIUM`/`CACHE SLOTS` in FINANCE, the `E→M` status cell, the Earth→Mars packet crawl, the cache-framed conjunction nudge, and a `P cache` key hint — while the playable game was NET·LAUNCH with a *different* wallet. Fix: `tickSim` early-returns after `netSession.step` in net mode (cache/M2/Mission stay constructed-but-inert); the cache log render + packet provider + conjunction nudge are gated to `!netMode`; FINANCE (`src/panels/finance.ts`) + STATUS (`src/panels/status.ts`) take a `netMode` flag and read a new `FrameState.netEconomy` (wallet / earned / revenue-rate / sat + contract counts) instead of the cache `demand` block.
2. **Black SYSTEM / TOP-DOWN camera views.** Both presets `focus:"sun"`, which net mode deliberately hides, and carried heliocentric log-fold params that compress the toy globe to sub-pixel → a black frame. Fix: `CameraPreset` gains an optional `net` override (Earth-orbit fold + distance) applied only in net render mode; `setPreset` re-points focus to the operated body in net mode. SYSTEM → a pulled-back cislunar overview; TOP-DOWN → a north-pole orbital-plane view.
3. **Coverage flicker.** The net surface discs (region / per-sat footprints / coverage-gap) shared `renderOrder` with `depthTest:false`, so the transparent z-tiebreaker flipped which painted on top every frame as the floating-origin rebase jittered their near-equal view-space z. Fix: `buildSurfaceDisc` now `depthTest:true` + `depthWrite:true` + `polygonOffset`, and `orientSurfaceDisc` takes a per-class ascending `liftMul` (region < footprint < draft < gap), so co-located patches land at strictly distinct depths — a deterministic, flip-free stack that also occludes far-side patches correctly. Pure render; no golden impact.
4. **Goals surface.** An `OBJECTIVE · ACT N` group atop the planner, driven by `netSession.cursor` from a render-only per-act table mirroring the m1-doc curriculum (no new mechanics). Verified auto-advancing ACT 1 → ACT 2 on completing the loop.
5. **Contracts view.** A `CONTRACTS` card list in the planner: every live contract with its ENFORCED SLA terms (gated by `activeAxes`), reward/hr, live served% + term progress, and an inline ACCEPT (the accept path generalised to any offered contract id; the headless `[data-net="accept"]` selector preserved).
6. **Moon + lit Earth.** Net mode un-hides the Moon (the terminator was already Sun-driven). The toy Earth (300 km radius) vs the real Moon distance (384 000 km) is a ~1280× gap that makes an honest combined frame impossible (Moon off-screen, or Earth sub-pixel), so the Moon is **stylized**: pulled to a fixed multiple of the Earth scene-radius, projected onto the camera's screen plane so it always reads as a companion body beside Earth. Non-interactive ambiance/scale only (cislunar mechanics remain M3).
7. **Continents.** New `src/orrery/coastlines.ts` (Natural Earth 110m physical coastline, public domain — 134 polylines / 5 128 verts) drawn as a `LineSegments` child of the graticule, in the same body-fixed basis as `surfacePointRelative`, so it rides the Earth-spin transform and aligns with where regions/footprints land — the player aims at real landmasses. `depthTest` hides the far hemisphere.
8. **Onboarding.** The Act-1 cold-open card cut from a 3-imperative lecture to a one-line welcome pointing at the (now persistent) OBJECTIVE + globe; the act2–4 cards remain just-in-time concept intros.
9. **Launch loop legibility + the gentle-opener fix.** Per-axis EFFECT captions under each slider (altitude/inclination taught first; phase/RAAN marked "fine"); a coverage-aware planner hint (invites experimentation when the pre-aimed default already covers, rather than implying an absent gap). The 5% launch-failure roll is **suppressed during Act 1** (`scenarioCursor === 0`) per the doc's "place one thing and it works" — done golden-stably: the roll is still *drawn* (RNG draw count unchanged, downstream rolls byte-identical), only the *outcome* is overridden to success; the net-replay golden was unaffected (the canonical Act-1 launch already cleared the roll). Failures arm from Act 2 on.

**Design calls made (recommendation-led, per [[plain-decisions]] / [[gameplay-first-not-screensaver]]):** kept the documented gentle Act-1 opener and made the sliders *legible* rather than forcing a closeable gap; suppressed launch failure only in Act 1. Both flagged to the user as reversible.

**Gate:** `npm test` (702 pass) · `npx tsc --noEmit` clean · `npm run build` OK. Files: `src/main.ts`, `src/types.ts`, `src/orrery/orrery.ts`, `src/orrery/coastlines.ts` (+ test), `src/panels/{finance,status,net-planner}.ts`, `src/style.css`, `src/sim/net/session.ts`, `src/sim/net-replay.test.ts`.

**Revision (same day, after a second live play session).** Two of the above needed a different approach:
- **Flicker (#3) — the first fix BACKFIRED.** Adding `depthWrite:true` + `polygonOffset` to the surface discs traded the z-tiebreaker flip for outright Z-FIGHTING: this scene's near/far range (0.001 .. 100000) gives such poor depth-buffer precision that the tiny per-class lifts could not be resolved, so the user reported it "flickers like crazy." Final fix: NO depth comparison at all (`depthTest:false`, `depthWrite:false`) — ordering is decided SOLELY by renderOrder, and every co-located disc class + every footprint slot gets a DISTINCT renderOrder (region 6.2 · footprints 6.50+slot·0.02 · draft 7.0 · gap-dark 7.5 · gap-covered 8.0), so the z-tiebreaker is never consulted. Verified flip-free in the browser (green-coverage pixel count stable ±1% across 10 consecutive frames). Trade-off accepted: far-side patches paint over the globe (not visible in the close planner view).
- **Moon (#6) — the stylized offset was wrong.** Pulling the Moon to a fixed screen-plane offset made its WORLD position depend on the camera, so it slid around unnaturally as the view moved (user: "the moon position is insane"). Reverted to the Moon's HONEST rebased position: it sits at its real cislunar distance — off-frame in the tight EARTH/PLAY close-up (correct: you're zoomed into LEO/GEO) and in-frame when you pull back to the SYSTEM/CISLUNAR overview. No faking.

### SD-44 — Net-mode UX is "dogshit": full mission-control redesign across virtual desktops (IN PROGRESS)

**Status: IN PROGRESS (user, after live play: "the WHOLE UX needs a complete redesign — i LIKE the tiling WM, i LIKE the style, but the GAMEPLAY is dogshit"; "the launch has to be a SEPARATE menu from contracts, EXTREMELY CLEAR what you're launching and where — i bruteforced the satellites because the view is so poor"; "imagine you're mission control … clear information pages on what is where and how it works, telemetry … business / routing / connectivity dashboards"; "we're not using the virtual desktops to their full potential").** KEEP: the tiling WM (DD-10) + the DD-1 art direction. REDESIGN: how the net game plays + reads. North-star: **MISSION CONTROL — multiple focused dashboards across the WM virtual desktops (presets), never one crammed panel.** Distinct desktops (BUSINESS / ROUTING / CONNECTIVITY + an overview + "how it works"/telemetry reference), the LAUNCH as its OWN clear menu (what + where), contracts decoupled into the business dashboard. Render/orchestration only; mechanics + the 3 goldens untouched; M1-scoped + progressive. The current 3-net-preset set (PLAY/MAP/REVIEW) is underused and will be reshaped toward the DD-10 5-desktop intent. Design plan being produced by a redesign workflow; build phased, highest-impact first (the launch↔contracts split).

**PHASE 1 — DONE (5 desktops + launch↔contracts split + a clean control scheme).** The net WM preset set went 3→5 **virtual desktops** (keys 1–5), each one mission-control page for one concern, booting on OVERVIEW:
- **1 OVERVIEW** (triage) — orrery + a new STATUS·BOARD (the objective + per-contract state) + FINANCE.
- **2 CONNECTIVITY** — orrery (planner framing) + the split **NET·LAUNCH** menu + COVERAGE·ROSTER. The launch tile now leads with a two-line WHAT/WHERE headline ("LAUNCHING: GEO PARK · 1× sat · €1,735" / "OVER: equatorial metro · footprint 100% · WILL SERVE ✓") — the fix for "I bruteforced the satellites because the view is so poor."
- **3 ROUTING** — orrery (path-map framing) + LINK·LOAD + the PREFER tune (both read "no traffic to route yet" until Act 2/3).
- **4 BUSINESS** — the CONTRACTS board (with inline ACCEPT, fully decoupled from launch) + FINANCE + SYSTEM.LOG ledger. No globe.
- **5 REFERENCE** — HOW-IT-WORKS explainer cards + SYSTEM.LOG + FINANCE. (telemetry + the PARSE are NOT mounted in net mode — both still read the cache session and would leak the deferred caching game; net-aware versions are later-phase.)
The single 7-group net-planner was split into **net-launch / net-contracts / net-prefer** (cache mode keeps the original NetPlanner). New thin panels: status-board, coverage-roster, link-load, howto — all pure re-projections of existing NetSession state. The planner-overlay gate re-pointed `net-planner`→`net-launch`. Render/orchestration only; the 3 goldens + cache mode untouched; every `[data-net]` selector preserved.

**CONTROLS — fresh pass (the "crazy amount of shortcuts, some don't work" fix).** Net mode now has its own minimal, collision-free keymap and RETURNS early (so no cache-era key fires): `1–5` desktop · `0` reset layout · `Space` pause · `,`/`.` speed · `↑↓` alt · `←→` inc · `[ ]` phase · `L` launch · `R` reset-cam. ACCEPT / CONSTELLATION / PREFER are panel buttons now (not global keys), which kills the old **C** camera/constellation collision; E/C/O/S/T (the desktop sets the camera), F/P/H/D/B/M/'/N/J/A/; are all cut in net mode. The status-strip legend + the orrery on-canvas hint were rewritten to show only the working set. Cache-mode keymap unchanged.

Verified live in the browser (all 5 desktops render + switch on 1–5; launch headline reads what/where; contracts+ACCEPT on BUSINESS; `plannerActive` fires on CONNECTIVITY; cut keys are inert; no cache leak on any net desktop). Gate: tsc clean · smoke OK · 702 tests green. PHASES 2–5 (flesh out the triage/roster/link-load/howto dashboards) remain.

**PHASE 1b — DONE: the REAL flicker root-cause + OVERVIEW hero framing (2026-06-17, after "earth is so small on the overview, flicker is still there").**
- **THE LIMB FLICKER (root-caused at last).** Prior passes chased the coverage discs (depthTest:false stack) and the ground-track/beam (52ab989) — but the actual culprit was the **graticule built on the UNIT sphere (R 1.0), exactly COPLANAR with the SphereGeometry(1) fill**, both `depthWrite:true`. The two z-fought, and the per-frame floating-origin rebase (focusAbs advances with sim time ⇒ different f32 rounding) flipped the depth winner every frame — a high-frequency shimmer concentrated at the GLOBE LIMB (steep depth gradient + high globe↔black contrast). Compounded by the camera's absurd near/far (`0.001 .. 100000`) starving depth precision at the globe. **Fix (two scoped, render-only changes):** (1) lift the graticule to R 1.0025 (just under the coastlines at 1.004) so it can never be coplanar; (2) in net mode pull the camera NEAR plane up to `0.5·(dist − sceneRadius)` — everything else (log-folded Moon/Mars/markers) sits toward the origin i.e. farther, so only NEAR moves, FAR stays wide ⇒ ~1000× better precision, zero clip risk; restored to the wide default off-mode (cache framing byte-unchanged). **Verified with a 60fps `gl.readPixels` toggle-scan: limb avgToggles 0.26 (was a per-frame flip), max 5 over 40 frames — the residual frame-to-frame change is smooth rotation, not flicker.**
- **OVERVIEW Earth too small (#14).** The toy globe's scene radius is tiny (de-squashed+log-folded), so the bare EARTH preset framed it at **~3px** (`sphereHeightFraction` 0.004); only the planner's sphere-FILL dolly made it legible, and that was gated on `plannerActive`. Generalised the close-up into a per-desktop **hero framing** (`Orrery.setNetHeroFraming(fill)`): OVERVIEW frames Earth at 0.34 of pane height, CONNECTIVITY at 0.42, ROUTING stays pulled back (0). OVERVIEW now reads 0.331 (137px radius) — a clear central hero. Glides in/out via the same smoothing the planner uses. Render-only; cache mode untouched. Gate: tsc · smoke · build · 702 tests green.

**PHASE 2h — FLEET: COVERAGE·ROSTER is a real fleet view (2026-06-17).** The roster's first version showed only id + LEO/GEO + covers. Added a per-sat orbit sub-line — altitude (km) + inclination (°) + an "· idle" marker when the sat bridges nothing — so the player can manage the constellation: which shell each sat is in, how far off the equator it reaches, and which sats are idle (over-build / wasted capex, tying back to the constellation-size decision). New `altKm`/`incDeg` on the roster sat (pure reads of `orbit.aM`/`incRad`); render-only, goldens untouched. Verified live ("NET-SAT-0 · GEO · 535 km · 0° incl", idle LEOs marked).

**PHASE 2g — REFERENCE: HOW-IT-WORKS teaches the depth (2026-06-17).** The REFERENCE desktop's explainer cards were generic; rewrote the five act cards so the reference page actually teaches the decision surfaces this session added — the contract as a WAGER (reward vs penalty, can-my-fleet-hold-it), the constellation SIZE held-vs-capex ladder (trim to the minimum), the allocation ledger + who STARVES + the PREFER reroute, fault redundancy, and the Mars frontier — each with the desktop + control that acts on it. Keyed live/locked by the scenario cursor (verified: the Act-2 card reads LIVE, later acts LOCKED). Pure copy; goldens untouched.

**PHASE 2f — FLOW: deep-link the OVERVIEW triage to the fix (2026-06-17).** Tightened the mission-control loop — every demand row on the OVERVIEW status-board is now CLICKABLE and jumps to the desktop where its fix lives: an OFFERED deal → BUSINESS (accept), an over-cap bandwidth breach → ROUTING (reroute), any other unserved demand → CONNECTIVITY (launch/aim/constellation); served/terminal demands aren't clickable. A cyan left-border + hover tint marks the affordance; the row title reads "→ BUSINESS" etc. `StatusBoard` gains an `onNavigate(desktop)` callback main.ts maps to `setWmPreset` by preset name. Verified live (offered REGION-0 row → click → lands on BUSINESS). Render-only (CSS + DOM + a WM-preset callback); goldens untouched. Glance the problem → click → act.

**PHASE 2e — POLISH: 1-bit styling for the new decision surfaces (2026-06-17).** The new depth surfaces had rendered unstyled. DD-1 1-bit-chrome CSS added for: the CONSTELLATION SIZE stepper (compact −/+ around a cyan N, right-aligned), the LINK·LOAD allocation ledger (dim, red when a contract starves), the OVERVIEW status SUMMARY (bold, divider, amber/green tones), the price-the-bet verdict tones (amber would-breach / green holds). Restructured the status-row so the at-risk REASON is a full-width sub-line ("↳ no path — launch over it") instead of overflowing the tight served column. Verified live: compact 22px stepper buttons, zero horizontal panel overflow on any desktop. CSS + a DOM restructure (render-only); goldens untouched.

**PHASE 2d — TRIAGE: the OVERVIEW status-board is a real mission-control glance (2026-06-17).** With the three decision levers built, made the OVERVIEW triage board answer "is anything wrong?" at a glance. Added a network-health SUMMARY line ("FLEET 5 sats · SERVED 2/2 · +€21,600/hr · €13,475", amber + "N AT RISK" when an active demand is unserved) and a per-demand at-risk REASON (the router's binding axis → a fix phrase: connectivity "no path — launch over it", availability "coverage gaps — add a phased sat", latency "too slow — lower the orbit", bandwidth "over-cap — reroute or add capacity"). This closes the loop with the other threads: accept the bet the price-the-bet card warned against and the board shows the penalty BLEEDING (verified live: "1 AT RISK · +€-5,400/hr", row "UNSERVED · no path — launch over it · €-2"). New render-state fields `fleet{satCount,revenuePerHr}` + `NetContractRow.bindingReason`, both pure per-frame reads — goldens untouched.

**PHASE 2c — DEPTH: the Act-3 SCARCITY decision (2026-06-17, "continue improving until i tell you to stop").** The third decision-spine thread — the §4.3 oversubscription/routing call, made legible. Two surfaces: (1) a per-sat **ALLOCATION LEDGER** on LINK·LOAD — when ≥2 contracts share one sat it lists each one's served bandwidth vs its committed floor, computed the SAME way `router.ts` does (under cap = full offered load; over cap = proportional fair-share `capacity·own/shared`), and flags the one STARVED below its floor (the loser of a collision you must protect or sacrifice); (2) a **REROUTE PREVIEW** on the ROUTING·PREFER tile — the contract's current bridge sat + its load, and where preferring BANDWIDTH would route it (the congestion-relief alternative) + that sat's load, so the slider's effect is legible before you drag (`bridgeForPoint` at the bw-preferring weights over the live shared-load map). Both are PURE per-frame reads (no sim mutation, no fold) → the 3 goldens are untouched. Verified live: reroute reads "on NET-SAT-0 100% · no lighter path"; the ledger correctly stays hidden when each region is on its own sat (no contention to allocate). KNOWN follow-up (the design analysis's "oversubscription never truly stressed" shallow spot): the scenario doesn't reliably create a SHARED over-cap sat, so the ledger fires only when the player serves two regions off one sat — making contention a guaranteed Act-3 beat would be a scenario/golden change, deferred.

**PHASE 2b — DEPTH: ACCEPT is a priced bet now (2026-06-17, "continue improving until i tell you to stop").** The second decision-spine thread. ACCEPT was "click to earn" — the offered row showed only reward/hr. Now each OFFERED contract is an UNDERWRITE: "BET +€R/hr vs −€P/hr if it FAILs" (the hidden `penaltyPerSecond` downside) + a live "can my current fleet HOLD this?" verdict from a PURE preview solve (`router.solve` against the live roster, plus a rolling `windowAvailability` check when the availability axis is enforced) — green "fleet HOLDS this ✓ — accept", or amber "would BREACH {connectivity|availability|latency|bandwidth} — fix first, or accept the penalty risk". So accepting an un-servable contract is a deliberate penalty gamble, not a free button; and the verdict reinforces the launch-first flow (Act 1: REGION-0 reads "would BREACH connectivity" until the GEO is up, then flips to "HOLDS ✓"). Render/orchestration only — `netContractRows` runs a per-frame pure preview (no sim mutation, no fold), so the 3 goldens are untouched. Verified live (before/after launch flip). Next: PREFER reroute-preview + per-sat allocation ledger.

**PHASE 2 — DEPTH: the Act-2 CONSTELLATION decision (2026-06-17, "the gameplay needs to be deeper than some inclination sliders").** A GDD-grounded design workflow (3 readers × 3 vision lenses) found the core gap: the sim ALREADY models a strategy game (proportional fair-share allocation under oversubscription, per-contract SLA bars + penalties, congestion reroute, fault redundancy, constellation phasing + over-build cost) but every lever hides it — ACCEPT is "click to earn," PREFER drags blind, PLACE SET is one button. User picked the **Act-2 constellation** thread first. Turned the one-press PLACE SET into a real decision: a **held-vs-capex LADDER** (worst-phase rolling availability of an even N-sat train at each candidate size around the measured zero-gap minimum) + a **SIZE stepper** the player dials, with the chosen size's HELD %/✓ and a live **CAPEX / over-build** line (sats beyond the measured minimum read as idle capex). The player closes the gap themselves (dial up until HELD crosses the bar at the knee = `zeroGapN`) and sees the cost of over-provisioning (trim to the minimum). Default sits at `zeroGapN` (the safe minimum-that-holds, no soft-lock). New pure `phasingLadder()` in `sim/net/phasing.ts` (reuses the SAME `phasedTrain` + worst-phase `windowAvailability` probe `suggestPhasing` pins the knee with — byte-truthful) + 4 Vitest cases; `netConstellation()` launches the dialed size via the EXISTING batch-launch action (count = chosenN, even phaseSpread) — **the 3 replay goldens stay byte-identical** (verified; the chosen-N state is UI-only, the recorded action carries the concrete count). Render/orchestration + one pure sim helper; no sim-state/golden change. Verified live: ladder `2:50% 3:84% [4:100%✓] 5:100%✓`, over-build "€2,420 idle", under-build "will gap" + amber. Next threads (deferred): ACCEPT price-the-bet card, PREFER reroute-preview + allocation ledger.

**PHASE 1c — gameplay legibility (2026-06-17, "the gameplay still sucks"). A live play-through (accept→tune→launch→accept→earn) confirmed the LOOP is mechanically sound + rewarding (ACCEPT → EARNED ticks, REVENUE +€10.8k/hr, the served link draws on the globe, a new contract is offered, the objective advances) — the friction was GUIDANCE, not mechanics.** Killed the **dead-key instructions** that survived the controls redesign (the literal "shortcuts that don't work"): the ACT-1 objective said *"ACCEPT (K)"* and ACT-2 said *"CONSTELLATION (C)"* — both keys were cut (accept + place-set are panel buttons now). Rewrote all five `NET_OBJECTIVES` to be **desktop-anchored** and reference only live controls ("On CONNECTIVITY (2): aim … press L to launch. Then ACCEPT on BUSINESS (4)"); fixed the matching howto xref ("→ the PLACE SET button", was "/ key C"). Made the orrery's on-canvas hint **desktop-aware** (it keys off `plannerActive`): the orbit-tuning keys (↑↓/←→/[ ]/L) show only on the LAUNCH view; OVERVIEW/ROUTING instead read "open CONNECTIVITY (2) to aim + launch" so no inert keys are advertised. Verified live on all desktops. Render/UI copy only; goldens + cache mode untouched.

### SD-45 — The built M1 slice is condemned: FIRST LIGHT from-scratch redesign (gameplay + launcher + UX)

**Status: ACCEPTED direction (user, 2026-07-01: "the M1 implementation that's currently built has to be completely redesigned from scratch, the gameplay sucks, the UX sucks, there is NO GAME. Launcher system has to be way better"; "the game can't be dragging 3 sliders, has to be more"; "the bandwidth of each satellite has to be part of the gameplay"; "install playwright MCP to playtest loop the game"). Design doc: `docs/m1-redesign.md`.**

A first-hand Playwright playtest confirmed the verdict with specifics: **zero decisions on the golden path** (Act 1 pre-aimed default → press L; Act 2 ladder defaults to the computed answer → PLACE SET; Act 3 prescribes its own fix), the launcher a settings form with a non-event launch (no loadout expressible in the action), a dead world (packets-in-flight absent from net mode entirely), lying UI ("WILL SERVE ✓" + "footprint does not reach" simultaneously; permanently stale headline), a printer economy (€67k in 3 min), and a hidden uniform capacity constant. Root cause: **the build violated the M1 spec's own LOCKED planner rule** ("a planner that solves the puzzle for you is a vending machine, not a game", spec §3.2) — the spec stays canon (Part I shapes); the SD-44 dashboard-polish track is dead.

**The redesign (4-vision × 3-judge workflow; judges split 2-1, resolved by braid):** FIRST LIGHT chassis (launch-first: one MISSION desktop, the launcher as design→aim→commit→fly with drag-the-draft-ring aiming, coverage-comb facts-only instrument, 5-beat sim-driven launch event with partial-deploy drama) braided with FIRST WATCH's **beam-pointing** (per-antenna capacity; ACCESS/GATEWAY spot beams assigned one-target-at-a-time via a new `net_assign_beam` action; re-beaming = the free non-capex intervention lever), plus: local-solar-time diurnal demand archetypes (statistical multiplexing made visible), the economy theorem (no contract pays for its own honest provisioning; 2× penalty asymmetry; per-sat opex), typed-numeric orbit entry shipped BEFORE drag handles, queue-rings of unserved demand, and two structural laws — **facts-never-verdicts** (instruments recompute from sim snapshot per frame; solved diagnoses exist only post-hoc in the parse) and **goals-never-instructions** (CI lint bans imperative control strings in UI copy). Per-sat bandwidth = the consolidate-vs-split bet (fat T2: fewer beams/one fault domain vs T1 swarm) with geometry-based anti-win-button guards.

**Build plan R0–R4** (sim substrate → typed-PAD first playable → drag/drama/flows → the hour with faults+overclock → Mars teaser + gate), each phase gated by a **Playwright playtest loop** with bot-measurable falsifiers (decision-count on golden path, fork pick-rate 20–80%, bandwidth recurs as binding constraint, lint clean). Sim backbone (clock/RNG/replay/goldens/router/contracts/scenario/orrery/WM) carries; the net panel layer + objectives copy + verdict strings + `NET_LINK_CAPACITY_UNITS` die. Goldens re-pin once per phase (SD-40 discipline).

**R0 SHIPPED (SD-45, 2026-07-02) — the sim substrate.** All five R0 items landed in `src/sim/net/`, one coordinated net-golden re-pin (`4638365066733440034n` → `12975807967364188200n`; M1-cache + M2 goldens untouched); 714 tests green, tsc/build/smoke clean.
- **Antenna cards + bus tiers (`sat.ts`)**: T1 smallsat 1G+1S / T2 comsat 2G+2S; card catalog BROADCAST 1.5u/€2.5k · ACCESS-S 1.2u/€1.2k · ACCESS-L 2.4u/€2.8k · GATEWAY 4.0u/€4.5k · CROSSLINK 1.6u (inert relay substrate); capacity lives per-antenna — `NET_LINK_CAPACITY_UNITS` is no longer consumed by the router (kept only as a legacy alias for the old panels until R1 removes them).
- **Beams (`beams.ts` + router v2)**: pipes = (sat, slot); ACCESS/GATEWAY are one-target spot beams assigned via the new `net_assign_beam` action; BROADCAST floodlights latency-tolerant demand only; fair-share/congestion/bandwidth-bite all per-pipe; a beam reassignment is a topology change (folded beamsKey). **Design consequence discovered + recorded:** under the pipe model, cross-contract sharing lives on BROADCAST pipes (and same-region beams) — so the act3a squeeze was re-authored as REGION-0 + a new latency-tolerant BACKHAUL-3 sharing the GEO's BROADCAST pipe (phases ~103° apart), while the latency corridor teaches POINTING (3 phased ACCESS LEOs, since one moving LEO can't hold a low-latency SLA) and the GEO-ceiling lesson moves to the planner/comb surface (a latency-active contract never gets a GEO path to fail on latency — BROADCAST is ineligible by asymmetry).
- **Launch events (`session.ts` pipeline)**: commit → countdown 4s → ascent 10s → per-member deploys 1.5s apart; seeded rolls at commit (vehicle loss 2% — maiden + Act-1 forced clean; underburn 8% → the €300 `net_circularize` fix; no-sep 3%); sats no longer teleport. The canonical arc TAKES attrition on the pinned seed (2 polar no-seps + underburns) and answers it (circularize + an interleaved fill batch) — drama is mechanic, not script.
- **Economy**: start €40k; stack cost = vehicle (base €6k + tier-lift €/km) + bus + cards (GEO broadcast stack ≈ €19k); per-sat opex drain (T1 0.1 €/s, T2 0.35); pay 2 €/s, penalty 4 €/s (2× asymmetry), term 2 sim-h; the THEOREM (no single contract pays for its own honest provisioning) asserted in `economy.test.ts`. Balance across the full canonical hour lands deep negative — the real tuning pass is R3's (renewals + offer cadence), recorded as owed.
- **Re-tame witness hardened**: the dip counts only when bandwidth-binding; the latch requires a PLAYER topology action (launch-deploy/beam/circularize/prefer) strictly after the dip + sole-on-pipe + all-active-green (passing-sat transients and partner knock-outs can no longer fire the act3a gate).
- Near-breach knob 0.1 → 0.08 (PLAYTEST KNOB; sized under the ~54s seeded bite window). Canonical arc gate ticks: act1@1441, act2@39662, act3a@56303, act3b@58104; mild-first trio verified (degradation 56304→58104 → transient → telegraphed).

**R1 SHIPPED (SD-45, 2026-07-02) — the MISSION shell, playtest-gated.** Net mode's five SD-44 desktops are gone; the game is ONE primary desktop, **MISSION** (orrery hero 64% + right rail: the MISSION panel ⇄ PAD, the WIRE (SYSTEM.LOG), LEDGER·FLEET), plus a minimal REVIEW. 721 tests, tsc/build/smoke green.
- **The MISSION panel** — BOOK face: the per-act GOAL (copy.ts, no instructions) + tender rows with SLA pips, the honest bet ("pays €7,200/hr while served · bleeds €14,400/hr while signed and dark") and SIGN. PAD face (opened with L or the button): bus tiers, the antenna-card grid (per-card pipe capacity + price), typed orbit numbers (alt/inc/sub-lon/RAAN/phase — typed-first per all three judges; drag lands in R2), batch stepper, the **COVERAGE COMB** (facts-only LoS windows: "in view 0%→100% of the orbit · PARKS · one-way 3.6 ms"), the itemized stack cost, and a two-step ARM → LAUNCH.
- **The DEAD PRE-AIM**: the boot draft parks 90° W — the comb reads 0% and the red region visibly sits outside the ring until the player aims. The Act-1 hand-aim criterion is structural now.
- **LEDGER·FLEET**: wallet + live €/min flow (opex reads as −€6/min red while idle; +€114/min serving = pay − opex exactly), per-sat chips with pipe rows (BROADCAST floodlight load / pointable beams cycle targets on click → net_assign_beam), underburn chips carry the CIRCULARIZE €300 button.
- **The WIRE**: launch events beat through the log (terminal count → liftoff → separation confirmed / NO SEP / underburn / VEHICLE LOST) + "first signal — {region} lit" per contract; the boot line is the one-sentence premise. The SD-43 briefing MODALS are dead (anti-pattern).
- **LAWS enforced**: `copy-lint.test.ts` fails the build on imperative control references in player-facing copy (press X / click Y / on DESKTOP (n) / the X button); all mission copy lives in `panels/copy.ts`. No cached derived strings — both panels re-render from per-frame pure reads.
- **Playtest gate (bot-driven cold run, twice)**: pad → aim (mandatory) → arm → launch → deploy beats → sign → "equatorial metro lit" → +€114/min. Real decisions on the cold path: bus tier, antenna card, regime/altitude, AIM, batch size, sign-timing (signing before deploy bleeds the 2× penalty), tempo — ≥5, none pre-solved, zero instructions in any surface (runtime-checked).
- **Owed to R2**: drag-the-ring aiming (typed stays first-class), the staged launch-event VISUALS (ascent arc/deploy pops — WIRE beats only for now), queue-rings + flow particles + beam cones on the globe, hover cross-highlighting. **Owed to R3**: the acts 2–4 scenario/copy pass on the new surfaces, offer expiry clocks, the economy balance pass (renewals; the canonical hour currently lands wallet-negative), REVIEW/parse depth.

**R2a SHIPPED (SD-45, 2026-07-02) — the flicker root cause, the viewport bug, and INFORMED DECISIONS.** Driven by live user play ("region drawing keeps flickering"; "the gui doesn't feel the browser"; "i want clear pointers how the antennas are going to be serving, tooltips on the stats, previews for the planner — otherwise how do i make decisions?") + their session screenshot (4 comsats €74,441 parked at the dead pre-aim longitude, beams pointed at a region with no line of sight, −€324/min).
- **THE FLICKER, root-caused by measurement** (rapid-capture pixel diffing + live probes, not guesswork): the de-squash remap `orbitRenderRadius` had a CLIFF at the surface (identity at `d ≤ surfaceM`, `+surfaceLiftM` one ULP above) — surface-anchored discs sit EXACTLY at d = surfaceM, so ±1 ULP of floating-origin cancellation teleported them across the cliff every few frames. Fixes: (1) the lift now RAMPS in continuously over the first 0.1% of the band (identical for every real orbit, immune to float noise); (2) surface discs are placed BY CONSTRUCTION on the rendered sphere (body scene centre + f64 surface direction × bodySceneR) — never through the remap; (3) all binary dither discards (sphere + discs) got SOFT one-Bayer-step thresholds (cells fade, never pop) and the globe base paints solid (no discard depth-holes). Verified: region-area pixel churn 1,200/frame-pair → ~45 (pure edge motion); disc matrix advances monotonically across 16 frames. (User's GPU still needs their eyes for final confirmation.)
- **VIEWPORT SYNC** (the "GUI doesn't fill the browser" bug, environment-specific to their fractional-scaling setup): #app is now pinned to live innerWidth/innerHeight with re-sync on resize/visualViewport/DPR-change plus a 1 s watchdog that heals drift and relayouts the WM. Needs user confirmation on their machine.
- **INFORMED DECISIONS (facts, never verdicts):** the comb now unions the WHOLE BATCH (a 0°-spread stack legibly combs no better than one sat); batch-member park markers on the draft ring (a stacked batch reads as one pile before commit); the stack line carries `· wallet €N` and turns red when over; pad RULES-FACTS (a floodlight cannot carry a latency SLA; spot beams start unaimed; 0° spread stacks the batch); per-beam GEOMETRY FACT in the fleet ("in view" / "NO LINE OF SIGHT" — pointing does not bend physics); parked sats show "parked over lon X°" (the aim, previously invisible); native tooltips on every stat (bus/card roles, orbit params, comb, SLA pips, facts line, flow rate, ARM).
- Verified live: the user's four-stacked-comsats scenario now reads "in view 0% · 0° spread stacks every member · €56,041 · wallet €40,000 (red)" before a euro is spent. 721 tests, tsc, build, smoke green. Dev probes `__netDebug`/`__discDebug` kept (invoke-only, no hot-path cost).

**R2b/R3a SHIPPED (SD-45, 2026-07-02, the night shift) — pointers on the globe + the sustaining economy.**
- **BEAM POINTERS (R2)**: every assigned spot beam draws sat→region on the globe; a beam whose sat has NO line of sight to its target draws RED ("pointing does not bend physics" — the €74k session's missing pointer). The serving path's utilisation now reads per-PIPE (its own capacity), not the retired uniform constant.
- **LAUNCH ARCS (R2)**: in-flight launch events draw a rising arc from the ground site toward the first member's park, progress-clipped through countdown/ascent (red on a lost vehicle).
- **RENEWALS (R3, `renewalOffer` + session hook)**: a COMPLETED term immediately spawns its renewal — same region/axes/class, offeredLoad = the GROWN baseline (you built for less than they now need), pay ×1.15, on a 30-min offer clock. The sustaining loop the economy theorem depends on ("margins come from sharing + renewals"). Deterministic; unit-tested incl. replay-parity; the canonical golden is untouched (no completion inside its horizon).
- **OFFER WINDOWS (R3)**: Act-2+ tenders + all renewals carry expiry clocks (shared m2 expiry helper; the Act-1 opener stays patient); tender rows read "lapses in Xm Ys".
- 725 tests, tsc, build, smoke green. **Owed next (R2/R3 continue):** drag-the-ring aiming; deploy pop pulses + queue rings; the acts 2–4 copy/scenario pass on the MISSION surfaces (scenario Shortfall strings still reference presets and have no surface); the economy balance numbers pass (start/costs/tariffs against the renewal loop); REVIEW/parse depth; user confirmation on the viewport fix + flicker fix on their GPU.

**R2c SHIPPED (SD-45, 2026-07-02) — DRAG-TO-AIM + the acts made playable on MISSION.** User feedback mid-shift: "flicker is indeed gone" (the de-squash cliff fix CONFIRMED on their GPU); "earth is zoomed too much on the default view" (MISSION hero framing 0.38 → 0.24 so a full GEO ring fits with margin); "you replaced the sliders with a text box? doesn't change the gameplay, just makes it more annoying" — correct, and answered with the REAL R2 interaction:
- **DRAG-TO-AIM ON THE GLOBE**: with the pad open, pressing ON the globe grabs the AIM — the draft's sub-longitude follows the surface point under the cursor and the inclination rises to REACH the latitude you point at (raycast against the operated-body sphere, spin-corrected to body-fixed lat/lon). Off-globe drags still orbit the camera. The typed fields remain as live readouts + precision entry (their real role). Verified live: drag moved sub-lon −90°→30.5°, inc →17.9°, comb re-read 100% during the drag.
- **Acts 2–4 playable on MISSION**: active tender rows carry the §7.3 ROUTE toggle (SHORT ↔ SPREAD — the act3a relief verb, retired with the old PREFER panel, restored); the Mars tender carries LAUNCH DEEP-SPACE RELAY (the act4 verb — the MARS_RELAY preset id the presence-based router needs had NO reachable path in the new UI); fault WIRE lines + the amber fault pulse survived the panel retirement and are live.
- **R2 visuals**: the QUEUE RING (a pulsing red halo on a SIGNED-and-dark region — bleeding, not just dim) + the FRESH-DEPLOY pulse (a new sat flashes bright for its first seconds — the deploy payoff).
- 725 tests, tsc, build, smoke green.

**R2d SHIPPED (SD-45, 2026-07-02) — aim assist + honest launch trajectories.** User: "dragging is pretty brutal though, i kind of like it, but very hard to aim" + "wtf is with the launch trajectories? where do the rockets launch from?"
- **AIM ASSIST**: the raw 1:1 raycast is now DAMPED (the draft glides ~35%/event toward the cursor's surface point) and MAGNETIC — every live demand region is a magnet (within ~6° the aim clicks EXACTLY onto the customer's lat/lon), the equator snaps inclination to 0 within 7°, the poles snap to 90 past 80°. Verified live: an atomic scripted drag onto the live region lands lon 0 / inc 0 with a solid comb. (The earlier "−27.6° offset" was measurement artifact — the globe rotates 1.5°/s and my screenshot-then-probe gap was ~18 s; the conversion math was correct.)
- **LAUNCH ARCS FIXED**: the arc was chasing the sat's CURRENT position after separation (a LEO sweeps away → the "trajectory" stretched across space). It now targets the FIXED deploy-time insertion point, lives only from commit to separation +2 s, bows gently (0.12), and anchors at the ground station — where the rockets launch from.
- Dev probe `__aimProbe(x,y)` added (invoke-only). 725 tests, smoke green.

**R2e SHIPPED (SD-45, 2026-07-02) — Cape Canaveral, visible sites, wheel zoom, viewport hardening.** User: "you can't see GROUND-0 properly right now, put it to cape canaveral too" + "mouse zoom still doesn't work, GUI still doesn't fill the window".
- **CAPE CANAVERAL (28.4° N, 80.6° W) is the LAUNCH SITE** — the launch arcs rise from it, marked ▲ CAPE CANAVERAL on the globe. DESIGN SPLIT, recorded deliberately: the COMMS ground stations (GROUND-0 equatorial, GROUND-1 polar) stay where the act geometry needs them — relocating GROUND-0 to Florida breaks acts 2–3 physically (a corridor LEO cannot see both the equatorial metro and a 28° N dish at toy scale simultaneously; the whole canonical arc would re-derive). Real-world framing: launch pads ≠ ground stations. Relocating/choosing ground stations properly is an R3+ design decision (candidate: player-placeable grounds).
- **SITES VISIBLE**: ground stations draw as ⌾ dish markers + labels; the pad as a warm ▲ + label (pooled billboards + the shared DOM label layer).
- **WHEEL ZOOM FIXED**: the hero/planner FILL distance silently overrode every wheel event; the wheel now scales a `netZoomMul` (0.35×–4×) over the fill distance (reset on framing change). Verified: sphere 117 px → 195 px in, 76 px out.
- **VIEWPORT hardening round 2**: #app sizes from visualViewport metrics (innerWidth lies on some Wayland fractional-scaling stacks) + a 100vw/100vh CSS fallback; `__viewport()` console diagnostic added for the user's machine (their report persisted after round 1 — environmental; awaiting their probe output if it persists).
- 725 tests, build, smoke green.

**R2f SHIPPED (SD-45, 2026-07-02) — the TWO-ROW COMB: aim into the fleet's gaps.** User: "one of the satellites failed, i wanted to replace it, but how do i aim it in the first satellite's gap if i can't see current coverage." The comb now draws TWO rows on ONE time axis: TOP (dim) = the CURRENT fleet's line-of-sight windows over the target — the gaps are the dark cells (a no-sep hole reads as a hole); BOTTOM (bright) = fleet ∪ THIS draft — aim until the bottom row is solid where the top row gapped. Facts line: "fleet holds N% · with this launch M%". Verified live on the exact scenario (polar batch with seeded no-seps → fleet holds 27% with visible gaps → the replacement's union row 100%). Pure additions `combWindows`/`draftMembers` in comb.ts.

**VIEWPORT BUG RESOLVED (2026-07-02): NOT an app bug.** The user confirmed the GUI fills fine in their own browser — the "doesn't fill the window" reports came from the PLAYWRIGHT-DRIVEN Chromium window, whose viewport is PINNED at the 1920×1080 the MCP session set (`browser_resize`); resizing that OS window never resizes the page. The R2a/R2e viewport-sync + visualViewport hardening stays (harmless, still useful for real fractional-scaling stacks), and `__viewport()` remains as a diagnostic. Lesson recorded in memory: the MCP playtest browser is VISIBLE on the user's desktop — reports made while looking at it can reflect its fixed viewport, not the app.

### SD-46/SD-48 — Loadout exploit closed + single truthful launch pricing (net-golden re-pin #1, 2026-08-07)

**Status: ACCEPTED origin (the 2026-08-07 four-audit FL plan, `docs/first-light-integrated-plan.md`).**

- **The exploit (FL-01):** a lean wire launch (absent or `[]` loadout) silently FLEW the standard BROADCAST antenna but was charged for zero cards — a free €2,500 card, per launch. Root cause: the `cardIds.length > 0` guard in `applyNetAction` skipped validation and priced the raw (empty) list while `resolveLoadout` defaulted the fit. Fix: `DEFAULT_LOADOUT_CARD_IDS = ["BROADCAST"]` is applied *before* validate+price, always; `netLaunch` records the default for an explicitly-empty list (an absent key keeps the lean pre-R0 wire shape); the PAD stack line previews the SAME effective loadout (never the free-empty fiction).
- **Single pricing truth (FL-11):** `previewLaunch.costEur` no longer runs the legacy `base+altura×count` math (which had silently drifted from the charged stack — a lying by ~€17k preview on the old tiles). The preview IS `launchStackCost(bus, cards, semiMajorM, count)` — the one function the applier charges; `launchDraftCost` is deprecated (legacy readers only). The draft now carries an optional `bus` (defaults smallsat; the router never reads it — pricing/slots only).
- **Batch manifest discount (FL-11):** `NET_BATCH_MEMBER_DISCOUNT = 0.15` — members 2+ pay 85% hardware; the vehicle is already shared once. Batching is now cheaper per sat than singles by construction (the consolidation incentive the Act-2 ladder teaches).
- **Golden re-pin #1 (of two planned):** `12975807967364188200n` → `71513456548811159n`. Drift is economics-only (priced default + discounted canonical batch): the P0b/R0 debit tests were updated to the new truthful expectations, restore-replay == continuous-run and the JSON round-trip pin are unchanged. The M1-cache and M2 build goldens are untouched. The manifest-discount PAD fact row ships with FL-08 (copy must ride copy.ts/lint first).
- Tests: `src/sim/net/apply-action.test.ts` (absent-empty-vs-priced: the exploit-pinning regression, batch-discount arithmetic pins). 732 green.

**SD-50 RECORDED AS PROPOSED (not built):** a satellite power budget (bus power vs card draw) is the loadout editor's natural third trade-off axis — `BusSpec.massKg` is dead data, no power field exists. AGENTS.md §1 bars building it without user approval; the proposal is parked in `docs/first-light-integrated-plan.md` §3. Awaiting user decision.

**FL-02 SHIPPED (2026-08-07) — law coverage, not new law.** The copy-lint (LAW 2) now covers EVERY panel file that can render player-facing copy (17 files; the retired SD-44 panels included — they still mount in ?mode=cache; two live violations there were reworded, not exempted). A second lint bans new hardcoded hex colours in panels (tokens-only per DD-1) with a shrink-only allowlist seeded from mission-top.ts's 4 comb-canvas fills — FL-15a must empty it, nothing may grow it. 763 tests green; smoke + boot clean.

**FL-03 SHIPPED (SD-46, 2026-08-07) — slot-indexed loadout state + antenna-reactive draft.** New pure `src/panels/loadout-state.ts` (G-slots-then-S-slots, duplicates legal, bus-switch re-slots and truncates legally; 6 reducer tests). `r1Cards` is now a derived VIEW of the slot state; `netDraft.bus`/`netDraft.loadout` sync off it on every edit — preset clicks re-seed the orbit only, never revert the card fit. Live-verified in the driving browser: bus/card edits move the stack line (T2 8,500 · empty→charged-default 8,500 · ACCESS-S 7,200 · duplicate G 8,400 · 2× batch 35,361 with the −15% member). 769 tests, build, smoke green.

**FL-04 SHIPPED (SD-46, 2026-08-07) — the PAD silhouette slot editor.** The flat antenna toggle grid is gone. The pad now renders the bus as NAMED SLOTS (G1/G2/S1/S2, class legend "GROUND-FACING / SAT-FACING", ▣ fitted / ▢ empty — shape+label, never colour); clicking a slot opens the class-correct chooser (an S slot offers only CROSSLINK + EMPTY); duplicates are first-class (two ACCESS-S in G1+G2); EMPTY clears a slot (empty sat still flies + pays the default BROADCAST — the FL-01 truth is printed in the slot tooltip). Interface change: `onToggleCard` → `onSlotCard(slot, cardId|null)`; state carries `slots`. Slot styles are token-only (FL-15a shrinks the hex allowlist, these add none). Live-verified: G-slot ACCESS-S ×2 → hardware €8,400; + CROSSLINK → €10,200. 769 green, build+smoke clean.

**FL-05 SHIPPED (SD-46, 2026-08-07) — antenna-truthful footprints.** New pure `world.ts` pair: `horizonReachRad(altM)` (the LoS cap at the SAME 5° elevation floor the link budget enforces) + `footprintRadiusRad(loadout, altM)` (BROADCAST = the horizon cap; ACCESS/GATEWAY = cone clipped by horizon; S-only relay fit = horizon, no surface promise). The PAD draft disc AND committed covering discs are sized off the drafted antennas now, never the region's radius — fitting ACCESS-S visibly SHRINKS the promise from the floodlight sky to the 30° cone. Consequence-truth invariant extended to a comsat+ACCESS draft (`footprint.test.ts`, 4 pins). Golden-clean: net-replay pin holds (`71513456548811159n`). A behaviour note for players: an ACCESS fit previews UNSERVED until pointed after deploy — correct teaching, by design.

**FL-06 SHIPPED (SD-46, 2026-08-07) — the FIT assist, viable-never-optimal.** Pure `suggestLoadout(bus, {latency, bandwidth})` in sat.ts: latency-active ⇒ cheapest legal spot (ACCESS-S); bandwidth ⇒ upsized spot (ACCESS-L); else BROADCAST. NEVER fills a second slot, NEVER returns GATEWAY — headroom is the player's ceiling (spec §3.2 vending-machine ban, pinned by tests). The PAD's FIT button fills the silhouette from the target tender's active axes. Live-verified: act-1 connectivity tender ⇒ G1 BROADCAST. 777 green.

### SD-47 — Contract texture: sign-on bonus, decaying pay, Act-1 clocks (net-golden re-pin #2, 2026-08-07)

**Status: ACCEPTED (FL plan P3-C1).** The tender is now a live market object. Additive Contract fields `offeredAtS / signOnBonusEur / signOnBonusUntilS / payHalvingS` (named to stay clear of the act4 freshness-fence lint). Pure helpers `decayedPayAtS` / `signOnBonusAtS`. `acceptContract(id, t)` prices the signature: pay FREEZES at the decayed board value, the penalty rebinds to 2× frozen pay (asymmetry pay-side-symmetric — the audit's open question), the bonus lands in the wallet once. Act 1 emits TWO clocked tenders: REGION-0 (flat €2/s, +€2,000 sign-on within 900 s) and REGION-C (equatorial transit 5°E, 1.3× pay halving every 1200 s unsigned). **The Act-1 patient-Infinity offer exemption is dead** (2 h windows). Pay multiple is 1.3×, not the plan's 1.6×: a full-pay term (€18,720) must stay under the €19,055 honest stack (economy theorem; pinned in contract-texture.test.ts). Golden `71513456548811159n` → `14974205439654686823n`; pay/penalty now FOLD (accept-freeze made them mutable); restore == continuous. The act4 freshness-fence and purity lints green. Penalty on REGION-C scales its 2× with the frozen pay by construction. UI rows land in FL-08.

**FL-08 SHIPPED (SD-47, 2026-08-07) — the tender board carries the texture.** Each offered tender shows: the LIVE board price (the pay a signature would freeze at now — REGION-C visibly decays, €9,322→€9,305/hr in 3 s), the sign-on bonus counting down, the decay tempo ("pay halves every 20m unsigned"), the breach-grace fact (10m00s), and the lapse clock. Facts only (LAW 1); rows live-update per frame. The FL-11 manifest-discount row also landed (PAD stack appends "2nd+ satellite on the same vehicle −15% hardware" when batching). All copy rides the lint (copy.ts fragments). 785 green.

**FL-09 DECIDED (SD-47 addendum, 2026-08-07) — the Act-4 Mars relay tender stays PATIENT (no offer window).** With FL-07 every Earth tender is clocked; the Mars tender's clocklessness is now a SIGNAL, not a gap: the frontier does not run on your schedule, and act4's whole lesson is that *light-time* is the pressure (minutes each way), not the market. A market clock there would teach a second, orthogonal concept in the same beat (violates one-concept-per-act); the gate stays false forever regardless, so nothing downstream depends on expiry. No code change.

**FL-10 SHIPPED (SD-49, 2026-08-07) — the honest risk band.** Pure `launchFailureRates(count, armed)` beside the rolled constants + `session.failuresArmed` getter (cursor>0 — the same predicate the roll gates on). The PAD shows "launch risk — vehicle loss 2% · underburn 8%/sat (a burn fixes it) · no separation 3%/sat" ONLY once failures are real; in Act 1 the line is ABSENT (never "0%" — silence, not reassurance). Live-verified end-to-end: boot → two tenders → aim (typed sub-lon 0) → ARM/LAUNCH (€22,841 after the €19,055 stack + the +€2,000 sign-on landed) → deploy → REGION-0 carrying 100% → act-1 gate fires → pad re-opens WITH the band. 788 green.

**FL-12 SHIPPED (SD-49, 2026-08-07) — time-to-service on the pad.** Pure `timeToServiceS(eph, draft, regionPoint, grounds, t, horizonS, stepS=5)` forward-scans `isPointServed` (the same router gate): parked-GEO-over-region ⇒ 0 (rendered "serving NOW"); mis-aimed GEO ⇒ Infinity (rendered "never served on this orbit"); LEO sweep ⇒ answered within ≤2 periods. Pad facts line now ends "· serving NOW / first serve in Ns / never served on this orbit". 3 pins in footprint.test.ts. **Flake fix (same commit):** vitest testTimeout/hookTimeout 5 s → 60 s — the golden replay suites legitimately need tens of seconds under full-suite CPU contention (three false-reds today); real hangs still visible.

**FL-13 SHIPPED (SD-49, 2026-08-07) — ring-grab altitude drag.** Pad open ⇒ pointer priority: draft-ring grab (≤PICK_TOLERANCE_PX=26) → globe aim-drag → camera orbit. Vertical pull on the ring changes altitude through the SAME netEditDraft path (shared clamps + live preview — no extra solve throttle needed, the pad re-previews every frame anyway). metres-per-pixel derives from the ring's screen span at grab so the gesture scales with the orbit. `__dragOrbitProbe` beside `__aimProbe`. Scripted Playwright verification: ring drag 534.7→659.9 km with live price (€19,055→€21,059); globe drag at 60 px from the ring still aims (phase −90→−61.6°, altitude untouched). Note: finding aim points near the ring scan showed the aim region sits at the left ~third of the hero pane in this viewport — flagged for FL-15 geometry pass. P4-C4 carried the pointer priority: ring → globe → camera documented in the SD-49 skeleton.

**FL-14 SHIPPED (SD-49, 2026-08-07) — the launch payoff: draft chip, pooled arcs, deploy pops.** (a) Ring-pinned DRAFT chip on the orrery while the pad is open: "DRAFT — €19,055 · period 240s · serving NOW / first serve in Ns / never serves the target" (facts only; the dead pre-aim reads 'never serves' by construction). (b) Launch arcs are POOLED (one Line per concurrent event, ≤4) — a second launch no longer invisibly replaces the first's theatre. (c) Deploy pops are per-member one-shots: freshAgeS ≤ 3 s drives a quadratic-decay flash (9→31 px peak) instead of the old constant shimmer. Dev probe __launchTheatre() (events + pops). Live-verified: two concurrent launches each in flight, two pops with distinct ages. Chip hide-path bug caught in verification (fast-path treated "" as hidden) — fixed. 791 green.

**FL-15 SHIPPED SUBSTANTIALLY (2026-08-07) — the style overhaul, one pass on final surfaces.**
- **15a palette unification:** the R1 mission chrome's private palette (#49d7c8/#e2b04a/#e2604a/#2a3646/…) is DEAD — style.css is 100% tokens (334 var() reads; two divergent cyans/ambers unified); the panels hex lint allowlist is now EMPTY (shrink-only contract kept); the comb canvas reads tokens via getComputedStyle with keyword fallbacks.
- **15b type scale:** 4 tokens (ts-cap 9 / ts-ui 11 / ts-body 13 / ts-head 16), the ad-hoc half-pixel sizes (11.5, 10.5, 9.5, 15) swept onto the scale. Font BUNDLING deferred — the repo has no offline font pipeline (recorded; revisit).
- **15c affordance honesty:** the static ● ⛶ ✕ glyphs (zero handlers, cursor:default lies) are DEAD in the topbar AND every titlebar. Gutter split gains a 48 px zone-edge floor under the 12/88% clamp (span-aware, pinned) — tiles can't starve to unreadability.
- **15d dither made VISIBLE:** sparse 0.045→0.09, dense 0.06→0.13, mid 0.10→0.16 (the 1-bit tonal depth actually reads at a glance now). Focused tile gains a redundant SHAPE channel (▸ in the titlebar, not just the cyan border — CVD law). Preset tabs are REAL clickable buttons (same setWmPreset as keys 1–5; hover/active affordance).
- **15e cursor:** crosshair on the signal surface (aim/drag/grab verbs) — the CSS keyword carries the 1-bit feel; a bundled pixel-cursor asset is deferred with fonts (no asset pipeline). Boot sequence deferred — flavour, not affordance.
- Verified live: fakes gone, tab click switches desktops, ▸ focus marker present, crosshair cursor, dither injected. 794 tests, build, smoke green.

### SD-50 — Satellite power model: DEFERRED to M2 (user decision, 2026-08-08)

**Status: DEFERRED (was PROPOSED).** A per-bus power budget (bus generation vs antenna-card draw, throttle on overdraw, optional fault coupling) is the loadout editor's natural third trade-off axis — but the GDD gives power budgets to *datacenters* and is silent on satellite buses, so building it now would have been inventing a requirement. User call: **defer to M2**, where satellite power has real physics to hang on (solar flux per body, eclipses, RTG vs panels, station-keeping) and won't need a toy re-design. **Standing consequence:** `BusSpec.massKg` stays on the table as the M2 anchor — do NOT delete it as dead code; it's the reservation. If M2 doesn't land power, delete it then.

### SD-51 — The R3 economy balance pass (net-golden re-pin #3; canon extracted, 2026-08-08)

**Status: ACCEPTED (numbers pinned in `src/sim/net/canon-balance.test.ts`).** The measured canonical hour opened at −€83,646 (R3 owed: "lands wallet-negative") and now closes **+€3,536, floor −€1,169, all four gates at the same ticks (24.0 / 661 / 938.4 / 968.4)**:
- **Prices:** launch base 6000→**2400**, lift 16/30→**10/16 €/km** (consolidation ratio kept); ante **€40k→€75k**.
- **Throughput:** pay €2→**€20/s**, penalty 2× (€40/s), **term 7200→480 s** — terms now CYCLE inside the hour: renewals (+15%, grown demand) actually land in-session, which is the real economy lesson ("margins come from sharing + renewals" becomes playable, not doc-fiction).
- **The squeeze survived only via a real invariance fix:** renewals now INHERIT the customer's diurnal `loadPhase` (same region, same rhythm — time-continuous across generations); without it a renewal silently re-phases the shared-pipe asymmetric-peak window that act3a is calibrated around. The canon signs `REGION-0+R1` (t=520); the re-tame witness rides the renewal generation (escalation test + "gate doesn't fire early" updated).
- **Canon extraction:** the arrival-arc builders/ticks/replay driver/hasher moved to `src/sim/net/canon.ts`, shared by the golden and the measurement suite (one source; the two tests pin the same hash from both sides).
- Golden `14974205439654686823n` → `17948230282099181132n` (economics + texture folds already in; gates + physics pinned identical).

### SD-52 — Ground-station placement/relocation: FIXED grounds, no placement verb (recorded 2026-08-08)

**Status: ACCEPTED (user call, 2026-08-08: "fixed grounds indeed").** Option (a) wins through the M1 gate and by default beyond: ground stations are AUTHORED GEOMETRY, never a capex verb. The split stays honest and diegetic (pads ≠ ground stations — rockets rise from Canaveral, comms grounds sit where the act geometry needs them); every act's calibration holds; "better geometry per site" is a MAP question M2 owns, not a launch-pad knob. No code change (the absence of a placement action IS the decision); revisit only if M2's coverage grid makes cheap placement legible.

*The proposal context is preserved below for the record.*

**Was: PROPOSED, not built.** The R2e design split (launch pad = Cape Canaveral; comms grounds stay equatorial/polar for act geometry) left a hanging question: do players ever PLACE or MOVE ground stations? Options: (a) fixed authored grounds forever (M1 fence — simplest, geometry stays teachable); (b) player-placeable grounds as an Act-3/M2 verb (a real capex lever: better geometry vs another site to protect — but re-derives every act's calibrated geometry); (c) M2-tier "expansion board" where grounds are bought per-region (the escalation natural home). My build-side recommendation: **(a) for the M1 gate** — the current pairing (free launch site from Canaveral, fixed comms grounds) is honest ("pads ≠ ground stations") and every act's geometry is tuned to it. Revisit when M2's coverage grid makes placement cheap to reason about. Needs the user's call before any code.

**PLAYTEST LOOP SHIPPED (2026-08-08).** `tools/playtest.mjs` — a headless scene runner (playwright-core over system chromium, scenes = code modules in `tools/scenes/`), with the first two scenes: `boot` (cold-open composition, tender texture ticking, the epoch-shortfall regression pinned) and `act1` (the full loop: dead pre-aim read, typed aim, a real RING-GRAB through the probe, ARM→LAUNCH wallet charge at the new €12,247 stack, deploy, SIGN, serve, the gate beat on the WIRE, act-2 risk band, REVIEW parse). 22 assertions, ~21 s. `npm run playtest`; `npm run test:fast` skips the heavy golden replays for the inner loop. The probe contract stands: __aimProbe / __dragOrbitProbe / __launchTheatre — the loop only verifies what a probe exposes, so new visuals get probes. With vision enabled (session-side), screenshots also get actual aesthetic review — the first eyes-on sweep fixed: Mars line gated to act 4, dynamic hero fill (0.45 cold open → 0.24 ring-fit), tender history strip, mission-elapsed gate stamps, net PARSE subtitle.

**X-04 SHIPPED (2026-08-08) — the VAULT: browser-storage checkpoints.** Sim-side pure envelope `src/sim/net/persist.ts` (version field + tick + readouts + the folded snapshot) and `src/vault.ts` (localStorage slots quick/autosave, quota-safe, corrupted-slot reads as absent). Keys: `v` save, `V` load; the 120 sim-s autosave cadence runs on sim time (pause pauses it). Resume restores session + tick and prints the fold hash on the wire as the honesty receipt. The two traps the build surfaced and pinned: `Set` fields don't survive JSON (envelope converts activeAxes both ways) and JSON's Infinity→null (offer-clock/decay fields re-hydrated to the canonical defaults; persist.test.ts has the fold-hash-equal pin for in-memory AND JSON paths). Playtest scene `vault` proves save→wreck→resume truth: the resumed world never carried the wreck. 807 unit tests + 45 playtest assertions green.

**X-05 SHIPPED (2026-08-08) — the procedural net audio engine.** `src/audio/engine.ts`: a full Web-Audio synth layer over the existing one-way cue bus. Plate reverb from a deterministic xorshift-seeded impulse response ("slight reverb", mix pinned ≤ 0.18 and tested); a cue grammar where every COMMITTIONal shape is meaningful (rising = good news, falling = bad); health bed = 50 Hz hum + undertone that DETUNES a few cents under strain (the congestion you hear before you read it); a deterministically-sequenced ambient arpeggio under it. Everything player-triggered gets a cue: button clicks, commits, deploy pops, no-seps, underburns, signs/renewals, act bells, fault amber/telegraph, prefer reroutes, Mars relay/first-signal/breadcrumb, vault save/load. The golden cue line a full opener prints: key_click…credit_committed…deploy_pop…signed_offered…gate_act…serve_locked. 812 unit tests + 55 playtest assertions green.
