# SIGNAL HORIZON — Ticket Backlog (trackable)
### Companion to implementation-plan (v0.2) + scoping (historical)

> The plan is the *prose spec*; this is the *checklist*. Tickets are paste-ready for GitHub Issues / Linear. Tick `[x]` when the "Done when" (in the plan) is met. Sizes: `S`≈≤1d · `M`≈2–4d · `L`≈1–2wk · `XL`≈multi-week · `SPIKE`=time-boxed, produces a written conclusion. Scoping notes (`▸`) flag where scoping refined the plan.

**Status legend:** `[ ]` todo · `[~]` in progress · `[x]` done · `[-]` cut/deferred

---

## 🔁 BUILD TRACK — TypeScript / Three.js (browser)

> The project runs in the browser (SD-2). The spike (see `FINDINGS.md`) proved the UX builds at least as naturally in the web stack with no fidelity loss and dramatically faster iteration. The C# Godot project's design docs (GDD, tiling-WM spec, UI critique) remain the design authority; this backlog reflects the *current* TS stack.
>
> **Key status:** the spike demonstrated the full M0 visual/UX scope. Phase 0 foundations and M0 are "done in spike" — save/replay (P0-05/06) still needs production implementation. WebKitGTK validation is no longer a gate (SD-2).

**Build phases (live status):**
- [x] **PA — pure sim (TypeScript port)** — `src/sim/ephemeris.ts` (8-iter Newton → perifocal → 3-1-3 → recursive parent); bit-identical to the C# golden master. Light delay, freshness, LoS occlusion all ported. **Vitest pin + 13 structural assertions green.**
- [x] **PB.1 — Vite dev shell** — `npm run dev` via Vite + HMR; full-bleed dark app, no browser leakage, keyboard-native preset switching.
- [x] **PB.2 — M0 money-shot mechanics** — floating-origin f64→f32 rebase + sim-driven orrery + honest Earth→Mars packet crawl matching the on-screen readout. Packet progress == light-delay readout. **Verified headful in Chromium.**
- [x] **PB.2r — orrery in 3D + camera-only views** — body-anchored orbit camera + animated presets (EARTH / CISLUNAR / ORBITS / SYSTEM / TOP-DOWN; EARTH is the boot default). LEO inclined + GEO equatorial planes read in 3D. Dashed rings, dithered Bayer 4×4 billboards, freshness-as-saturation.
- [x] **PB.2v — live orrery: show the physics, give the player the clock** (SD-35) — render/interaction/clock-boot only (no sim math change; both replay goldens untouched). Near-body de-squash (`orbit-render-scale.ts`) so LEO/GEO/deployed sats SEPARATE from the Earth disc + visibly sweep in the primary view; launched sats get dashed orbital-plane RINGS sampled from their own Kepler elements (`rebuildSatRings` — was dataset-only); deployed sats drawn ALWAYS (not heatmap-gated); CLICK-TO-FOCUS screen-space pick (`pick.ts`); clock BOOTS AT 1× (player drives accel, GDD §3/Risk-6), not the 1000× lock. Screenshot-verified.
- [x] **PB.2w — mouse-driven chrome: on-canvas camera buttons + 3 WM presets + window-summon rail** (SD-36) — WM/DOM/interaction only (no sim math change; both replay goldens untouched). On-canvas clickable camera-preset buttons (EARTH/CISLUNAR/ORBITS/SYSTEM/TOP-DOWN, §8 1-bit chrome, same `setPreset` path as the E/C/O/S/T hotkeys, active-state lit). The 7 WM presets cut to **3** (PLAY/MAP/REVIEW, keys 1–3); no panel lost — the retired recombinations are summoned on demand. The **window-summon rail** (`src/wm/window-rail.ts`): a right-edge vertical button rail (one per panel) that summons a panel into the focused tile LIVE via the new pure `zonegrid.summonInto` (the displaced panel hides + is re-summonable; always-tiled preserved); the Shell tracks a focused tile. Tests: `wm/presets.test.ts`, `wm/zonegrid.summon.test.ts`, `wm/window-rail.test.ts` (478 tests). Screenshot-verified with real mouse clicks.
- [x] **PB.2x — the FLEET tile: the focused body's constellation** (SD-37) — render/read-only (no sim math change; both replay goldens untouched). Click-to-focus turns into fleet management: a new FLEET panel (`src/panels/fleet.ts`) lists the satellites orbiting the orrery's SELECTED/focused body — the dataset sats whose parent is that body PLUS the player's launched roster sats orbiting it, each with a stable label · orbit CLASS (LEO/MEO/GEO/HEO by altitude band) · altitude · period · inclination · EIRP · kind glyph (▲ launched / △ dataset) · status. Backed by a PURE, unit-tested derivation (`src/sim/m2/fleet.ts` — `deriveFleet` / `classifyOrbit`, no DOM/three/Ephemeris-class dep; `fleet.test.ts`: N dataset + M launched, correct classes, no cross-body leakage, empty/null cases). Wired to the SD-35 selection (main.ts builds a `Fleet` each frame from the focused body + the live roster + the dataset sats; rows rebuild only on a signature change — X-02) and registered in the SD-36 rail (`RAIL_PANELS` + the shell registry → a FLEET summon button). 485 tests. Screenshot-verified with real rail click + canvas click-to-focus + L launch (Earth fleet lists, a launch appears, Mars shows the empty placeholder).
- [x] **PB.3 — tiling WM shell + 1-bit chrome** — DD-10 zone-grid in the DOM (1–3 cols × 1–3 rows, relative weights, always-tiled invariant). Title-bar drag → zone swap, edge-resize, 5 presets (OVERVIEW / OPS / TRACK / STREAM / SPLIT) switched by keys 1–5. Runtime Bayer dither via CSS custom properties. Monospace placeholder font. _(The preset SET was later cut to the main 3 — PLAY/MAP/REVIEW — by SD-36, with a window-summon rail composing any panel into any tile; see PB.2w.)_
- [x] **PB.4 — panels** — SYSTEM.LOG (severity syntax highlighting + glyphs, scrollable, ring-buffer), telemetry readout, status strip (sim time, time scale, light-delay, focus, presets, occult).
- [x] **PC — deterministic backbone (TS)** — fixed-tick integer clock ✅ (P0-03), seeded RNG ✅ (P0-04), action-log pending (P0-05/06).
- [ ] **PD — M1 connectivity game (v0.3 reshape)** — the Earth-orbit satellite-ISP connectivity puzzle (`docs/signal-horizon-m1.md` Part I): accept SLA contracts → launch via a consequence-showing planner → tune-by-exception a deterministic solver; serve-or-breach heartbeat; oversubscription tension; Acts 1–3 buildable + Act 4 Mars teaser. **The old cache/freshness economy is reclassified to the Act 4 concepts teaser + post-gate campaign** (see Milestone 1 below). The Act 1 coverage opener (`src/sim/a1/`, isolated world + own golden) is the immediate next build.
- [x] **CI** — GitHub Actions (`.github/workflows/ci.yml`): vitest + `tsc --noEmit` + build + headless Playwright orrery screenshot (uploaded as an artifact). Node 26.

---

## Pre-flight (must precede everything)

- [x] **GIT-00** Init git repo + commit current tree as baseline
- [x] **DEC-00** Confirm decisions D1–D3, D7; establish the sim/ purity rule
- [x] **SD-1..11** Spike decisions recorded (see `decisions.md`)

---

## Phase 0 — Foundations

> **Status: substantially done. Deterministic clock (P0-03) and seeded RNG (P0-04) are done; save/replay (P0-05/06) remains.**

**Spikes**
- [x] **P0-S1** Pin stack versions (Node + Three.js + Vite) — *SPIKE S*
- [x] **P0-S2** Sim-core language — *SPIKE S* · RESOLVED: TypeScript. `src/sim/` is pure TS with zero DOM/Three.js imports. Bit-identical to C# golden master (SD-3).
- [x] **P0-S3** Test & CI tooling — *SPIKE S* · Vitest + Playwright headful. Green.

**Project & repo**
- [x] **P0-01** Repo + module folders — done (`src/sim/ src/orrery/ src/wm/ src/panels/ data/`)
- [x] **P0-02** CI — GitHub Actions (`.github/workflows/ci.yml`): vitest + `tsc --noEmit` + `npm run build` + headless Playwright screenshot via `tools/shoot.mjs` (CI-bundled Chromium through `CHROMIUM_BIN`, `HEADLESS=1`), artifact-uploaded. Node 26 (D1).

**Deterministic core backbone**
- [x] **P0-03** Authoritative sim-clock + fixed-step tick — *M* · Integer fixed-step clock implemented in `src/sim/clock.ts`. Tick accumulator, `scheduleWall()`/`nextTick()` drain pattern, `DT = 1/60` s. Death-spiral clamp. `setTick()` for save/load. 12 Vitest tests green.
- [x] **P0-04** Seeded RNG abstraction — *S* · Splitmix64 ported to TS via `bigint` in `src/sim/rng.ts`. Golden values cross-verified bit-identical against C# `SimRng`. 11 Vitest tests green. No `Math.random()` in `src/sim/`. D4 resolved → bigint for spec-defined portability.
- [x] **P0-05** Action-log + state-snapshot save format — *M* · JSON, versioned; port the C# `SaveGame` shape. Pure `src/sim/`, no DOM state. **B2:** `src/sim/save.ts` (SaveGame, lossless `dt_bits`, bigint-as-string) + `src/sim/action.ts` (SimAction) + snapshot shape; round-trip-pinned. **E3:** live action-log recording wired at `src/main.ts` — prefetch + pause/faster/slower now recorded as actions at `clock.tick` (closing the reproducibility gap). _Minor optional: clock scale/pause in the snapshot itself (reconstructible by replaying the log; only matters for snapshot-only fast-load)._
- [x] **P0-06** Determinism / replay golden-master test — *M* · **B1:** `src/sim/state-hash.ts` (canonical hash port) + TS-native golden pin (`state-hash.test.ts`); the C# hash is not bit-portable cross-runtime (SD-15). **B3:** `src/sim/scheduler.ts` (unclamped, no-drop accumulate-steps kernel) + `save-replay.test.ts` — replays an action log through the kernel and pins the folded mutable-state hash (tick + RNG + Mission), with a scale / frame-slicing-independence proof (a coarse-frame drive only the no-drop kernel can complete). 146 tests green.

**Rendering & styling backbone**
- [x] **P0-07** Floating-origin scene manager — done in `src/orrery/orrery.ts` (f64 subtract → f32 cast; per-preset log-compression; constant-screen-size billboards).
- [x] **P0-08** 1-bit chrome theme + signal-layer styling split — done. CSS dither for chrome; Three.js coloured signal path with freshness-as-saturation; Bayer 4×4 terminator stipple in GLSL.

---

## Milestone 0 — Sim spike + the visible web

> **Status: SPIKE-DONE.** The full M0 scope was built and verified headful in Chromium. See `docs/screenshots/` and `FINDINGS.md`.

**Orbital sim core (truth layer)**
- [x] **M0-01** f64 Keplerian element model — done (`src/sim/ephemeris.ts`, parent hierarchy)
- [x] **M0-02** Analytic Kepler propagation `position(body,t)` — done (8-iter Newton, 3-1-3, recursive parent; bit-identical to C#)
- [x] **M0-03** Earth-orbit satellite propagation — done (LEO 53° + GEO equatorial in `data/system.json`)
- [x] **M0-04** Solar system dataset — done (Sun/Earth/Moon/Mars + sats; vendored as a real file in `data/system.json` — SD-12, was a symlink under SD-4)

**Render layer (orrery)**
- [x] **M0-05** Orrery: bodies + orbits (1-bit field, coloured signal) — done (Bayer 4×4 billboards, dashed rings, sim-driven via floating origin)
- [x] **M0-06** LOD / icon collapse — done (constant-screen-size billboards with min-icon clamp)
- [x] **M0-07** Camera + selection — done (body-anchored orbit camera + 4 animated presets)
- [x] **M0-08** Time controls UI — done (1×/10×/100×/1000× + pause in status strip)

**The visible signal**
- [x] **M0-09** Link model + line-of-sight + distance — done (LoS occlusion in `src/sim/links.ts`)
- [x] **M0-10** Light-delay computation + display — done (`SignalDelay.oneWaySeconds`, readout in telemetry)
- [x] **M0-11** Packets-in-flight rendering — done (packet crawls Earth→Mars at honest light-speed; on-screen progress == readout)
- [x] **M0-12** Freshness-as-saturation — done (packet amber → machine-grey; freshness gauge in telemetry)

**Exit:** ✅ MET — money shot reads: orrery with Earth/Mars on dashed rings, coloured packet honestly crawling, in 1-bit tiling shell. Vitest green. See `docs/screenshots/`.

---

## Milestone 1 — THE EARTH-ORBIT CONNECTIVITY GAME (kill-gate)

> **v0.3 reshape (`docs/signal-horizon-m1.md` Part I, the new M1 mechanics spec).** M1 is no longer the Mars-cache freshness loop — it is an **Earth-orbit satellite-ISP CONNECTIVITY puzzle** culminating in a fenced Mars/light-delay teaser. The core loop: **accept** a pre-shaped standing SLA contract → **launch** satellites via a consequence-showing planner → **tune-by-exception** a deterministic solver that auto-routes and diagnoses its own shortfalls. The heartbeat is **serve-or-breach** every tick across the SLA axes; the early-game optimisation tension is **oversubscription** (statistical multiplexing of shared links). The hour-long curriculum runs **Acts 1–3** (the buildable Earth game) + **Act 4** (a fenced Mars concepts teaser).
> The freshness/caching loop the old M1 was built around is **reclassified to a post-gate campaign concept**, teased (concepts only) in Act 4 — see RECLASSIFIED below. The existing M1 cache code (`src/sim/m1/*`) is not deleted; it becomes the Act 4 teaser content and is otherwise fenced.
> **Build order:** Act 1 (the immediate next build — a fresh, isolated `src/sim/a1/` world that does NOT touch the M1/M2 sessions or goldens) → the §7 reactive routing-solver epic (M1-SLV-*, now specified in spec v0.2) → Acts 2–3 (which need it) → the Act 4 teaser (reclassify-and-fence, near-zero new code) → the gate.
> **Numbers are all TUNABLE/PLAYTEST-KNOB starting guesses; only the mechanic SHAPES are locked (spec §10).** The mechanic shapes locked by the spec are noted per-ticket as `▸ LOCKED`.
>
> **PLAY-READABILITY PASS — DONE (SD-43, 2026-06-17).** First hands-on session in the live browser (Playwright MCP) surfaced nine readability/feel gaps; all fixed + verified in `vite dev` (net-mode only; cache mode + the 3 goldens untouched; 702 tests green): the cache game no longer leaks into the connectivity opening (log/finance/status/packet all net now); the black SYSTEM/TOP-DOWN views frame Earth; the coverage-disc flicker is gone (deterministic depth stack); a persistent **OBJECTIVE · ACT N** + a full **CONTRACTS** view land in the planner; the **Moon** (stylized) + **continent coastlines** (`coastlines.ts`, Natural Earth 110m) make the globe legible to aim at; onboarding cold-open lightened; per-axis slider captions + Act-1 launch-failure suppression. This is play-tuning, NOT the gate — the M1-GATE run is still owed.
>
> **MISSION-CONTROL UX REDESIGN — Phase 1 DONE (SD-44, 2026-06-17).** Net UX rebuilt as FIVE virtual desktops (keys 1–5: OVERVIEW/CONNECTIVITY/ROUTING/BUSINESS/REFERENCE), each a focused mission-control page; the muddy 7-group NET·LAUNCH panel split into a dedicated **launch menu** (with a "LAUNCHING: … OVER: …" what/where headline) + a separate **contracts board** (BUSINESS) + a **prefer** tile (ROUTING); new status-board/coverage-roster/link-load/howto dashboards; and a **fresh control scheme** (clean net keymap, cache-era shortcuts cut, the C collision killed, accept/constellation/prefer are buttons). Keeps the tiling WM + DD-1 style; render-only; goldens + cache mode untouched. Phases 2–5 (flesh out the new dashboards) remain.
>
> **PHASE 1b — DONE (SD-44, 2026-06-17).** Two long-standing bugs killed: (1) **the limb flicker** — root cause was the lat/lon **graticule built coplanar with the globe surface** (both R 1.0, depthWrite:true) z-fighting under per-frame floating-origin rebase jitter; fixed by lifting the graticule to R 1.0025 + tightening the net-mode camera NEAR plane (~1000× better depth precision, FAR untouched so the Moon/Mars don't clip). Verified flicker-free with a 60fps `gl.readPixels` toggle-scan. (2) **OVERVIEW Earth was a ~3px speck** (`sphereHeightFraction` 0.004) — added a per-desktop **hero globe framing** (`setNetHeroFraming`): OVERVIEW 0.34 / CONNECTIVITY 0.42 / ROUTING pulled back; OVERVIEW now frames Earth as a clear central hero (0.33). Render-only; goldens + cache mode untouched; 702 tests green.
>
> **PHASE 1c — gameplay legibility (SD-44, 2026-06-17).** A live play-through proved the loop is mechanically sound + rewarding (accept→earn→new contract→next objective all fire); the "gameplay sucks" friction was **broken guidance**. Killed the dead-key objective copy that survived the controls redesign ("ACCEPT (K)", "CONSTELLATION (C)" — both keys cut; they're buttons now), rewrote all five objectives **desktop-anchored** to live controls, fixed the howto xref, and made the orrery on-canvas hint **desktop-aware** (tuning keys only on the LAUNCH view; OVERVIEW/ROUTING point to CONNECTIVITY instead). UI copy only; goldens + cache mode untouched.

> **PHASE 2 — DEPTH: Act-2 constellation decision (SD-44, 2026-06-17).** User: "the gameplay needs to be deeper than some inclination sliders." A GDD-grounded design workflow found the sim already models real strategy (oversubscription allocation, SLA penalties, reroute, fault redundancy, phasing/over-build) but every lever hides it. First thread shipped: the one-press PLACE SET → a **held-vs-capex LADDER + SIZE stepper** — the player dials the constellation size, watches worst-phase HELD cross the bar at the measured minimum (`zeroGapN`), and sees over-build sats read as idle capex (trim to the minimum). New pure `phasingLadder()` (+4 tests); launches the chosen size via the existing batch action; **3 goldens byte-identical** (chosen-N is UI-only). Deferred threads: ACCEPT price-the-bet card; PREFER reroute-preview + per-sat allocation ledger.
>
> **PHASE 2b — DEPTH: ACCEPT is a priced bet (SD-44, 2026-06-17).** The offered contract row went from "accept to start earning" to an underwrite: "BET +€R/hr vs −€P/hr if it FAILs" + a pure-preview "fleet HOLDS this ✓ / would BREACH {axis} — fix first or accept the risk" verdict (router.solve + windowAvailability against the live roster). Accepting an un-servable contract is now a deliberate penalty gamble. Render-only per-frame preview; goldens untouched. Next thread: PREFER reroute-preview + per-sat allocation ledger.

### Reused from existing code (the determinism + render backbone carries over wholesale)

The new M1 reclassifies the codebase more than it invalidates it. Directly reused as-is:
- **Determinism backbone** — fixed-tick integer clock (`clock.ts`, `DT=1/60`), seeded splitmix64 RNG (`rng.ts`), action-log + replay harness (`action.ts`, `scheduler.ts`), snapshot/restore, state-hash goldens (`state-hash.ts` `mixFloat/mixInt/mixString`), the `src/sim/` purity discipline.
- **Orbit math** — `SatOrbit` / `solveOrbit` / `orbitPeriodSeconds` / `meanMotion` (`m2/orbit.ts` + `m2/roster.ts`) reused unforked; the shrunk Act-1 orbit family changes only the altitude constant, not the Kepler math.
- **Orrery render pipeline** — floating-origin rebase, dashed orbital-plane rings (`sampleSatOrbitRelative`, `orbit-render-scale.ts`), the footprint shell (`coverage-overlay.ts`), the readout overlay (`readout.ts`), click-to-focus pick (`pick.ts`), the tiling WM + window-summon rail (`shell.ts`, `window-rail.ts`, `PanelHandle`), SYSTEM.LOG (`log.ts`), the status strip.
- **The accept/decline verb** — `apply-build-action.ts` accept/decline actions + the K/J keys + the contracts panel match the spec's "accept-only" floor exactly (spec §4.1). Verb essentially done; only the accepted contract object gains the 3-axis SLA.
- **Launch-failure roll** — `m2/launch.ts:rollLaunch` (flat seeded per-launch failure) is exactly the spec §3.5 flat risk %. (Act 1 ships with no roll for a clean opener, but the `SimRng` seam is threaded; the roll re-arms in late-M1.)
- **The escalation engine** — `coverage/dynamic-demand.ts` (demand grows where you serve, bounded) is the Act 3 endogenous strain, reused directly.
- **The warn+countdown pattern** — the M1-blk conjunction foreshadow nudge (watch-band → SYSTEM.LOG NOTICE + countdown) is the reusable presentation pattern for the Act 3 telegraphed fault.

### Reclassified under the new spec (classification changes, not rebuilds)

- **M1 cache/freshness/prefetch/coherence loop (`src/sim/m1/*`)** — was *M1 — THE FUN GATE (the whole milestone)*; now **Act 4 teaser CONCEPT + otherwise POST-GATE** (spec §8 fenced). The code is excellent and directly serves the Act 4 light-delay/freshness teaser; its class flips from "M1 core" to "Act 4 concepts teaser + deferred." Do NOT build the full freshness/prefetch/coherence economy from the M1 spec — Act 4 teases the concepts only.
- **Coverage hex/geodesic grid (`coverage/grid.ts`, `demand.ts`, `field.ts`, `score.ts`) + cell-targeted contracts** — was *M2a "The Heart"*; now **M2 (fenced OUT of M1)**. M1 demand is region/point endpoints, NOT a per-cell field (spec §8). The grid is reusable only as solver-input geometry; M1's demand shape is incompatible with cell-field demand. Act 1 ships a standalone region-disc endpoint (`a1/region.ts`) that never pulls in `grid.ts`.
- **M2 contracts (`m2/contracts.ts`, single-axis connectivity SLA)** — was *M2d coverage revenue*; now **the M1 §4.1 contract SKELETON, but the 3-axis SLA it must carry is genuinely NEW M1 work**. The state-machine / term / tariff / accrual / breach-grace / offer-generation reuse; the latency + availability + bandwidth SLA fields and the time-varying offered_load do not exist and are the heart of the puzzle.
- **Escalation + emergent-events/rivals bundle (`dynamic-demand.ts`, `m2/events.ts`, `m2/rivals.ts`)** — the bundle SPLITS: **escalation → IN M1 Act 3** (endogenous "demand grows where you serve" strain); **rivals / emergent-narrative generator → DEFERRED to M2** (spec §8 — M1's tension is endogenous, not competitive).
- **Launch-failure flat seeded roll (`launch.ts:rollLaunch`)** — was *M2c launch market*; now **directly-reusable M1 §3.5 launch risk**. The insurance market stays M2-fenced.
- **M3a orbital datacenters / edge-compute (`m3/*`)** — **out of M1 entirely**; no DC/compute concept appears anywhere in the M1 spec. Valid M3 cislunar work, flagged so the backlog does not treat it as M1-adjacent.

### Deferred (spec §8 fences — do NOT build in M1)

- **Freshness / caching / the information economy** (§8) — Act 4 concepts teaser only; the full system is post-gate. (Existing M1 cache code serves the teaser, stays otherwise fenced.)
- **The coverage hex grid** (§8, GDD §4.2) — M2; M1 demand is region/point endpoints. `grid.ts` reusable only as solver-input geometry.
- **Orbit decay** (§8 → §6) — M2 (M1 sessions aren't long enough for lifetime to matter; it would muddy the gate). SGP4 already deferred (Q1).
- **Insurance market** (§8 → §3.5) — M2; a flat per-launch risk % is enough for the gate.
- **Launch-cadence / rideshare → dedicated → owned-launch progression** (§8, GDD §4.7) — M2.
- **Interplanetary launch windows** (§8) — M2/M4 (`launch.ts:launchWindowOpen` is a reserved no-op hook returning true).
- **BGP peering / trust / inter-operator economics** (§8 → §7) — later; M1 routing is OSPF-interior over your OWN fleet only.
- **The leverage curve / tech-tree depth** (§8, GDD §4.11) — post-gate; M1 ships only the minimum bus/antenna unlock ramp the session needs, not a designed tree.
- **Rivals / emergent-narrative generator** (§8, GDD §3) — M2; `events.ts` + `rivals.ts` stay fenced (escalation splits out into Act 3).
- **Hard random failure in M1** (§5.1 #4) — OPEN DECISION (§10), leaning rare-in-late-M1; confirm at playtest. Full force is M2. See ticket M1-FLT-04.

### EPIC — §7 routing solver (RESOLVED in spec v0.2; M1 = reactive + the predictability seed)

> Spec §7 (now fully specified, v0.2 — no longer user-owned). A **deterministic shortest-path-over-time-varying-graph solver** (Dijkstra/A\* over the current line-of-sight adjacency), re-solved on **topology-change events** (a link enters/leaves view, a fault, a demand change) — NOT every tick, NOT a real routing daemon (determinism + golden-master preserved, §7.1 `▸ LOCKED`). Link cost is a **physics-blend** `w_lat·latency_term + w_bw·congestion_term + w_stab·instability_term`: the `*_term`s are physics (truth, not tunable); the `w_*(class)` weights are the design surface, set per traffic-class so demand-shape produces topology-shape (§7.2 `▸ LOCKED`).
> **M1 SCOPE (§7.5 `▸ LOCKED`): reactive only — `w_stab = 0` (latency + congestion); the cost-blend + per-contract weight present but the stability term DORMANT; the three control tiers at least floor (auto-solve) + basic ceiling (the per-contract prefer-latency↔bandwidth↔stability slider — the FIRST tunable, §7.3); the self-diagnosing trace view (M1 NECESSITY, §7.4); and the PREDICTABILITY SEED planted (the trace stamps every link loss with its geometric cause + time, §7.5).** Act 1's `a1/reachability.ts` bent-pipe stub is the seam the solver subsumes (same `(eph, region, ground, sats, t) → {reachable, latency}` shape). Acts 2–3 *need* this epic (a real dependency, no longer user-owned).
> **Deferred to M2+ (§7.5/§7.6, recorded so the M1 seeds know where they lead):** the stability term `w_stab > 0`, predictive routing as a *discovered* capability, the efficiency-vs-resilience / fragility-cascade tension, the routing↔fault coupling (§5), BGP-style peering/policy across other operators, and the full visual-constructor depth.

- [ ] **M1-SLV-1** Deterministic time-varying-graph solver core (reactive) — *L* · `▸ LOCKED (§7.1)`: sats = nodes, LoS links = edges; Dijkstra/A\* over the current adjacency; re-solve on **topology-change events** (link in/out of view, fault, demand change), not per tick; pure + replay-safe (folds into the session golden). · Dep: M1-SAT-3 (the CROSSLINK/LASER sat-to-sat links are the edges), the A1 reachability single-hop stub it generalises. · Reuse: the A1 elevation+budget gate per edge; the event-driven re-solve fits the fixed-tick clock.
- [ ] **M1-SLV-2** Physics-blend link cost + per-contract weight (`w_stab` dormant) — *M* · `▸ LOCKED (§7.2): cost = w_lat·latency + w_bw·congestion + w_stab·instability; the *_term are physics-truth; weights per traffic-class. M1: w_stab=0`. The first player tunable is the per-contract prefer-latency↔bandwidth↔stability weight (§7.3; the `w_stab` control is present but dormant). · Dep: M1-SLV-1, M1-CON-1 (traffic class on the contract). · Reuse: A1 bent-pipe latency as the `latency_term` seed.
- [ ] **M1-SLV-3** Three control tiers — floor + basic ceiling (+ terminal) — *M* · `▸ LOCKED (§7.3)`: floor = auto-solve (the whole early game plays here); ceiling = the per-contract weight slider that *visibly moves the path* (the Cisco-Packet-Tracer reference for fun); power-user = FRR-style terminal mapping onto our params (e.g. `set latency-weight 100 on contract-7`). M1 ships at least floor + basic ceiling. · Dep: M1-SLV-2. · Reuse: the panel + slider chrome.
- [ ] **M1-SLV-4** Self-diagnosing trace / shortfall view (M1 NECESSITY) — *L* · `▸ LOCKED (§7.4)`: when the solver can't meet a contract, surface the **binding constraint + the kind of fix** ("availability breaks 8min/orbit: need ≥1 more sat in this plane"; "latency floor 340ms via 4 LEO hops: a GEO relay at X → 180ms"; "trunk saturated: add a parallel path"). Turns "solver says no" into "I launch THAT"; also shows fault state (§5.3 — one legibility system). · Dep: M1-SLV-1. · Reuse: the M1 PARSE panel pattern (`src/panels/parse.ts`) as the review/diagnose template.
- [ ] **M1-SLV-5** The predictability SEED (REQUIRED in M1; §7.5) — *S* · `▸ LOCKED`: the trace stamps every link loss with its **geometric cause + time** ("link SAT-7↔SAT-12 lost: SAT-12 set below horizon at 14:32") — a time, a cause: geometry not gremlins. Cheap (the sim knows the geometry) and the prerequisite for the POST-M1 predictive-routing a-ha. **Act 1 plants the first grain:** the LEO-set / region-dark log stamps the geometric cause + time. · Dep: A1 serve/breach logging (the first grain), M1-SLV-1 (the general case). · Reuse: SYSTEM.LOG + the event-log plumbing.

---

### EPIC — M1 as ONE game · Phase A: shared systems + the Act 1 beat (`src/sim/net/`) · ↞ IMMEDIATE NEXT BUILD

> **Reframed (SD-40, `docs/signal-horizon-m1.md` Part II).** M1 is ONE game — the real connectivity/coverage + launch-planner + routing-solver + fault systems under a curated, **state-gated** arrival sequence (the contract is the teacher; only the *order* contracts/faults arrive is scripted; the systems run for real). The four acts are **gated beats**, not prototypes. This **replaces the isolated-Act-1 contraption** epic (the old A1-0a…A1-9: bent-pipe stub, co-located ground, dish-EIRP closing-lever, the `windowCoverage`/0.85-bar forced imperfection — all DROPPED). New namespace **`src/sim/net/`** (the old `src/sim/m1/` cache economy is the reclassified Act-4 teaser, left alone; the `m1/`→`m1-economy/` rename is DEFERRED). Own `NetSession` + own golden (`4242424242424242n`); the two existing goldens untouched.
> **Act 1 is WINNABLE BY THE DEFAULT** (onboarding script, the gentlest beat): equatorial region, the parked GEO covers the **whole** disc (`coveredFraction === 1.0` pinned, no edge-clip), one connectivity-only contract (avail/latency/bandwidth present in the struct but HIDDEN via an `activeAxes` mask), one verb (LAUNCH), gate = served + revenue positive. **No forced imperfection.** KEEP from the stopped `a1/` build: the spinning-Earth frame, the toy-radius GEO-parks pacing, the sat atom. Phase A (A0–A4) is the smallest provable-fun slice (all Act-1-shippable); Phases B/C/D (the Act 2/3/4 epics below) add the later beats on the SAME systems. Full plan: the design doc §7.

- [ ] **A0** Namespace + migrate atoms + re-center the region — *M* · create `src/sim/net/`; migrate `frame.ts`/`world.ts`/`sat.ts` (drop `A1_DISH_EIRP`) + the geometry half of `region.ts → endpoint.ts` (drop co-located ground + `windowCoverage`/`A1_AVAILABILITY_BAR`); **re-center the Act-1 region to lat 0** + add `NET_ACT1_REGION_RADIUS_RAD`/`NET_MIN_ELEVATION_RAD`; carry `frame`/`pacing`/`region`/`purity` tests, remove `a1/`. · Reuse: the stopped `a1/` frame/world/sat. · Verify: migrated physics-gates + purity green under `net/`; **both existing goldens pass byte-for-byte.**
- [ ] **A1** Link budget + router (path-existence) + the WHOLE-DISC pin — *L* · `link-budget.ts` (elevation + inverse-square + LoS on the spinning frame, reusing `field.ts` formulas with `net/` constants) + `router.ts` `solve()` degenerate path-existence (region→sat→groundNet) → `{served,path,latencyS,bindingConstraint,losses}`; the re-solve split (per-tick cheap predicate re-eval incl. horizon rise/set; event-driven Dijkstra). Replaces `a1/reachability.ts`. · Dep: A0. · Reuse: `coverage/field.ts` formulas. · Verify (HIGH must-fix): **`coveredFraction === 1.0` at eirp 1.0 over the equatorial region (whole disc, no clip) + a worst-sample margin**; a single LEO is NOT served (it sets) + re-solves UNSERVED + a link-loss stamp records the geometric cause + time.
- [ ] **A2** Contract + session + apply-action (SHARED m2 state machine) — *L* · `contract.ts` (3 axes + `activeAxes` mask + `prefer` + `SLA_AXIS_ORDINAL`); `session.ts` (`NetSession.step` derives `servedFraction` from the router, calls the **imported** `stepActiveContract`/`stepOfferedContract`/`BREACH_GRACE_SECONDS` from `m2/contracts.ts`); `apply-action.ts`; action kinds `net_launch` (radians/SI + `count`)/`net_accept`/`net_set_prefer`. Log the contract-reuse decision in `decisions.md`. · Dep: A1. · Reuse: `m2/contracts.ts` helpers. · Verify: accept→serve→€ loop on ONE state machine; DT-invariant revenue; the shared `BREACH_GRACE_SECONDS` (not a `net/` copy).
- [ ] **A3** Scenario engine + Act-1 beat + the M1 golden — *L* · `scenario.ts` (the state-gating engine: pure data table + deterministic gate evaluator; the `act1` beat emit/gate/fallback; `act3a`/`act3b` as SEPARATE cursor placeholders) wired into `step`; `src/sim/net-replay.test.ts` (seed `4242424242424242n`, fold incl. the fixed-ordinal `activeAxes`, pinned golden). · Dep: A2. · Reuse: `state-hash.ts` mix primitives, the `m2-build-replay.test.ts` template. · Verify: replay-twice bit-identical + live==replay + JSON round-trip; the Act-1 gate fires deterministically; **both old goldens untouched.**
- [ ] **A4** Act-1 planner + visualization — *L* · `previewLaunch` (truthful footprint/track/period/latency preview via the router); orrery **net render mode** (the Decision-G toy-radius de-squash scoped to net mode, M2/M3 untouched); region light-up; the launch→cover→paid causal chain; finance tick. · Dep: A3. · Reuse: `orrery.ts`, `coverage-overlay.ts`, the panels. · Verify: planner preview == post-launch solve (consequence-truth); net mode doesn't alter M2/M3 sizing. **Acceptance (Act-1 gate, miniature): a cold tester presses the default LAUNCH and is paid; the launch→cover→paid chain reads. ← Act 1 SHIPS here.**

---

### EPIC — Act 2: Connectivity + the constellation (the Earth game proper) · ↯ needs the §7 solver epic (M1-SLV-*)

> Spec §9 Act 2 (~10–30 min). A new region a single sat CAN'T hold: the player hits the **AVAILABILITY wall** (one sat covers only while overhead) and must build a constellation; the planner assists with phasing (viable-but-imperfect, a playtest knob); the player watches hand-offs as sats rise/set; the path forms, breaks, re-forms. First **oversubscription** pressure (a second contract sharing infrastructure). Teaches availability → constellations, the planner's ceiling, sharing scarce capacity.

- [ ] **M1-A2-1** Availability SLA axis (path holds continuously as sats orbit) — *L* · `▸ LOCKED` axis-2 of the §4.4 ramp (introduced after connectivity, before latency/bandwidth). Per-PATH (not per-cell) continuous-hold evaluation; the most common breach. · Dep: A1 epic, M1-SLV-1. · Reuse: the A1 window-coverage min over an orbit window is the seed; the per-path eval belongs to the solver. `↯ needs the §7 solver epic (M1-SLV-*)`.
- [ ] **M1-A2-2** Constellation phasing assist (viable-but-imperfect) — *M* · `▸ PLAYTEST KNOB` (degree-of-assist, §3.3/§10 — lean viable-but-flawed, never perfect): the planner suggests a phased plane so a batch of N sats gives continuous coverage; the player tunes the hand-off. · Dep: M1-A2-1, A1-6/7. · Reuse: the A1 planner + PHASE slider (now previews against the whole constellation).
- [ ] **M1-A2-3** Hand-off as a taught beat — *S* · render/UX: sats visibly rise/set, the path/beam migrates between sats, the served region holds green across the hand-off (or dips amber on a mis-phase). · Dep: M1-A2-2, A1-8. · Reuse: the orrery beam + region-pulse machinery.
- [ ] **M1-A2-4** First oversubscription pressure (shared infrastructure) — *M* · a SECOND contract sharing a backbone link; the player feels capacity is finite and must judge how thin to cut it. Lighter than the Act 3 full bite. · Dep: M1-OSUB (below), the antenna link-capacity model (M1-SAT epic). · Reuse: the A1 contract struct + accrual. `↯ needs the §7 solver epic (M1-SLV-*)`.

---

### EPIC — Act 3: Strain, faults, and re-taming · ↯ needs the §7 solver epic (M1-SLV-*)

> Spec §9 Act 3 (~30–50 min). **ESCALATION:** success grows demand on served regions (network effect) and congests shared paths → satisfied contracts ride close to breach; re-engineer/launch more (the first full tame→outgrow→re-tame cycle). **LATENCY + BANDWIDTH** SLAs now bite together (the GEO ~240ms-round-trip ceiling is felt; antenna/link limits force parallel paths / better hardware). The **chaos-kitten arrives mild-first**: a degradation, then a telegraphed fault. The **optimizer pull** appears via the diagnostic/trace view (overprovisioned links, single points of failure). Teaches: the loop sustains past novelty; build resilient not just cheap; the parse is worth reading.

- [x] **M1-A3-1** Endogenous escalation in M1 (demand grows where you serve) — *M* · **DONE (SD-40-C1b).** The escalation law lives on `NetSession`: a well-served contract's `offeredLoad` grows by the EXACT closed-form logistic flow (the `coverage/dynamic-demand.ts` semigroup, DT-invariant, bounded) toward a cap, gated ON by the act3a beat. Two-pass per-step `loadBySat` aggregation + a quantized `congestionEpoch` (E3) make a rising load congest the shared link + force a re-solve through the cache. · Reuse: the `dynamic-demand.ts` closed-form growth (mirrored, not imported — net/ stays pure).
- [x] **M1-A3-2** Latency SLA axis + the GEO ceiling — *M* · **DONE (SD-40-C1a router; C1b scenario).** Axis-3 of the §4.4 ramp: a path with `latencyS > slaLatencyS` does NOT satisfy a latency-active contract (binding=latency) — the parked GEO (~3.57 ms) fails the act3a corridor's low SLA, a short equatorial LEO (~2.07 ms) passes (the GEO ceiling, felt). Arrives by the AUTHORED REGION-2 corridor contract.
- [x] **M1-A3-3** Bandwidth SLA axis (capacity / link limits) — *M* · **DONE (SD-40-C1a router; C1b escalation).** Axis-4: the chosen bridge's shared load `≥ NET_LINK_CAPACITY_UNITS` ⇒ binary-unserved (binding=bandwidth — the HIGH-1 fix so `breachSecondsAccum` accrues). Arrives by the escalation law crossing capacity (the §4.4 escalation-triggered mask flip), ONE AT A TIME after latency. The relief is a parallel path / a per-contract `net_set_prefer` override (the §7.3 tune-by-exception).
- [ ] **M1-A3-4** The diagnostic / trace view (the optimizer pull) — *L* · the solver diagnoses its own shortfall (binding constraint + kind of fix → "solver says no" becomes "I launch THAT"); surfaced as a trace view showing overprovisioned links + single points of failure. The "do it better" parse pattern is reusable here. · Dep: M1-SLV-1. · Reuse: the M1 PARSE panel pattern (`src/panels/parse.ts`) as the "review-at-rest, do-it-better" template. `↯ needs the §7 solver epic (M1-SLV-*)`.

---

### EPIC — Act 4: The Mars frontier teaser (concepts only, fenced §8) · ↞ near-zero new code

> Spec §9 Act 4 (~50–60 min). The session CULMINATES reaching toward Mars; the Earth playbook BREAKS (first across-tier invalidation). The player places the first object near Mars, sends the first signal, and watches it crawl (round-trip now MINUTES) — light-delay taught by sight. The freshness CONCEPT (not the economy) appears: Mars data arrives old ("as of 8m ago"); one demand cares about freshness; the player gets ONE cache to place and feels "putting data closer helps" (the breadcrumb). Stops on purpose on a "to be continued." Teaches the campaign hook: light is slow, distance changes everything, caching is the answer, freshness is the new currency-to-be.
> **▸ This is the strongest reuse in the whole spec: the existing M1 cache/freshness/light-delay/Mars-blackout system IS the Act 4 teaser content.** The net-new work is FENCING/scoping ONLY — present it as a teaser (one cache, "as of 8m ago", a single freshness-caring demand, stop on "to be continued"), NOT the full caching/prefetch/coherence economy that's already coded. The risk is over-exposing it.

- [ ] **M1-A4-1** Reach Mars + light-delay by sight — *S* · place the first object near Mars; the packet crawls at honest light-speed (round-trip now minutes); the player feels the helplessness of not being able to real-time-tune an 8+ min command-delay topology. · Dep: Act 3 (the playbook to break). · Reuse: **full** — the existing Earth↔Mars packet crawl + light-delay readout (`M0-10/11`).
- [ ] **M1-A4-2** Freshness CONCEPT (not economy) + the one cache breadcrumb — *S* · Mars data arrives old ("as of 8m ago"); ONE demand cares about freshness (pays less for stale); the player places ONE cache and feels "data closer helps." · Dep: M1-A4-1. · Reuse: **full** — the existing cache + freshness-as-saturation + Mars-blackout (`src/sim/m1/*`), FENCED down to a single cache + a single freshness demand. **Do NOT expose the full prefetch/coherence/policy/parse toolkit** (it stays post-gate per §8).
- [ ] **M1-A4-3** "To be continued" stop — *S* · the teaser ends on purpose; the player should finish WANTING to see where it goes. · Dep: M1-A4-2. · Reuse: the SYSTEM.LOG + readout for the closing beat.

---

### CROSS-CUTTING EPIC — The launch planner (the ceiling) · spec §3 / §3.1–§3.5

> The launch verb is NOT piloting (no ascent minigame) — it is choosing orbit + loadout via a planner that shows **truthful, predictable consequences before commit**. The floor is "pick a preset, go"; the ceiling drags altitude, inclination, phase, RAAN. The first two parameters exposed are **altitude and inclination** (Act 1 exposes inclination + phase). Cost = f(payload mass, destination Δv); risk = a flat per-launch failure probability. Act 1 builds the floor (A1-6/7); this epic carries the ceiling forward.

- [ ] **M1-PLN-1** Consequence-preview principle, generalised — *M* · the pre-commit preview (footprint / ground-track / period / latency floor / coverage-gap overlay, commit-on-confirm) generalised beyond Act 1's single region/preset. `▸ the §10 degree-of-assist is a PLAYTEST KNOB`. · Dep: A1-6/7. · Reuse: the A1 planner panel + `Orrery.setLaunchPreview` as the foundation.
- [ ] **M1-PLN-2** Cost = f(payload mass, destination Δv) — *S* · `▸ TUNABLE`: replace the flat preset € with a mass×Δv model; mass comes from the bus tier. · Dep: A1-1 (cost stub), the bus-tier model (M1-SAT epic). · Reuse: the A1 `a1LaunchCost` scalar as the seam.
- [ ] **M1-PLN-3** Ceiling parameters — altitude / phase / RAAN — *M* · expose the remaining orbit knobs beyond Act-1's inclination+phase; preset floor stays the easy default. · Dep: M1-PLN-1. · Reuse: the radians-at-the-boundary action payload + `resolveA1Orbit` pattern.
- [ ] **M1-PLN-4** Flat per-launch failure risk re-armed — *S* · `▸ TUNABLE, low enough not to feel arbitrary`: re-arm the seeded launch-failure roll (Act 1 ships clean for the opener). · Dep: A1-5 (the threaded `SimRng` seam). · Reuse: **full** — `m2/launch.ts:rollLaunch` is exactly the spec §3.5 flat risk %.

---

### CROSS-CUTTING EPIC — The satellite atom + contract/SLA model · spec §1 / §4

> The composable atom (orbit + bus tier + typed antenna slots) and the standing 3-axis SLA contract are the substrate the whole connectivity game composes from. Act 1 ships the minimum shape (one tier, two antennas, single-axis); this epic grows it to the full spec model that Acts 2–3 need.

**The satellite atom (spec §1):**
- [ ] **M1-SAT-1** 4 bus tiers with fixed G/S slot counts — *M* · `▸ LOCKED SHAPE (access→distribution→backbone, laser scarce + high-tier-only); numbers TUNABLE`: Smallsat / Comsat / Trunk-sat / Backbone as data (slot counts, mass guesses, identity); M1 uses mostly tiers 1–2, tiers 3–4 stub the LASER slot. · Dep: A1-1 (the `smallsat` minimal atom). · Reuse: the A1 `BusTier` enum + `loadout` shape.
- [ ] **M1-SAT-2** 2 slot classes (G ground / S sat-link) + validation — *S* · `▸ LOCKED`: G holds BROADCAST/ACCESS/GATEWAY; S holds CROSSLINK or (if unlocked + tier permits) LASER. Pure validation, fits the purity discipline. · Dep: M1-SAT-1. · Reuse: none (small pure rule set).
- [ ] **M1-SAT-3** 5 antenna types incl. BROADCAST asymmetry + sat-to-sat links — *L* · `▸ LOCKED: the five types, their roles, and BROADCAST's down-only asymmetry (what makes GEO a broadcaster); numbers TUNABLE`. BROADCAST/ACCESS/GATEWAY (G), CROSSLINK/LASER (S). **The sat-to-sat CROSSLINK/LASER relay links are wholly new — the substrate the routing solver consumes.** · Dep: M1-SAT-1/2; feeds M1-SLV-1. · Reuse: the A1 BROADCAST/ACCESS specs as the seed; the field.ts inverse-square formula for the new inter-sat link.
- [ ] **M1-SAT-4** 3 orbit-regime tradeoffs bound into evaluation — *M* · `▸ LOCKED: GEO ~240ms round-trip is a HARD floor; numbers TUNABLE`: bind LEO/MEO/GEO → latency-floor / per-user-bandwidth / motion into the contract-evaluation path (the geometry already exists; the gameplay-meaningful tradeoffs do not). · Dep: M1-SAT-3, M1-A3-2. · Reuse: `ephemeris.ts`/`m2/orbit.ts` propagation + the orrery rings (geometry reuse).

**The standing 3-axis SLA contract (spec §4):**
- [ ] **M1-CON-1** 3-axis SLA contract struct (latency + availability + bandwidth) — *M* · `▸ LOCKED: demand is a standing SLA paid-while-met / breached-when-not, NOT discrete packages; the player only ACCEPTs (no negotiation)`. Endpoints are region/point A↔ground or A↔B (NOT grid cells). · Dep: A1-4 (the single-axis struct already carries the fields). · Reuse: the A1 `A1Contract` struct (fields pre-shaped) + `m2/contracts.ts` state machine.
- [ ] **M1-CON-2** Serve-or-breach heartbeat across all 3 axes — *M* · `▸ LOCKED`: MET iff a path satisfies latency AND availability AND bandwidth simultaneously; breaches the moment any axis fails; the per-tick truth source moves from coverage-field to the solver-path. · Dep: M1-CON-1, M1-SLV-1. · Reuse: the A1 per-tick accrual loop (predicate replaced). `↯ needs the §7 solver epic (M1-SLV-*)`.
- [ ] **M1-CON-3** Reconcile the breach penalty (pay-pauses vs €/sec drain) — *S* · `▸ spec §4.1 says reputation loss / drop, not a €/sec drain`: confirm Act 1's "breach pauses pay" against spec and apply consistently (the M2 €/sec drain is the divergence to reconcile). · Dep: A1-4. · Reuse: the A1 contract tick.

**Oversubscription (spec §4.3):**
- [ ] **M1-OSUB** Time-varying offered_load + shared-link statistical multiplexing — *L* · `▸ LOCKED: real statistical multiplexing — "N contracts, M sats, honest provisioning needs ~2M sats, how cleverly do you share?"; variance is a PLAYTEST KNOB`. offered_load varies and can exceed sla_bandwidth; share a backbone across non-coincident peaks; cut it as thin as you dare before a breach costs more than the hardware saved. The early-game's headline skill. · Dep: M1-SAT-3 (link capacity), M1-SLV-1 (allocation across shared links). · Reuse: none (M2 escalation is monotonic, not bursty). `↯ needs the §7 solver epic (M1-SLV-*)`.

---

### CROSS-CUTTING EPIC — The fault spectrum (the chaos kitten, mild-first) · spec §5

> Four faults, introduced mild-first in Act 3, split into causal (your-success-driven) + rare-random mechanics. No asset-health/fault model exists today; the event-stream + SYSTEM.LOG plumbing is reusable to SURFACE them, and the conjunction foreshadow nudge is the reusable warn+countdown pattern for the telegraphed fault. `▸ LOCKED: the spectrum, causal+rare-random as distinct mechanics, mild-first introduction; all rates / multipliers / the rare-random floor / WHEN faults first appear are PLAYTEST KNOBS`.

- [ ] **M1-FLT-01** Degradation (recoverable, self-recovers, unwarned) — *M* · the FIRST fault the player meets (Act 3); teaches capacity headroom; bites the player who over-cut oversubscription. Surfaced on the orrery (amber pulse) + SYSTEM.LOG. · Dep: M1-OSUB (it pairs with oversubscription). · Reuse: the event-log + orrery-cue surfacing plumbing (the fault model itself is fresh).
- [ ] **M1-FLT-02** Transient outage (recoverable brief, unwarned) — *M* · teaches the need for a backup PATH; the solver's self-healing reroute proves itself. · Dep: M1-SLV-1 (the reroute demonstration). · Reuse: none (the teaching point is solver-bound). `↯ needs the §7 solver epic (M1-SLV-*)`.
- [ ] **M1-FLT-03** Telegraphed failure (warned + countdown) — *M* · the "telegraphed event"; teaches watch-fleet-health + act-on-early-warning fairly (it's warned); countdown shown in the diagnostic view / SYSTEM.LOG. · Dep: Act 3. · Reuse: **partial** — the M1-blk conjunction foreshadow nudge (watch-band → SYSTEM.LOG NOTICE + countdown readout) is a proven warn+countdown pattern; the fault itself is fresh.
- [ ] **M1-FLT-04** Hard failure (permanent, unwarned) — *M* · `▸ OPEN DECISION (§10) — whether even a taste belongs in M1; leaning RARE-in-late-M1, confirm at playtest`. The single-point-of-failure enforcer: only built-in redundancy survives. · Dep: Act 3. · Reuse: **partial** — the seeded launch-failure roll PATTERN for the rare-random floor; the live-asset removal + redundancy-survives logic is fresh.

---

**Gate (sharpened, spec §9 two-layer success criterion):** ≥5 testers cold, the full hour (Acts 1–3 buildable + Act 4 teaser). The hour (not half-hour) forces the loop to carry past novelty and lets one full **tame → outgrow → re-tame** cycle complete. PASS requires: (1) the player completes the Act-1 "I made signal reach there *and held it*" hit by hand-aiming an orbit (no auto-pick); (2) the Act-2 **availability wall** lands — the player feels a single sat can't hold it and builds a constellation; (3) Act 3's tame→outgrow→re-tame cycle fires (success grows demand, the latency/bandwidth SLAs + the mild-first chaos kitten bite, the player re-engineers) AND the **optimizer pull** fires (the player wants to read the trace/parse and do it better); (4) the Act-4 teaser lands the campaign hook (light is slow, distance changes everything) and the player finishes WANTING to see where it goes. FAIL = the connectivity loop reads as a screensaver / "a spreadsheet" / nothing is the player's own decision → iterate the planner-legibility + the solver-shortfall diagnosis + the fault tuning. **The human gate is the user's to run.**

---

## Milestones 2–6 (headlines only — detail after M1 PASS)

- [x] **M2 — Earth tycoon vertical slice:** coverage grid, coverage field, link budget, placeable assets, launch market, contracts state machine, escalation engine, emergent-event generator v1, one coverage heatmap. **COMPLETE (M2a–M2f, SD-27..SD-32):** the full GDD §9 M2 scope, built + adversarially verified on a separate deterministic M2 world (own golden `6225853297339560787n`; M1 golden `544847093270497462n` untouched). The loop is live: build the coverage monument → serve contracts for € → demand grows where you serve (escalation) → emergent shocks/rivals perturb it. _Owed/deferred (later M2 polish or M3+): Level-1 routing policy + the trace view (the §4.3a/§5 seed the plan lists under M2); deep rival-competition AI; globe-raycast placement; the connectivity-ramp + escalation-bite tuning; demand-overlay colour channel._
  - [x] **M2a — geodesic coverage grid + coverage field (GDD §4.2 "The Heart", SD-27).** Pure, standalone `src/sim/coverage/`: roll-your-own subdivided-icosahedron grid (`grid.ts`, triangular faces, 20·4^level, default level 2 → 320 cells, symmetric degree-3 adjacency, areas sum to 4π), deterministic placeholder demand field (`demand.ts`, lat-band + fixed hotspots, no RNG), `coverageOf(cell, assets, t)` over the §4.2 dimensions connectivity/bandwidth/latency with the LoS-horizon + inverse-square/min-elevation link budget reusing the ephemeris (`field.ts`), and the demand-weighted multi-axis scoring stub (`score.ts`). NOT wired into M1Session → replay golden `544847093270497462n` untouched. M2b renders it.
  - [x] **M2b — coverage heatmap render (GDD §5 view #2, the monument begins, SD-28).** Render-only `src/orrery/`: `coverage-overlay.ts` builds a 320-triangle shell ONCE from the grid's corner unit-vectors, floating-origin-rebased to hug Earth a hair above the surface; `heatmap-color.ts` (pure, tested) maps each cell to a §8 hue ramp by the selected dimension — connectivity=cyan / bandwidth=green / latency=amber (lower=hotter), uncovered cells dark-desaturated (a visible hole in the web), CVD-redundant via brightness × opacity. Live coverage swept each frame from the 4 dataset sats via the allocation-free `coverageDimsAt` fast path (parity-pinned to `coverageOf`); X-02 honoured (geometry built once, colour buffer rewritten in place). Keys **H** toggle / **D** cycle dimension; footer + readout show the active dimension. Render-only → golden `544847093270497462n` untouched. 355→369 tests; screenshot-verified (cyan connectivity + green bandwidth shells on Earth, the dimension cycle, clean toggle-off). _Tuning (placeholders, flagged): CONNECTIVITY_HOT=3 reads flat at 4 sats (→2 once more assets exist); shell prominence is modest at curated camera distances (a dedicated Earth-coverage framing is later polish); coverageCellColor allocates ~320 tiny objects/frame (negligible)._
  - [x] **M2c — build-the-monument loop: placeable assets + launch market (GDD §3/§4.7, SD-29).** New separate deterministic M2 world `src/sim/m2/` (`roster.ts` placeable ground/sat roster; `orbit.ts` pure Keplerian propagation for launched sats — own elements, not in the shared ephemeris; `launch.ts` market = 3 presets LEO/MEO/GEO with €cost + seeded failure roll; `sites.ts` candidate sites + starter; `session.ts` `BuildSession` = roster + own €5000 wallet + seeded PRNG, snapshot/restore; `apply-build-action.ts` shared applier). Keys **B** deploy (€250) / **L** launch (€600–1800, charged win-or-lose) / **;** cycle preset. The M2b heatmap + `scoreCoverageAt` read the LIVE roster so coverage GROWS as you build; readout shows `DEMAND NN% · Ngs/Nsat · €NNNN [OVERSPENT]`. Own replay golden `2503511112643458855n` (records deploys + launches incl. a seed-failure; LIVE==REPLAY + coverage-grows invariant); **M1 golden `544847093270497462n` untouched**. 369→393 tests; screenshot-verified (built 6gs/4sat, coverage 9%→76%, €‑3200 overspend with an exact-math seeded launch failure). _Minors: altitudeKm holds semi-major axis (rename owed); constellation-spread wrap; body-fixed frame (inherited)._
  - [x] **M2d — contracts state machine + coverage revenue (GDD §4.9/§3, SD-30).** Closes the gap→asset→integration→revenue loop. New pure `src/sim/m2/contracts.ts` (Contract = region cells + quality + tariff + term; OFFERED→ACTIVE→COMPLETED/FAILED) + `contract-generator.ts` (deterministic seeded offers off the session PRNG) + `servedFractionAt` (region demand-weighted served fraction over the live roster). New `BuildSession.step(eph,t,dt)`: continuous DT-invariant revenue (€/s = tariff × servedFraction, −1.5/s breach penalty) summed into the wallet. Actions `accept_contract`/`decline_contract` via the shared applier; keys **N** cycle / **K** accept / **J** decline. CONTRACTS board panel (WM preset **key 7**), §8 chrome, colour-redundant STATE words. M2 golden re-pinned `2503511112643458855n`→`5706799219860839795n` (LOOP-CLOSES + DT-invariance + decline tests); **M1 golden `544847093270497462n` untouched**. 393→408 tests. Loop closes (verified): golden replay €5000→−€350→+€2398→**€2048 net-positive**; live run €1304→€6192 serving. Screenshot-confirmed the board + the breach/fail path + exact capex wallet math; active-serving wallet-climb verified numerically (blind keypress couldn't align coverage-region to offer cadence at 1000×). _Minors: latent dt-dependent generator re-arm (unreachable at current cadence); sub-tick accept timing ≤1 tick (bit-identical at live DT)._
  - [x] **M2f — emergent-event generator v1: rivals + news shocks (GDD §3 / Risk-7, SD-32).** New pure `src/sim/m2/events.ts` (M2Event: demand_shock / rival_action / news + M2EventLog) + `event-generator.ts` (deterministic seeded, first at 2400s, ~4h cadence) + `rivals.ts` (3 named operators with §8 identity hues — HELIX RELAY violet / MERIDIAN LINK orange / PALE BLUE NET blue). World coupling: a demand_shock applies a decaying per-cell multiplier overlay on DynamicDemand (ripples through scorers/contracts, expires to ×1.0, never inflates the bounded growth); a rival relay_failure spawns a real premium contract (`r{N}`, ×1.6). Surfaced §8-truthfully in SYSTEM.LOG with faction-coloured names via `log.renderM2()`. M2 golden re-pinned `15734905161678697793n`→`6225853297339560787n`; **M1 golden untouched**. 418→427 tests; screenshot-verified (SOUTH-AMERICA dust storm + a `✕ MERIDIAN LINK RELAY FAILURE … their customers come knocking` line in orange). _Deep rival-competition AI deferred to M3+; flavour numbers/cadence are placeholders._
  - [x] **M2e — the escalation engine: demand grows where you serve (GDD §3b gen 1 / Pillar 6, SD-31).** New pure `src/sim/coverage/dynamic-demand.ts`: per-cell demand grows logistically toward a 3× cap where served (quality≥bar), decays toward the M2a baseline floor where not — integrated by the EXACT closed-form flow (DT-invariant semigroup, bounded, no RNG). A `DemandReader` interface makes static+dynamic drop-in for the scorers. `BuildSession.step` advances growth on a 60-s sim-cadence (sweeps served-quality via the allocation-free gates), folds the dynamic field into the snapshot/state-hash. Escalation verified: fixed roster → total demand 154→392 (2.54×, capped), covered fraction eroded 0.546→0.507; adding capacity restored 0.914 (the renewing OpenTTD cycle). M2 golden re-pinned `5706799219860839795n`→`15734905161678697793n`; **M1 golden untouched**. 408→418 tests. Orrery readout `COVERED NN% · DEMAND·GROWTH +NN%`; screenshot-verified `COVERED 81% · DEMAND·GROWTH +55%`. _Minors: coexisting static fallback field in orrery; cadence-divides-dt DT caveat (honored by the fixed-DT callers); gentle per-sitting bite (−0.039/41sim-hr) — placeholder constants._
- [~] **M3 — Cislunar on-ramp:** Moon + L-points, first orbital datacenter, basic autonomy policies, observation contracts, light-delay teaching beat, DC thermal model.
  - [x] **M3a — the first orbital datacenter + the thermal/power model (GDD §4.5, SD-33).** New pure `src/sim/m3/datacenter.ts` (the §4.5 node + physics: POWER ∝ 1/d² solar flux × panelM2, +RTG constant floor; COOLING = radiative-only cap ∝ radiatorM2; COMPUTE = `min(power-limited, thermal-limited)` — both ceilings bite; `computeLiftMultiplier` a capped ≤+40% saturating force-multiplier) + `dc-roster.ts` (placement + the best-of footprint lift, max not sum) + `dc-sites.ts` (candidate list, `DC_CAPEX_EUR` €6,000). Wired into `BuildSession`: a logged `place_dc` capex action via the shared applier; the DC lifts the SERVED revenue of contracts in its sub-point footprint (DT-invariant), folded into the snapshot/state-hash. Worked numbers: Earth 450 W/8.3 u (thermal-limited); Mars solar-only 195 W/3.9 u (power-starved); Mars+RTG 1395 W/8.3 u (rescued). M2 golden re-pinned `6225853297339560787n`→`8431658617016421069n`; **M1 golden `544847093270497462n` untouched**. 427→438 tests; tsc + build clean; src/sim pure. Render: a distinct violet compute node + compute halo + a `DC … pwr/rej/CMP [PWR|THERM]/LIFT ×N.NN` bl readout; place key **M** (`'` cycles site). Screenshot-verified `436W pwr · 375W rej · CMP 8.3u [THERM] · LIFT ×1.23`. _Deferred to later M3/M4: autonomy tiers gated on DC compute (§4.6), cache/coherence + brokerage roles, DC upgrades, radiation/failure, eclipse shadowing, the §4.10 mint endgame; numbers are placeholders._
  - [x] **M2-fix — anchor the M2 schedulers to the session start + clamp shock compounding (live epoch-mismatch bug, SD-34).** The live `BuildSession` boots on the shared clock at the M1 scenario epoch (`SCENARIO.tick0` ≈ 14.5M s), but the M2 schedulers (event gen 2400 s, offer gen ~1800 s, demand-growth cadence from 0) were cursor-initialised near t=0, so the FIRST live step was ~14.5M s overdue → the catch-up loops fired their whole backlog (event gen hit its 64-cap), shock overlays compounded multiplicatively, demand → ~1e40, DEMAND·GROWTH readout → "+6.5e39%". Fix: a lazy first-step anchor (`anchorSchedulers(startT)` re-anchors all cursors relative to the session start; the `startT===0` golden path is byte-identical), a `MAX_SHOCK_MULTIPLIER=4.0` clamp on the compounded per-cell shock product, and a finite/sane-range guard on the orrery DEMAND·GROWTH segment (omitted in the degenerate case — never scientific notation). New `m2-epoch-anchor.test.ts`: 0 events on the first step at the 14.5M-s epoch (was 64), demand ratio 1.0 (was ~877× step-1 / ~1e38 sustained), bounded under sustained stepping, LIVE==REPLAY at the large epoch, t=0 first-event still at ≥2400 s. M2 golden stayed `8431658617016421069n`; **M1 golden `544847093270497462n` untouched**. 441→446 tests; tsc + build clean; src/sim pure. Sane readout shown via CONTRACTS preset (key 7) + heatmap (H) at the live epoch.
- [ ] **M4 — Interplanetary (the game becomes itself):** Mars + synodic windows, patched-conic planner, conjunction blackouts, full caching/prefetch/coherence, edge-processing DCs, autonomy tiers, constrained brokering.
- [-] **M5 — Outer system + DTN** *(post-1.0)*
- [-] **M6 — Optional information-economy endgame** *(post-1.0)*

---

## Cross-cutting (continuous from P0)

- [x] **X-01** Determinism & replay — fixed-tick clock ✅ (P0-03), seeded RNG ✅ (P0-04); golden-master green every milestone; +1 replay fixture per milestone.
- [ ] **X-02** Performance budget — GC allocation pools in Three.js (scratch vectors + direct Float32Array writes — proven in spike review); event-driven route re-solve; headless perf benchmark M2–M3; budget real before M4.
- [ ] **X-03** Accessibility — chrome/signal split + CVD-safe palette + purist toggle. "Colour-off fully playable" = per-milestone exit check.
- [ ] **X-04** Save/load robustness — JSON-serialisable from pure `src/sim/`; versioned saves + migration hook; fast snapshot load.
- [~] **X-05** Audio system — Web Audio API; one-way event-bus → cues. **First cue landed (M1-11/E5):** `src/audio/cue.ts` lazy gesture-unlock + `CueBus` (sim stays Web-Audio-free). Health-sonification M2+.
- [ ] **X-06** Content pipeline — `data/` JSON from M0-04; migrate each mock's constants as its real system lands; CI schema validation.

---

## Spike-specific findings (adversarial review, 2026-05-29)

All 7 findings fixed:
- ✅ Orrery hot loop allocated ~960 `Vector3`/frame → scratch vectors + direct `Float32Array` writes
- ✅ Terminator sun-direction computed in log-compressed space → uncompressed
- ✅ WM gesture listeners could stack on overlapping pointerdowns → gesture-active guard + `destroy()`
- ✅ `freshness()` degenerate-halfLife branch diverged from C# → matches `ageSeconds <= 0 ? 1 : 0`
- ✅ Edge-resize divider drifted from cursor by `frac·gutter` → gutter-excluded span
- ✅ Drag hit-test used a stale snapshot on mid-drag relayout → hit-tests the live layout
- ✅ Orrery titlebar lamp hardcoded `ok` → reflects Earth→Mars occult

---

## Follow-ups

- ⬜ Hide far/parent orbit rings in near-field presets (ORBITS shows stray distant dashes)
- ⬜ Wire Tab/Split/Close gestures in the WM
- ⬜ Port the WmModel unit tests to Vitest
- ⬜ Pool the ephemeris `Vec3` array returns (truth-layer still allocates small arrays)
- ⬜ Save/replay backbone — P0-06 ✅ (B1+B3); P0-05 format ✅ (B2); live action-log recording + snapshot scale/pause owed at E3

---

*Keep `[x]` honest — tick only when the plan's "Done when" is actually met. M1 was re-aimed v0.3 from the Mars-cache freshness loop to the Earth-orbit connectivity game (`docs/signal-horizon-m1.md` Part I); the old M1 cache code is reclassified to the Act 4 teaser + post-gate, not deleted. The Act 1 coverage opener is the immediate next build. The §7 routing solver is RESOLVED in spec v0.2 (the M1-SLV-* epic): M1 builds the reactive solver (latency + congestion, `w_stab=0`), the per-contract weight, the self-diagnosing trace view, and the predictability seed; everything in Acts 2–3 needs it. Re-detail M2+ after the M1 gate.*
